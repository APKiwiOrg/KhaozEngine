<Project>
  <!-- Central package management: every external package version is pinned HERE, once.
       csprojs carry only <PackageReference Include="..."/> with no Version attribute.
       Added in 9.0.0 after Silk.NET drifted apart (Windowing 2.22.0 vs Audio 2.23.0). -->
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>
  <ItemGroup>
    <!-- GPU / windowing / render stack -->
    <!-- APKiwi Veldrid 4.9.0 fork with opt-in serialized Direct3D11 immediate-context recording. See
         KhaozEngine.Gpu/GpuDeviceContext.cs for the D3D11-only opt-in and KhaozEngine #410 for field evidence. -->
    <PackageVersion Include="Veldrid" Version="4.9.103" />
    <PackageVersion Include="Veldrid.SPIRV" Version="1.0.15" />
    <!-- Already a transitive dependency of Veldrid 4.9.0 (its own D3D11 binding), declared explicitly in
         KhaozEngine.Gpu so the D3D11 driver-threading probe can call ID3D11Device::CheckFeatureSupport.
         Keep this pinned to whatever Veldrid depends on, so there is one D3D11 binding in the graph. -->
    <PackageVersion Include="Vortice.Direct3D11" Version="2.3.0" />
    <!-- FXC for the engine-owned native Direct3D11 backend (KhaozEngine.Gpu.D3D11), which compiles the
         cross-compiled HLSL to DXBC itself. Pinned to the SAME 2.3.0 Vortice line as Vortice.Direct3D11 above,
         not to the newest 3.x: both 2.3.0 packages resolve SharpGen.Runtime 2.0.0-beta.13 and
         Vortice.Mathematics 1.4.25, so the graph keeps one COM interop layer. A 3.x D3DCompiler beside a 2.3.0
         Direct3D11 would drag a second SharpGen.Runtime major into the same process, and Direct3D11 cannot move
         because it is pinned to what Veldrid depends on. -->
    <PackageVersion Include="Vortice.D3DCompiler" Version="2.3.0" />
    <!-- CVE override: Veldrid.SPIRV drags in vulnerable Newtonsoft.Json 9.0.1 (NU1903). Not used directly. -->
    <PackageVersion Include="Newtonsoft.Json" Version="13.0.3" />
    <PackageVersion Include="Silk.NET.Windowing" Version="2.23.0" />
    <PackageVersion Include="Silk.NET.Windowing.Glfw" Version="2.23.0" />
    <PackageVersion Include="Silk.NET.Input" Version="2.23.0" />
    <PackageVersion Include="Silk.NET.Input.Glfw" Version="2.23.0" />
    <PackageVersion Include="Silk.NET.GLFW" Version="2.23.0" />
    <!-- The Vulkan binding for the engine-owned native Vulkan backend (KhaozEngine.Gpu.Vulkan). Same 2.23.0
         Silk.NET line as the windowing, input and audio stacks above, which is decision V-P2 rather than a
         coincidence: a binding off the same generated line adds a package and not a vendor, shares
         Silk.NET.Core with what is already in the graph, and rides the lockstep upgrade the comment at the top
         of this file exists to enforce. The two extension packages carry the KHR and EXT entry points the
         backend loads by hand (swapchain, the three platform surfaces, debug utils), which are separate
         assemblies in this binding rather than members of the core one. -->
    <PackageVersion Include="Silk.NET.Vulkan" Version="2.23.0" />
    <PackageVersion Include="Silk.NET.Vulkan.Extensions.KHR" Version="2.23.0" />
    <PackageVersion Include="Silk.NET.Vulkan.Extensions.EXT" Version="2.23.0" />
    <PackageVersion Include="StbTrueTypeSharp" Version="1.26.12" />
    <PackageVersion Include="StbImageSharp" Version="2.30.15" />
    <PackageVersion Include="SharpGLTF.Core" Version="1.0.6" />
    <PackageVersion Include="SharpGLTF.Toolkit" Version="1.0.6" />

    <!-- Audio -->
    <PackageVersion Include="Silk.NET.OpenAL" Version="2.23.0" />
    <!-- OpenAL Soft natives version their own line (not Silk.NET 2.x). -->
    <PackageVersion Include="Silk.NET.OpenAL.Soft.Native" Version="1.23.1" />
    <PackageVersion Include="NVorbis" Version="0.10.5" />
    <PackageVersion Include="NLayer" Version="1.16.0" />

    <!-- Foundation / content / physics -->
    <PackageVersion Include="JsonSchema.Net" Version="9.1.3" />
    <PackageVersion Include="BepuPhysics" Version="2.4.0" />

    <!-- Netcode / stores -->
    <PackageVersion Include="LiteNetLib" Version="2.1.2" />
    <PackageVersion Include="Microsoft.Data.Sqlite" Version="10.0.9" />
    <PackageVersion Include="SQLitePCLRaw.lib.e_sqlite3" Version="3.50.3" />
    <PackageVersion Include="Microsoft.Data.SqlClient" Version="6.1.6" />

    <!-- Identity (opt-in OIDC provider) -->
    <PackageVersion Include="Microsoft.IdentityModel.Protocols.OpenIdConnect" Version="8.19.1" />
    <PackageVersion Include="Microsoft.IdentityModel.JsonWebTokens" Version="8.19.1" />

    <!-- ke-mapedit MCP server (author-time tool). MCP SDK pins its abstractions to 10.0.7. -->
    <PackageVersion Include="ModelContextProtocol" Version="1.4.1" />
    <PackageVersion Include="Microsoft.Extensions.Hosting" Version="10.0.7" />

    <!-- Packaging / tests / tools -->
    <!-- Roslyn for the localization analyzer. Pinned to the .NET 10 SDK's compiler line so the analyzer is
         never built against a newer Roslyn than the host compiler. -->
    <PackageVersion Include="Microsoft.CodeAnalysis.CSharp" Version="4.14.0" />
    <PackageVersion Include="Microsoft.SourceLink.GitHub" Version="8.0.0" />
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageVersion Include="xunit" Version="2.9.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="2.8.2" />
    <PackageVersion Include="SixLabors.ImageSharp" Version="2.1.13" />
  </ItemGroup>
</Project>
