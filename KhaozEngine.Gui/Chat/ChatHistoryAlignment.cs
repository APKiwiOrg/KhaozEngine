namespace KhaozEngine.Gui.Chat;

/// <summary>Vertical placement for history rows that do not fill the chat viewport.</summary>
public enum ChatHistoryAlignment
{
    /// <summary>Place sparse history against the top of the viewport.</summary>
    Top = 0,

    /// <summary>Place sparse history against the bottom of the viewport.</summary>
    Bottom = 1,
}
