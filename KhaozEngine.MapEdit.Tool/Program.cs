// ke-mapedit MCP server entry point.
// Placeholder for Task 1: the hosted MCP bootstrap (Host.CreateApplicationBuilder + AddMcpServer over
// stdio) lands in Task 5. Kept as a top-level return so the Exe builds and packs meanwhile.
return 0;
