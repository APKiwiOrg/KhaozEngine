<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>KhaozEngine.Netcode.LiteNetLib</PackageId>
    <!-- Graduated onto the shared 5.x line at 5.46.0 (was the 4.x <Version>); MonoGame-free foundation. -->
    <Version>$(KhaozEngineVersion)</Version>
    <Description>LiteNetLib transport binding for the channel-split kernel. The transport-free split contract (IChannelSplittable&lt;T&gt; + NetChannelReliability) now lives in KhaozEngine.Netcode; this package adds ChannelSplitter, which maps NetChannelReliability to LiteNetLib's DeliveryMethod (ToDeliveryMethod) and drives "split, send each non-empty part on its channel" (Sequenced vs ReliableOrdered) via Send, so reliable events never head-of-line-block position updates. The game keeps its own batch type and field layout.</Description>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="LiteNetLib" Version="2.1.2" />
    <ProjectReference Include="../KhaozEngine.Netcode/KhaozEngine.Netcode.csproj" />
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="KhaozEngine.Tests" />
  </ItemGroup>
</Project>
