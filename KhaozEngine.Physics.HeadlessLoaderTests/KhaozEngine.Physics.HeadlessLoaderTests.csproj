<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <OutputType>Library</OutputType>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>
  <!-- Intentionally references ONLY KhaozEngine.Physics + KhaozEngine.Physics.Bepu - the exact package
       set a headless authoritative server is allowed (Physics is in the Server umbrella; Physics.Bepu is
       the opt-in backend) - and NOT KhaozEngine.Render3D / Gpu / Windowing. This project compiling and its
       tests passing prove an MMO server with no GPU/windowing can load baked .coll collision shapes via
       PropCollisionFormat and build the same BepuPhysicsWorld the client predicts against. Adding a
       Render3D/Gpu/Windowing reference here breaks the build on purpose: that boundary is what this project
       guards. (Mirrors KhaozEngine.Netcode.DecouplingTests.) -->
  <ItemGroup>
    <ProjectReference Include="../KhaozEngine.Physics/KhaozEngine.Physics.csproj" />
    <ProjectReference Include="../KhaozEngine.Physics.Bepu/KhaozEngine.Physics.Bepu.csproj" />
  </ItemGroup>
</Project>
