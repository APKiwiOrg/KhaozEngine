<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <PackageId>KhaozEngine.Simulation</PackageId>
    <Version>$(KhaozEngineVersion)</Version>
    <Description>Headless authoritative simulation primitives: FixedTickHost, the worker scheduling seam, and a transport-free world clock stack with a strict codec, tick-owned host, client mirror, command queue, and restart anchor. Deterministic and renderer-free.</Description>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="../KhaozEngine.Determinism/KhaozEngine.Determinism.csproj" />
  </ItemGroup>
</Project>
