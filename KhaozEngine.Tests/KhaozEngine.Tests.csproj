<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <OutputType>Library</OutputType>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>
  <!-- No ProjectReference to any KhaozEngine.* package: the rump is ArchitectureTests.cs (pure XML parsing of
       every *.csproj) and CetCompatDefaultTests.cs (pure file/XML parsing of KhaozEngine.Foundation's packaging
       assets via a repo-root-relative path), neither of which has a single `using KhaozEngine.*` directive. -->
</Project>
