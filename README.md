# KhaozEngine

A shared, game-agnostic, **MonoGame-free** 2D/3D engine: windowing + input, a GPU abstraction, 2D and 3D
renderers, an immediate-mode + screen-stack GUI, audio, particles, an ECS, netcode, and the usual foundation
(content, persistence, localization, diagnostics). One implementation, used by four games (Hardpoint,
Nullwake, SpaceGame, Ruinborne), so a fix written once propagates to all of them.

KhaozEngine is split into focused, independently-referenceable NuGet packages plus a few umbrella metapackages,
so a game pulls in just what it needs (and a logic library or headless server can pull a renderer-free subset).

| Package | What it gives you | Depends on |
|---|---|---|
| **KhaozEngine.Primitives** | The zero-dependency leaf (`System.Numerics` only): `Color` (`FromHex`/`ToHex`, `* float`, `Lerp`), `DeterministicRng`, `XorRng`, `MathUtil` (clamp/lerp/smoothstep plus the angle helpers `WrapAngle`/`DeltaAngle`/`MoveTowardsAngle`/`LerpAngle`), `ViewportMath` (fit/cover + device-pixel snapping for DPI-aware UI), `Easing`, `Rect` + `IDesignViewport` (the design-viewport seam, incl. `SnapsToDevicePixels`, moved from Windowing in 9.0.0), `ObjectPool<T>`/`IPoolable`/`PoolRental<T>` (free-list pool, rented through a handle that names one rental rather than one slot so a stale return is refused, absorbed from the retired Pooling package in 9.0.0), and `TrailSampler`/`TrailPoint` (a pure render-free ring of timed motion-trail samples bounded by max age + count, feeding `Scene3D.DrawTrail`), plus display formatters `NumberFormatter`/`NumberNotation` (idle-game large numbers: Simple suffix / Scientific / Engineering, NaN/Inf-safe) `TimeFormatter`/`DurationStyle` (durations as a `Clock` `1:02:34` or a `Coarse` `2h 15m`), `VersionComparer` (numeric, dot-separated `x.y.z` version compare, missing/non-numeric segments count as 0, null/blank as the empty all-zero version, the shared rule `KhaozEngine.Updates.UpdateVersion` and `KhaozEngine.ServerStatus.VersionOrder` both delegate to), and `RayMath` (allocation-free ray-vs-AABB slab test `IntersectAabb`, unnormalized-direction `t` scaling, for editor picking and future spatial queries). The bottom of the dependency graph. An RGBA color anywhere in the public API is a `Color`. | Pure .NET |
| **KhaozEngine.Gpu** | The GPU backend seam: backend selection (since 17.40.0 the OS probe answers the engine's OWN backend on every platform, `MetalNative` / `Direct3D11Native` / `VulkanNative`, and since 18.0.0 those are the ONLY backends: the Veldrid incumbent was deleted and the four members it implemented, `Metal` / `Vulkan` / `Direct3D11` / `OpenGL`, are RETIRED tokens that `GpuBackendSelector.IsRetired` names, `NativeReplacementFor` maps to a live backend, and `GpuBackendRetiredException` refuses when a caller names one in code) plus its provenance (`GpuBackendSelector.Resolve` returns a `GpuBackendSelection` saying whether the OS probe, a stored user preference, or a `KE_GRAPHICS_BACKEND` override decided, keeping the raw override value, and `GpuDeviceContext` logs it once per device so an unrecognized override warns instead of silently reading as the OS default), an in-game-selectable backend (a `GpuBackendKind?` preference passed in as data ranks between the env override and the OS probe, `IsBackendSupported`/`SupportedBackends()` functionally probe what a settings UI may offer, and device creation falls back to the platform's own default and reports `FallbackAfterFailure` rather than leaving a player stuck on a choice that cannot start, which is also what a STORED preference for a retired member reports so the game clears it, headlessly as well as windowed since 17.40.0, and a stored preference with no registered provider falls back the same way since it may name a kind this build has no package for, while a DEFAULT or a `KE_GRAPHICS_BACKEND` pin with no provider throws `GpuBackendProviderMissingException` and the 17.40.0 `DefaultProviderMissing` source is retired at 18.0.0 with no producer, none of which an ordinary game meets because `AppWindow` and both snapshot hosts call `GpuBackends.RegisterResolvedIfUnregistered()` for the RESOLVED kind plus the platform's own, and a fallback that fails as well throws `GpuNoUsableBackendException` naming both backends and both reasons), the Direct3D11 driver threading probe (`GpuThreadingCaps` / `GpuThreadingDiagnostics`, read off the live device and logged next to the backend line, with a WARN when the driver cannot build command lists and Windows is emulating them in software), per-device diagnostics for a Windows performance report (`AdapterDescription` logs which adapter ran on every backend, and on Windows `GpuInjectedModules` scans the process for known overlay/capture injectors and WARNs per match, both also on `GpuDeviceContext` / `AppWindow`), the env-gated `GpuD3D11DeviceFlags` probe (`KE_D3D11_PREVENT_THREADING_OPTIMIZATIONS` adds `D3D11_CREATE_DEVICE_PREVENT_INTERNAL_THREADING_OPTIMIZATIONS` to device creation, off by default, a diagnostic lever rather than a setting), the device + command-list abstraction every backend implements (this package builds NO device of its own since 18.0.0, and no Veldrid package is left in the graph at all: the shader toolchain that survived the backend deletion was itself swapped for `Silk.NET.Shaderc` plus `Silk.NET.SPIRV.Cross` in the same release), deferred mid-life resource retirement (`GpuRetireQueue`: fence-polled where the device can signal GPU completion, frame-counted where the caller cannot mint a fence, so a renderer frees a streamed mesh or an evicted sprite set without a drain per unload, with a `MaxSealedBatches` safety valve that trades the poll for one drain and frees the lot when the CPU runs away from the GPU), and compute shaders (compute pipelines, storage textures + buffers, `Dispatch`, typed buffer readback, gated on `GpuCapabilities.SupportsCompute`). `ShaderValidation.ValidatePair` / `ValidateCompute` device-free validate a GLSL shader pair or compute source by cross-compiling it to HLSL/MSL/GLSL/ESSL on the CPU, and `GpuCapabilities.SupportsShadowMaps` reports whether the device can drive the shadow-map path. The only graphics-API-aware layer. | Diagnostics, Primitives (+ Veldrid.SPIRV and the Veldrid base package it reflects through) |
| **KhaozEngine.Gpu.D3D11** | The engine's own native Direct3D 11 backend for the `KhaozEngine.Gpu` seam, and **the DEFAULT backend on Windows since 17.40.0** (carried by the `Game2D` and `Game3D` umbrellas since 18.0.0, along with its two siblings, because `KhaozEngine.Gpu` builds no device of its own any more). One public entry point, `KhaozEngineD3D11.Register()`, registers an `IGpuBackendProvider` for `GpuBackendKind.Direct3D11Native`, so opting in is a package reference plus one line at startup rather than a `[ModuleInitializer]` the CLR may never run (a missing registration then throws naming that line instead of silently falling back onto a different backend and filing a soak session's numbers under the wrong name). Targets `net10.0`, deliberately not `net10.0-windows`: the Windows boundary is `[SupportedOSPlatformGuard("windows")]` entry points over `NoInlining` Vortice bodies, so the assembly compiles and its device-free tests run on Linux and macOS while the Vortice interop stays off the load path there, and CA1416 enforces the boundary at compile time. The machine-capability probe behind `IsBackendSupported` creates a throwaway feature level 11_0 device (hardware, then WARP) and checks the two hard device requirements the backend cannot work without, `ConstantBufferOffsetting` and `MapNoOverwriteOnDynamicConstantBuffer`, so an incapable machine is reported and falls back rather than crashing on the first frame. Carries no `Veldrid` edge of its own (asserted on both the project graph and the built IL): the SPIRV-Cross shader path stays in `KhaozEngine.Gpu` behind an internal, Veldrid-free cross-compile helper. Diagnostics: `KE_D3D11_ADAPTER=warp|hardware|<index>|<name substring>` pins which adapter the device is created on (an unhonourable request WARNs, lists what WAS enumerated, and falls back to letting DXGI pick, never fails), `KE_D3D11_DEBUG=1` adds `D3D11_CREATE_DEVICE_DEBUG` and pumps `ID3D11InfoQueue` into the engine log rate-limited with corruption and error raised to WARN, and a device loss calls `GetDeviceRemovedReason` at the first site that notices, latches it, flips the liveness token and surfaces it in the telemetry session header alongside the software-adapter flag. Capabilities were at parity with the incumbent field for field, `SupportsCompletionFences` excepted, with an out-of-range MSAA request throwing at texture creation rather than silently degrading. **Device creation is real**, so `CreateForWindow` and `CreateHeadless` build a device on Windows, the OS probe answers this backend there, and `KE_GRAPHICS_BACKEND=direct3d11-native` (or an explicit `GpuBackendKind.Direct3D11Native`) names it deliberately. The `direct3d11-native` CI leg is blocking and owns the `direct3d11-native` golden family, verifying it on a pinned WARP adapter. Since 18.0.0 it is the only Direct3D 11 path the engine has: the incumbent `Direct3D11` member is a retired token that resolves here. | Gpu, Diagnostics (+ Vortice) |
| **KhaozEngine.Gpu.Vulkan** | The engine's own native Vulkan backend for the `KhaozEngine.Gpu` seam, **the DEFAULT backend on Linux since 17.40.0** (carried by the `Game2D` and `Game3D` umbrellas since 18.0.0, along with its two siblings), and phase 3 of the staged native GPU backend program. **REGISTRATION, PROBE, A HEADLESS DEVICE, ITS COMPLETION TIMELINE, ITS MEMORY ALLOCATOR, THE COMMAND LIST'S LIFECYCLE, THE UNIFORM RING, THE RESOURCE FACTORY, THE DESCRIPTORS, THE BIND FLUSH, DYNAMIC RENDERING, THE SHADER PATH, THE PIPELINES, THE WINDOWED SWAPCHAIN, THE BARRIER TRACKER, THE CAPABILITY, COUNTER AND DIAGNOSTICS READS AND THE DRAW, DISPATCH AND TRANSFER PATHS as of 17.34.0**: `KhaozEngineVulkan.Register()` registers a real provider, that provider answers a real functional machine probe (a Vulkan loader, a throwaway instance at the 1.3 floor, every physical device read against the design's requirements, and the instance destroyed before the answer returns), and `GpuDeviceContext.CreateHeadless(GpuBackendKind.VulkanNative)` builds a real `VkDevice` and graphics queue on one refcounted process `VkInstance`, with features enabled by name, a device-loss latch that fires in Release and the `KE_VULKAN_DEVICE` / `KE_VULKAN_VALIDATION` levers ([#514](https://github.com/APKiwiOrg/KhaozEngine/issues/514)). That device owns ONE timeline semaphore with a real `IGpuFence`, a counted `vkWaitSemaphores` drain and the deferred-disposal retire list ([#515](https://github.com/APKiwiOrg/KhaozEngine/issues/515)), plus an engine-owned block suballocator ([#516](https://github.com/APKiwiOrg/KhaozEngine/issues/516)): chunks pooled by `(memoryTypeIndex, linear|optimal)` so `bufferImageGranularity` is satisfied by separation rather than by the incumbent's per-allocation rounding, first-fit with alignment correction and coalescing free, host-visible chunks mapped once at creation and never unmapped, dedicated allocations on driver preference or above a size threshold, and real flush and invalidate on the non-coherent path that the incumbent had nowhere at all. It also hands out real command lists ([#517](https://github.com/APKiwiOrg/KhaozEngine/issues/517)): `FramesInFlight` `VkCommandPool`s per list reset with `vkResetCommandPool` rather than the incumbent's one pool with `RESET_COMMAND_BUFFER`, a slot wait counted as backpressure through `KE_VULKAN_FRAMES_IN_FLIGHT`, pools handed to the retire list when a list is disposed in flight, and `Submit` as ONE `vkQueueSubmit` whose timeline value is allocated inside the lock that orders it, with a failed submit registering nothing so the drain can never be left waiting for a value nothing will signal. There is no CPU op stream and no second recording driver, which is a decision rather than a gap: a `VkCommandBuffer` already IS the deferral the Direct3D 11 recorder had to build. It owns the UNIFORM RING too ([#518](https://github.com/APKiwiOrg/KhaozEngine/issues/518)): every `UniformBuffer`-usage buffer is ONE `VkBuffer` of `stride * FramesInFlight` in host-visible coherent persistently mapped memory, with the per-frame base applied at BIND so the `IGpuBuffer` identity never changes, a segment recycled against the timeline's COMPLETED value rather than a submit receipt, and a record-time `UpdateBuffer` that is a `memcpy` with no staging buffer, no copy command, no barrier and no render-pass split (the shipped incumbent's same call was a render-pass split plus a full pipeline flush plus a global barrier whose destination mask did not even cover a uniform read). A device-level `UpdateBuffer` reaches EVERY segment and NEVER BLOCKS, deferring a segment still in flight as a pending patch the next frame boundary replays, which is [#484](https://github.com/APKiwiOrg/KhaozEngine/issues/484)'s correction adopted wholesale. **One backend divergence lands with it and is documented rather than discovered: a buffer created `UniformBuffer` combined with any other bindable usage is REFUSED at creation, where `GpuBackendKind.Vulkan` accepted it.** Bulk payloads take a per-list staging arena instead, pooled by size class with a real 8 MiB retention cap against the incumbent's 512 bytes, with the copy barrier narrowed to the destination's actual usage. The ring's SEMANTIC policy is tested against BOTH backends' rings through one internal test-only interface in `KhaozEngine.TestSupport.Gpu`, which is what the decision to share the tests rather than the code owes. It has a REAL RESOURCE FACTORY too ([#519](https://github.com/APKiwiOrg/KhaozEngine/issues/519)): every `VkImageView` is created at RESOURCE creation from the declared usage bits and none at a bind or a draw, with no view factory reachable from the recording type at all, because all 25 `DEVICE_REMOVED` stacks in [#423](https://github.com/APKiwiOrg/KhaozEngine/issues/423) surfaced inside a lazy view constructor on the draw path and no counting seam can see a `vkCreateImageView`. Every texture is assigned its canonical RESTING LAYOUT at creation, which is what lets lists compose in any submit order. NO texture creation submits anything: the incumbent issued a whole `vkQueueSubmit` per render target and per sampled texture created, so two hundred textures meant two hundred submissions before a frame was drawn, and here the clear and the first-ever transition are appended to ONE device-owned setup command buffer under its own short lock, flushed at the next submit or at any device-level read. The clear is preserved deliberately, because undefined contents are not stable across runs. Staging is a `VkBuffer` with the incumbent's SOFTWARE subresource layout reproduced byte for byte and pinned by a checked-in 63-row table taken from the incumbent's own arithmetic, because every golden reads back through `Map` and `MappedData.RowPitch` and a different arithmetic garbles all 36 at once. `Map(staging, Read)` waits on the timeline's last submitted value first, counted as a drain, which is the thing Direct3D 11's `Map(READ)` did implicitly. The shared point and linear samplers WRAP on all three axes, built from wrap-addressed descriptions and NOT from the identically named `GpuSamplerDescription` statics, which default every axis to clamp and cost two goldens on the Direct3D 11 leg. Disposal is one TERMINAL retire per resource, destroying children inline rather than re-retiring them, which bounds the retirement depth at the generation the teardown already drains twice for. **A second documented divergence lands with it: `GpuBufferUsage.Dynamic` does not make a buffer CPU-mappable there, where it did on the Veldrid leg deleted in 18.0.0.** It has DESCRIPTORS too ([#520](https://github.com/APKiwiOrg/KhaozEngine/issues/520)): `VkDescriptorSetLayout` and `VkPipelineLayout` are CONTENT-DEDUPLICATED, which is not a micro-optimisation but the thing that makes bound descriptors survive a pipeline switch (Vulkan decides pipeline-layout compatibility by comparing set layouts, so one handle per distinct content turns that into a pointer compare, where the incumbent created one per `ResourceLayout` object and therefore made nothing ever compatible with anything). Every uniform buffer binds as `UNIFORM_BUFFER_DYNAMIC` rather than only the elements declared dynamic, because the per-frame ring base has to be applied at bind and the dynamic offset is the only bind-time knob Vulkan offers on a uniform buffer, so the declared flag decides exactly one thing: whether the caller's own per-draw offset is added on top. Pools are sized from ACTUAL demand rather than the incumbent's fixed `maxSets = 1000` with 100 descriptors of each of seven types, and freeing restores EVERY counted type: the incumbent's free path restored five and forgot `UniformBufferDynamicCount` and `StorageBufferDynamicCount`, so an application that churned dynamic-offset resource sets leaked pool budget forever, which is aimed squarely at a consumer whose sets are overwhelmingly dynamic-offset ones and whose map editor churns them on every document load. A set is allocated and written ONCE at creation with one `vkUpdateDescriptorSets` covering every binding, and the descriptor's range is the BIND WINDOW: never `VK_WHOLE_SIZE`, and never the ring stride, which is the shape that looks safe and overruns the buffer at the last frame slot by exactly the caller's own offset for the five shipped renderers that pass a non-zero one. Zero descriptor allocations and zero descriptor writes during recording is enforced STRUCTURALLY, because neither call is a bind, a draw or a barrier and no counting seam can ever see them: the pool is unreachable from the recording type. And a device-free test asserts every one of the 33 shipped pipeline shapes stays at or under Vulkan's required minimum of 8 dynamic uniform buffers, so a layout combination that would break a minimum-spec device fails on the free Linux leg rather than on a player's machine. Descriptor indexing, bindless, push constants and descriptor buffers are all declined with an argument rather than an omission, and the trigger that reopens them is named. All four resource-set BINDS are live too ([#521](https://github.com/APKiwiOrg/KhaozEngine/issues/521)): a bind RECORDS ONLY into a per-slot `(set, offset)` array with TWO dirty states rather than the Direct3D 11 backend's three, because a Vulkan descriptor bind is one call whether one offset moved or every image in the set changed, so a third state would change no call and skip no work. The flush emits ONE `vkCmdBindDescriptorSets` per CONTIGUOUS RUN of dirty slots with `firstSet` at the run's start, so a full activation is one call carrying every set and an offsets-only rebind is one call carrying one, which is the whole Vulkan argument for the descriptor model and a number a budget ported from the other backend could not even express. `pDynamicOffsets` is POSITIONAL and covers sets the caller never named: one entry per dynamic descriptor in the run, in set-then-binding order, each composed as `ringBase + rangeOffset + (declaredDynamic ? engineOffset : 0)`, recomposed once per run rather than accumulated across a flush, which is the incumbent's own defect (it reset the batch count and the first set but not the offset count, so a second batch passed a too-large count built from stale entries) not being inherited. A device-free test composes the array for all 33 shipped layout shapes at every frame slot and asserts each entry plus the descriptor's range stays inside the buffer, which is the offset `VUID-vkCmdBindDescriptorSets-pDescriptorSets-01979` measures. Each entry is also checked at the flush against the ring's derived alignment (`VUID-vkCmdBindDescriptorSets-pDynamicOffsets-01971`), refused by name with the slot left dirty. A pipeline switch invalidates recorded slots from the first INCOMPATIBLE set onward, computed as the longest common prefix of the two pipeline layouts' set-layout handle sequences, which content dedup is what makes both correct and cheap. That prefix is GUARDED rather than trusted, because a prefix longer than the truth leaves a set the driver invalidated marked clean and the next draw renders wrong without throwing: a device-free test walks all 1089 ordered pairs of the 33 shipped pipelines and asserts the computed prefix never exceeds the true prefix of identically DEFINED set layouts (compared by binding table, not by handle, so the guard is not a restatement), and under `KE_VULKAN_VALIDATION` the flush asserts every bound set's layout is the pipeline layout's set layout at that index. It has DYNAMIC RENDERING too ([#522](https://github.com/APKiwiOrg/KhaozEngine/issues/522)), and the headline there is an absence: there is no `VkRenderPass` and no `VkFramebuffer` anywhere in the backend, so no cache for either and no invalidation of either on a resize, against an incumbent that created three passes per framebuffer with no cache, deduped nothing across framebuffers of identical format, and rebuilt all of it every resize. `CreateFramebuffer` therefore makes no driver object at all and its disposal releases nothing, because every attachment view already exists on the texture. `vkCmdBeginRendering` is DEFERRED to the first draw, so a clear recorded after `SetFramebuffer` folds into `loadOp = CLEAR` instead of costing a `vkCmdClearAttachments`, while a clear arriving after the pass opened still issues one. `storeOp` is `STORE` unconditionally and `DONT_CARE` is not expressible, because undefined contents are not stable across runs and the goldens require stability on the same rasterizer. The clear-only pass is reproduced deliberately: a bind plus a clear plus an `End` with no draw still clears, through a begin and end pair, flushed at `End` and at the next framebuffer change, and it needs no did-a-draw-happen flag because a begin CONSUMES the pending clears. Every command illegal inside a render pass instance ends it first, through one helper with one device-free test. `SetFramebuffer` emits a viewport and a scissor ON A CHANGE ONLY, reproducing Veldrid's own identity guard whole: a backend that does not emit rasterises nothing, and one that emits unconditionally silently restores the full scissor over a live one on the shipped bind-scissor-draw-rebind-draw sequence, which is golden-visible. That viewport carries NEGATIVE height, which is what makes Vulkan's clip space match Direct3D's and why `ClipSpaceYInverted` stays false and `GpuClip.Correct` stays the identity: getting it wrong does not throw, it renders every golden upside down, so a device-free test asserts the emitted height alongside the goldens and the capability parity test. The rendering class got its OWN device-free seam rather than widening the budget seam, because nothing about a begin, an end, a viewport or a clear scales with draw count and freezing marginals over them would gate on figures nobody should gate on. It has a SHADER PATH too ([#526](https://github.com/APKiwiOrg/KhaozEngine/issues/526)), and it is the shortest one in the engine because there is no cross-compilation on it at all: Vulkan consumes SPIR-V, so each GLSL 450 source goes through the engine's own front end and `vkCreateShaderModule` takes the bytes verbatim, with no HLSL, no FXC, no register scheme to invent and no reflection read back off the module. `SpirvCrossCompile` was SPLIT along the seam it already had, into a glslang front end this backend depends on and a SPIRV-Cross back end it names no member of, asserted over the built IL because both halves live in `KhaozEngine.Gpu` and every other scan reads identically either way. That split is the whole Metal-facing carrying cost of the phase and it makes the eventual direct-SPIRV-Cross migration ([#462](https://github.com/APKiwiOrg/KhaozEngine/issues/462)) a change to one half of one file. The claim that the native backend hands `vkCreateShaderModule` the SAME bytes the incumbent did is TWO artefacts rather than one: a `SpirvFrontEndPin` stating the options as constants with a derived cache identity, and a one-off in-process measurement, taken 2026-08-08 before the first golden run, that compiled all 34 shipped graphics programs and all 8 compute kernels through both paths and found 76 of 76 stages byte-identical, which is what licensed the committed `vulkan` goldens carrying over without a rebake. The per-program hash table shipped alongside is a DRIFT detector and its header says so, because a wrong emission baked once passes forever. `VkShaderModule`s are deduplicated by SPIR-V hash within a device, so those 76 stage emissions are 59 distinct modules and `IGpuShaderSet.Dispose` releases nothing. And the `set` and `binding` numbers, which Vulkan inherits from the shared GLSL rather than inventing, get a device-free table test over every shipped program: N indexes the pipeline's layout array and M the layout's element array, and `SpriteBatch` declares its UBO at `set = 1`, so "the UBO set comes first" is false in shipped code and a reordered layout array would be caught by nothing else. It has PIPELINES too ([#523](https://github.com/APKiwiOrg/KhaozEngine/issues/523)), which close the resource factory's refusal list entirely: a pipeline create-info here names no `VkRenderPass` and no meaningful subpass, because the target's colour formats and depth format ride a `VkPipelineRenderingCreateInfo` built off the seam's own `GpuOutputDescription`, which is the whole of what a render pass would have carried and is why there is no pass to cache and nothing to invalidate on a resize (the same description's sample count rides the multisample state instead, since the rendering create-info has no sample-count field). Vertex input comes from the caller's own `GpuVertexLayoutDescription` list with no reflection read off the module, numbered as ONE flat location sequence across every buffer slot (restarting per slot reads the instance stream's first attribute as the vertex buffer's second, which renders plausible garbage rather than throwing) and packed within each, with a declared stride winning over the computed one. An instance step rate above 1 is refused by name, because Vulkan's core input rate is two-valued with no divisor and flattening a 2 to a 1 would draw every instance from the same element silently. Dynamic state is exactly viewport and scissor, expressed as a value a device-free test reads rather than a line inside a create-info no headless test can see, so everything else including the constant blend colour is baked into the pipeline object. **A blend state count that is not the colour output count is REFUSED by name, in both directions**, because Vulkan requires the two to be equal, the seam lets them differ with nothing checking, and a mismatch is a validation error at creation on a shipped renderer. Repairing it would invent a per-backend semantic for an attachment nobody described, where the Direct3D 11 native backend answers the same description with its own struct defaults, and `GpuPipelineDescription.BlendAttachments` is already documented as one per colour output with all 22 shipped call sites declaring exactly that. The `VkPipelineCache` is PERSISTED, against an incumbent that passed `VkPipelineCache.Null` at both creation sites and therefore recompiled every pipeline from SPIR-V on every launch across considerably more permutations than programs: one cache is created with the device, keyed on `(pipelineCacheUUID, driverVersion, engine version)` where the UUID is the vendor's OWN validity key, header-validated before `pCacheData` is ever passed, written back through a process-unique temp-and-move so a reader sees a whole entry or none, and best-effort at every step so a missing, unreadable, truncated or foreign file is a colder start rather than a throw. A driver that refuses a blob this backend accepted gets one retry with no seed AND the refused file deleted, which takes both: the retry rescues the run, and the delete is what stops the same rejected blob being read, seeded and refused once per launch after an unclean exit. `KE_VULKAN_PIPELINE_CACHE` relocates the cache or turns it off. `SetPipeline` emits `vkCmdBindPipeline` and then invalidates recorded descriptor slots from the first incompatible set onward, with a pipeline-identity guard in front that is strictly stronger than the layout one: two different programs sharing a layout each bind and invalidate nothing, and the same pipeline twice does neither. Creating a pipeline is UNREACHABLE from the recording type, which is the descriptor rule applied to the most expensive thing a driver does on demand, so a command list holds a separate one-call binder that can bind a pipeline that exists and can make none. It has a WINDOWED SWAPCHAIN too ([#527](https://github.com/APKiwiOrg/KhaozEngine/issues/527)), so `GpuDeviceContext.CreateForWindow` reaches a real native device: one platform surface chosen from `GpuWindowKind`, candidates filtered on whether their graphics family can present to it, `VK_KHR_swapchain` on the device, and a swapchain the present boundary acquires from, resizes, recreates and presents, behind an `IGpuDevice.SwapchainFramebuffer` whose identity never changes while every image view underneath it is replaced. Not one line of that path runs in CI on any leg, ever, because a headless Vulkan device enables no surface extension at all, which is what shaped the row: the create-info is decided in a pure function and the whole present boundary sits above a two-interface seam, so the format, the present-mode ladder, the image count, the extent clamp, the retry count, the retirement order and the counter are asserted device-free while a human at a window is the only instrument for the rest. The create-info is REPRODUCED from the incumbent exactly (`B8G8R8A8_UNORM` in `SRGB_NONLINEAR`, `min(maxImageCount, minImageCount + 1)` images, `COLOR_ATTACHMENT | TRANSFER_DST`, `OPAQUE`, `clipped`, no MSAA and no depth), including `FIFO_RELAXED` under a vsync request, which permits tearing on a late frame and is arguably the wrong answer and is reproduced anyway because the pacing work ([#380](https://github.com/APKiwiOrg/KhaozEngine/issues/380)) is where that gets decided with a measurement. Two deliberate departures, both bugs rather than behaviours: `preTransform` reads the surface's `currentTransform` rather than being hardcoded to `IDENTITY`, and the incumbent's sRGB fallback compared an already-`Undefined` format against an sRGB one so its intended throw was dead code, where this one refuses for real. The one behaviour that CHANGES is forced rather than preferred: the incumbent blocked the CPU on a fence every frame and then presented with no wait semaphore at all, which was a specification violation a validation layer flags, and a design that gates on validation cannot deliberately reproduce a configuration validation rejects. So the acquire TIMING is kept and the SYNCHRONISATION is replaced, with `KE_VULKAN_ACQUIRE=stall` restoring the incumbent's shape exactly for the A/B, documented as not usable with the validation knob, and removed at rollout gate 4 with the blocking path deleted. The acquire semaphores are a ring indexed by a MONOTONIC ACQUIRE COUNTER and never by image index, which is the most common Vulkan swapchain bug and manifests as an intermittent hang rather than a clean failure, sized `max(FramesInFlight, imageCount) + 1` because acquires are paced by the presentation engine while recording is paced by the frame loop. The `OUT_OF_DATE` boundary is four questions answered in one method: the recreate runs at that SAME boundary so the failed acquire's semaphore is retired by its unconditional drain rather than reused or destroyed while pending, ONE fresh acquire follows before the boundary returns, the retry is ONE so a surface mid-resize cannot spin it, and an imageless frame binds a device-owned ORPHAN TARGET and then records, submits and completes exactly like any other frame with only its present skipped, which `FramesBegun` still counts. A minimised window is survivable by ARITHMETIC rather than by a special case: its surface reports every extent as zero, the clamp produces zero, and the spec then reads as not creatable, so `vkCreateSwapchainKHR` is never called at a size the specification forbids. A resize, a runtime `SyncToVerticalBlank` change and a checked `vkQueuePresentKHR` result all queue the same recreate, coalesced and applied at the next boundary on the submit thread, and Vulkan cannot change a present mode in place so vsync is a full recreate here rather than the argument of a present call it is on Direct3D 11. The new views are published onto the existing wrapper BEFORE the old ones are destroyed, every time, and a creation that fails at a creatable extent keeps the old generation and retries, because that is the only response with no window in which the wrapper names a dead view. **One seam member pair is added with it: `GpuDeviceCounters.AcquireWaitCount` and `AcquireWaitMs`, with the `gpuAcquireWaits` and `gpuAcquireWaitMs` channel names**, without which the acquire A/B has only mean frame time to read and both positions produce the same mean by construction on a machine pinned at its refresh rate. The native Direct3D 11 device passes zero for the pair, which is the honest reading on a backend whose present has no acquire to wait on. It has a BARRIER AND LAYOUT TRACKER too ([#524](https://github.com/APKiwiOrg/KhaozEngine/issues/524)): every image layout transition is a `vkCmdPipelineBarrier2` whose stage and access masks are answered per LAYOUT rather than per layout PAIR, so the pair space is total by construction and a layout it does not know throws by name, against an incumbent that ran a 25-arm if/else over pairs, ended it in a debug assertion, and in Release emitted `NONE` on both stage masks for a pair it did not handle, which was a barrier that synchronised nothing on a path whose only signal compiled away. Tracking is per subresource RANGE and per COMMAND LIST, against the canonical resting layout each texture was assigned at creation: a list assumes every texture is at rest when it starts, transitions as it needs, and restores everything it touched before `End`, so nothing shared is read or written during recording, two lists cannot disagree, and their submissions compose in any order. The incumbent kept that state ON the texture instead, so the loser of a two-recording race recorded either a redundant barrier or NO barrier for a transition it needed, which was a corruption no golden on a software rasterizer would show. **What the ruling LOSES is recorded rather than left to be discovered: `OpenListTrackingGpuDevice` passes TRIVIALLY on this leg**, exactly as it does on the Direct3D 11 native leg, so a reader must not take it as evidence about this backend. The attachment transitions the dynamic-rendering row deferred here land at the DEFERRED begin, as one batched barrier immediately before `vkCmdBeginRendering` and never inside the instance, where the same call means something much narrower, and an attachment already resting in the layout it is about to be used in costs nothing at either end (which is every plain render target, while a post-chain target that is also `Sampled` pays one barrier in and one back). A transition out of `VK_IMAGE_LAYOUT_UNDEFINED` DISCARDS the image's contents, so it is refused at the general entry point and permitted only through the two named ones that may do it: a texture's first-ever transition and a swapchain image reacquired for a frame that will fully overwrite it. A transition to the layout an image is already in emits nothing at all, which is what keeps the barrier count proportional to passes times touched textures rather than to draws, asserted directly rather than assumed. It reports on ITSELF honestly too ([#528](https://github.com/APKiwiOrg/KhaozEngine/issues/528)): the capability read is a device-free type whose five constants, device-name normalisation and sample-count floor are plain `[Fact]`s on a machine with no Vulkan loader, and it was held against the incumbent's own answers with ZERO permitted differences while the incumbent existed, which was a stricter bar than the Direct3D 11 backend's because there was no incumbent capability defect to correct here (the incumbent's capability read already answered true for completion fences on Vulkan). The comparer behind that was held against every public member of `GpuCapabilities` by reflection, so a member appended later could not make the assertion quietly weaker. One real difference was found and fixed writing it: the read was carrying the LOGGABLE device name, which substitutes `unnamed device 0x...` for a driver that reports nothing, where the incumbent substituted nothing at all, so the seam now carries the driver's own string with the empty case left empty. `MaxMsaaSampleCount` stays pinned to one sample until [#525](https://github.com/APKiwiOrg/KhaozEngine/issues/525) reproduces the incumbent's own `GetSampleCountLimit`, because two drafts of the design each invented a formula, the two differ, and both then asserted equality with the incumbent as a test. The `GpuDeviceCounters` fill is checked as nine READINGS rather than an absence, with the three present-boundary fields reading zero on a headless device because that is literally true of a device with no swapchain, and `DrainCount` documented as NOT comparable against the Direct3D 11 native backend's, which counts every `WaitForIdle` because its drain must signal and flush to know it is idle. Both `GpuDeviceDiagnostics` fields are asserted from the device through `WithGpu` into the header JSON, which is rollout gate 5's fifth clause. The two-device rows are `[GpuFact]`s that RUN on the `vulkan-native` leg [#529](https://github.com/APKiwiOrg/KhaozEngine/issues/529) installs, against lavapipe, and a green run anywhere else is still not evidence about this backend. It DRAWS too ([#525](https://github.com/APKiwiOrg/KhaozEngine/issues/525)), which closes the command list's refusal list entirely: the vertex and index binds, both `Draw` overloads, `DrawIndexed`, `Dispatch`, both texture copies, the buffer copy, `GenerateMipmaps` and `ResolveTexture` are all live, so a windowed run presents a frame the backend really rendered. The pre-command ORDER is a type of its own rather than four steps repeated at five members, because a missing step renders plausibly wrong rather than throwing: every image the bound sets name goes into the layout its binding needs BEFORE the render pass instance opens (a barrier inside a dynamic-rendering instance is a different and much narrower call), then the deferred begin, then the vertex and index binds as one call per contiguous run of dirty slots, then the descriptor flush and the command as one pair with nothing between them. A rebind of a vertex buffer already recorded emits nothing, where the incumbent issued one per call with no comparison at all. That first step IS compute rule 1: a storage texture a dispatch wrote is moved to `SHADER_READ_ONLY_OPTIMAL` where the sampled bind is assembled, as a real image barrier rather than the incumbent's queued layout restore armed by a usage flag, from images a resource set carries as plain data resolved at CREATION with the range each binding's own view covers. **Compute rule 2 is honoured AS WRITTEN and no seam member is added**, and the backend additionally emits one read-after-write barrier per REAL dependency between dispatches in one list, driven by a set of written resources rather than by a barrier per dispatch: that is evidence for the automatic-hazard seam capability ([#461](https://github.com/APKiwiOrg/KhaozEngine/issues/461)) and explicitly not a contract change, because a consumer that drops the drain breaks on Metal. A texture copy is one of four shapes decided by the two textures' staging flags, with the readback direction taking its buffer offset and row terms from the STAGING side's software layout and its level and layer from the IMAGE side's, and a whole-texture copy naming every level and every layer at its own offset. The mip chain is one halving blit per level with the two levels in disjoint ranges each step, which is exactly the shape the tracker answers per level and collapses at the whole-chain sampled bind. `MaxMsaaSampleCount` stops being a pinned 1 and becomes the INCUMBENT'S OWN computation reproduced with its citation pinned, because both design drafts invented a formula, the two differ, at most one could have equalled the incumbent's, and both then asserted equality as a test. Re-reading the source before writing also caught the design document naming the wrong driver call. **`PRESENT_SRC_KHR` becomes the swapchain image's canonical RESTING layout** ([#557](https://github.com/APKiwiOrg/KhaozEngine/issues/557)), so the frame's own list leaves it presentable at `End` inside the submit that already signals the semaphore the present waits on, rather than the boundary growing a command pool, a second `vkQueueSubmit` per frame and a third for the post-acquire discard on the one path with zero automated coverage. And MV4's budget FREEZES here, because the draw-call half of every marginal was the last thing owed: until something emitted a `vkCmdDraw` the count read zero by construction, and a marginal over a number that cannot move is not a gate. It IS nameable: `GpuBackendKind.VulkanNative` and the `KE_GRAPHICS_BACKEND=vulkan-native` / `vk-native` tokens landed in 17.32.0, and a machine that cannot run it still routes through the reported fallback rather than a crash. `VulkanNative` has OWNED the committed `vulkan-native` golden family since 17.41.0, seeded as a byte-identical copy of the incumbent's `vulkan` family it was a guest in until then ([#683](https://github.com/APKiwiOrg/KhaozEngine/issues/683)). **The blocking `vulkan-native` CI leg is what holds the rollout to its gates** ([#529](https://github.com/APKiwiOrg/KhaozEngine/issues/529)): the golden subset on every push and the serialized full suite weekly, on lavapipe with `KE_VULKAN_DEVICE` pinned, `KE_VULKAN_REQUIRED=1` so a row that needs a native device cannot go quietly dormant, and `KE_VULKAN_VALIDATION=strict` on the full suite with a second job running the golden and compute subset under `sync`, which is the first Khronos validation layer this repo has ever installed anywhere and the only instrument in the net that can see a missing barrier a software rasterizer orders correctly anyway. Targets `net10.0` with no OS suffix, no `[SupportedOSPlatformGuard]` and no `NoInlining` bodies, which is decision V-P1 rather than an omission: Vulkan is not a Windows API, so the CA1416 apparatus the Direct3D 11 package needs has no analogue, the assembly loads harmlessly on macOS where it is never selected, and a machine with no loader fails the functional probe and routes through the existing fallback. Binds Vulkan through `Silk.NET.Vulkan` plus the `.Extensions.KHR` and `.Extensions.EXT` packages, pinned to the same `2.23.0` Silk.NET line the windowing, input and audio stacks already pin, so the binding adds a package rather than a vendor and rides an upgrade the engine already does in lockstep. Carries no `Veldrid` edge of its own, asserted on both the project graph and the built IL, and keeps every Silk.NET type off its public surface. Spec, decisions and the nineteen-row work breakdown: [docs/design/VULKAN-NATIVE-BACKEND-DESIGN-2026-08-05.md](docs/design/VULKAN-NATIVE-BACKEND-DESIGN-2026-08-05.md). | Gpu, Diagnostics (+ Silk.NET.Vulkan) |
| **KhaozEngine.Gpu.Metal** | The engine's own native Metal backend for the `KhaozEngine.Gpu` seam, **the DEFAULT backend on macOS since 17.40.0** (carried by the `Game2D` and `Game3D` umbrellas since 18.0.0, along with its two siblings), and phase 4 of the staged native GPU backend program, which is the last one. **CREATES A DEVICE, HEADLESS OR WINDOWED, THAT RECORDS, SUBMITS AND PRESENTS, as of 17.35.0**: `KhaozEngineMetal.Register()` makes the provider reachable, `GpuBackendSelector.IsBackendSupported` answers for this machine through a real functional probe (a device with a name, the `supportsFamily:` floor, a buffer-offset alignment the uniform ring's stride is a multiple of, and `supportsTextureSampleCount:` for 1), and headless creation returns a device holding a real `MTLDevice` and one `MTLCommandQueue` with `KE_METAL_DEVICE` selection, `KE_METAL_VALIDATION` reporting, a command-buffer error latch and a drain before teardown. **It also CREATES RESOURCES as of 17.35.0** (row 6): buffers, textures, samplers and fences through `IGpuDevice.Factory`, the shared WRAP sampler pair, the device-level buffer and texture uploads on a setup command buffer that issues no queue submit per call, and `Map` with a read drain the incumbent did not have. One documented backend-divergent creation failure: a buffer declaring both `UniformBuffer` and a structured usage throws at creation, because a uniform buffer here is rebased per frame by the ring. That device owns one `MTLSharedEvent` as its completion timeline with a real `IGpuFence` and a counted `WaitForIdle`, and a command list that takes a fresh `MTLCommandBuffer` per `Begin`, keeps exactly one encoder open at a time, invalidates every piece of encoder-scoped state at a boundary, and submits under one lock with the timeline signal encoded and the completion handler attached before the commit. `Submit` flushes the pending setup batch first, outside that lock, so an upload a recording depends on is enqueued ahead of the recording's own buffer. `KE_METAL_FRAMES_IN_FLIGHT` sets the depth, and it sizes the uniform ring alone because a Metal command buffer is single-use and there is no pool to size. **And it COMPILES SHADERS as of 17.35.0** (row 9): `CreateShadersFromSpirv` and `CreateComputeShaderFromSpirv` take the same GLSL 450 every other backend takes, emit MSL under a pinned option set, and compile one `MTLLibrary` and one `MTLFunction` per stage under pinned `MTLCompileOptions` (language version 3.2, fast math on), with the entry-point name READ out of the emission rather than assumed. Metal has no binding decorations, so the per-program binding table is READ off the emitted text rather than counted on the CPU: each argument name spells a SPIR-V id, resolved to its `(set, binding)` through that stage's own decorations, which is the defect class three recorded production incidents in this engine came from. The parse never falls back to a count and throws by name on any of five failure classes, device-free, before any device runs. Every shipped stage emits MSL byte-identical to the incumbent's, asserted on every leg until 18.0.0, which is what licensed the committed `metal` goldens with no rebake. **And it CACHES THE EMISSION as of 17.36.0** ([#592](https://github.com/APKiwiOrg/KhaozEngine/issues/592)): not a `.metallib`, which no public API can serialize for a source-compiled library and which would buy nothing anyway since macOS already caches the MSL-to-library compile across processes at 0.02 ms warm, but the engine's own GLSL-to-SPIR-V-to-MSL half, which nothing else caches and which cost 3,443 ms across the 42 shipped programs. One file per program under `<local-app-data>/KhaozEngine/metal-msl/<engine version>/`, keyed on `MetalShaderKey`, carrying every stage's MSL, every stage's entry-point name, the binding table read off that emission (2.2b pin 6: a hit skips the emission the table is read from, so the payload has to carry it) and a compute kernel's workgroup size, so a warm start skips glslang as well as SPIRV-Cross and loads all 42 in 13 ms from 333 KiB. `KE_METAL_MSL_CACHE` relocates or disables it with the same five disable words the other two backends' caches take. The payload is authenticated with a SHA-256 of its own body and restates its own key, which neither sibling needs and this one does: a mangled DXBC fails in `CreateVertexShader` and a mangled `VkPipelineCache` blob fails the driver's header, where a mangled binding table has no reader below it at all, so any entry that does not authenticate, does not restate its key, is not this format or fails the table's structural checks is a miss AND a delete. **And it BUILDS PIPELINES as of 17.35.0** (row 11): `CreateGraphicsPipeline` and `CreateComputePipeline` produce real Metal state objects and a command list can bind one, with the declared resource layouts checked against the shader's reflection at creation, vertex streams pinned at the TOP of the `[[buffer(n)]]` space (30 downward, so they cannot collide with the resource buffers the emission numbers from 0 and `ResourceBindingModel` stops being a concept the engine has), a depth-stencil state created only for a pipeline that declares a depth output, and an identity guard the incumbent lacked, so binding the pipeline already bound costs one reference comparison instead of a state re-emit plus a full re-activation of every resource set. **And it BINDS RENDER TARGETS as of 17.35.0** (row 12): `CreateFramebuffer` plus the deferred render pass. A pass is a descriptor built at the FIRST DRAW, so a clear recorded after `SetFramebuffer` folds into that attachment's `loadAction` and costs no command, and a framebuffer creates nothing native at all: there is no render pass object to cache and none to rebuild on a resize. A clear lands on the attachment the CALLER named, where the incumbent wrote every clear into `colorAttachments[0]` and therefore never cleared `ModelFB`'s normal and linear-depth attachments at all. That is the one deliberate rendering change this phase makes, and the `KE_METAL_CLEAR=attachment0` switch that reproduced the incumbent for the A/B was removed at rollout gate 1, where the golden suite passed in both positions and a texel-readback `[GpuFact]` turned out to be the only instrument that can tell them apart. Store actions are set explicitly rather than left at the descriptor's discarding default, a framebuffer plus a clear plus no draw still clears, and the viewport and the scissor are emitted on a framebuffer CHANGE only, with the scissor gated on the bound pipeline's `ScissorTestEnabled` the way the other two backends gate it. **And it BINDS RESOURCE SETS as of 17.35.0** (row 13): all four `Set*ResourceSet` overloads record into a per-slot array and the next draw flushes every dirty slot, emitting ONE ARRAY CALL per kind per stage through the binding table rather than one call per resource per stage, which is the fan-out defect this program already paid to fix once on another API and which the vendored Metal fork's binding layer could not even express, since it declared no array setter at all. Across the 34 shipped graphics programs the worst full activation is 6 array calls and no program produces a non-contiguous run. A slot whose only change is its per-draw dynamic offset takes `setBufferOffset:` instead, once per stage that actually reads the buffer, which is the shadow pass's shape thousands of times a frame, and what selects that arm is a comparison against what the flush last EMITTED rather than a third state on the record. Every bind record carries an encoder-epoch stamp, so a record-time upload that opens a blit encoder mid-pass re-activates the binds on the next draw, and the vertex-stream cache arrives WITH that invalidation rather than without it, which is the corruption the incumbent avoided only through a second defect that kept its own cache permanently cold. A pipeline switch invalidates recorded slots only where the incoming program's binding table differs, which the row-10 deduplication is what makes possible. A buffer bind's offset is `frameBase + rangeOffset + callerDynamicOffset` against the segment THAT RECORDING captured, and a window that would leave its own segment is refused by name, which is a refusal only this path can make. **And it REPORTS ON ITSELF as of 17.35.0** (row 16): the whole `GpuCapabilities` set at ZERO permitted differences from the Veldrid Metal backend, asserted by creating both devices in one process and comparing member for member with a reflection check that the comparison covers the struct, measured while that backend still shipped, so `AntiAliasing.ResolveFor` clamped to the same sample count on both Metal paths. Seven of the nine members are constants of what the incumbent answered, `MaxMsaaSampleCount` reproduces its walk over `-supportsTextureSampleCount:` from 32 downward including the fact that Metal's only sample-count query takes no pixel format, and the device name is its own `-name` untrimmed. `GpuDeviceCounters` is filled in every channel a device has, so a capture off this backend carries GPU columns where the deleted Veldrid Metal path carried none, with `BackpressureStallCount` counting the uniform ring's segment acquire alone and the present-boundary trio reading zero on a headless device. `GpuDeviceDiagnostics.SoftwareAdapter` is `false` with confidence rather than null, because Apple ships no software Metal rasterizer. And `GpuFrameCapture.ArmNext` is serviced with this backend's OWN queue pointer rather than by reflecting into the deleted incumbent's private field, which is the last of the three sites the `GpuBackendKind` append degraded silently. **And it DRAWS as of 17.35.0** (row 14): both `Draw` overloads, `DrawIndexed`, `Dispatch`, the vertex and index binds and the whole transfer family record, so `IGpuCommandList` has no refusing member left. **And it PRESENTS as of 17.35.0** (row 15): `GpuDeviceContext.CreateForWindow` resolves the Cocoa `NSWindow` into a `CAMetalLayer` (adopting the host view's when it already has one, creating and attaching one when it does not), and `SwapchainFramebuffer`, `SyncToVerticalBlank`, `ResizeSwapchain` and `Present` are all live, so `IGpuDevice` has no unbuilt member either and the swapchain framebuffer is the same object for the life of the device while an acquire and a resize both move what it points at. The layer configuration reproduces the incumbent's field for field and four behaviours deliberately do not. A frame whose `-nextDrawable` came back nil renders into a device-owned ORPHAN TARGET at the current size, records, submits and completes exactly like any other frame with only its PRESENT skipped, and counts into `FramesBegun` with the first skip WARNed once, where the incumbent reported its framebuffer unrenderable and threw every draw in that frame away with nothing logged and nothing counted. `displaySyncEnabled` is written UNCONDITIONALLY, where the incumbent wrote it only inside three values of an enum deprecated since macOS 10.15 and therefore lost its vsync toggle silently on any machine outside that set. A resize and a vsync change QUEUE and apply at the next present boundary after a drain, where the incumbent applied a resize inline on the calling thread with no drain anywhere. And the drawable acquire is counted and timed into `AcquireWaitCount` and `AcquireWaitMs`, because `-nextDrawable` blocks and Metal has no zero-timeout probe to ask it not to. The present rides its own command buffer, which is the PLUS ONE the uncommitted-buffer bound has carried since row 7. And the initial `drawableSize` is the content view's frame in POINTS multiplied by `-[NSWindow backingScaleFactor]`, because a drawable size is in PIXELS ([#605](https://github.com/APKiwiOrg/KhaozEngine/issues/605)): the incumbent's NSView arm wrote the points straight through and opened a Retina window at half its real resolution until the first framebuffer-resize callback corrected it, while its own UIView arm multiplied, so the two arms of one constructor disagreed and this backend follows the one that is right. Only the first frame moves, since a resize already carries pixels, and the multiply lives in `MetalSwapchainPolicy` where it is asserted device-free on every leg. Every member of the RESOURCE FACTORY is live. `GpuBackendKind.MetalNative = 6` and its `metal-native` token exist (row 3), so naming it with no provider registered throws rather than falling back. `IsPlatformSupported` and `Register` are the whole public surface. Targets `net10.0` with no OS suffix, and unlike the Vulkan package it DOES carry the `[SupportedOSPlatformGuard("macos")]` over `NoInlining` apparatus the Direct3D 11 package carries, which is decision M-P1 rather than a copy made by analogy: Vulkan is not an OS-specific API and Metal is, so the assembly still compiles and runs its device-free tests on the Linux and Windows legs while the Objective-C interop stays off the load path there, with CA1416 enforcing the boundary at compile time. **The only backend in the program with NO third-party package at all**, which is decision M-P3: no maintained managed Metal binding exists to take (Silk.NET ships none, Vortice ships none, Apple ships none) and vendoring `Veldrid.MetalBindings` is rejected by name, so the interop is an engine-owned `[LibraryImport]` layer over `objc_msgSend` with blittable-only signatures. Its `ArchitectureTests.ThirdPartyHomes` row is empty and the emptiness is the assertion. Carries no `Veldrid` edge of its own either, asserted on both the project graph and the built IL. Spec, decisions and the nineteen-row work breakdown: [docs/design/METAL-NATIVE-BACKEND-DESIGN-2026-08-09.md](docs/design/METAL-NATIVE-BACKEND-DESIGN-2026-08-09.md). | Gpu, Diagnostics |
| **KhaozEngine.Windowing** | `AppWindow` (owns the Silk.NET/GLFW window + the per-frame `Run` loop), the immutable `InputState` snapshot, `InputManager`/`Pointer` (unified pointer, edges, `IsTapIn` press-origin invariant, region blocking, drag/scroll, keyboard/gamepad/menu-nav, plus the right-button twins `IsRightTapIn`/`IsRightPressingIn` over their own `RightPressOrigin` and `ConsumeRightGesture` latch, which is what a right-click context menu hangs off), `GameClock`, `DesignViewport`/`AdaptiveViewport`, `UiViewport` (point-space DPI-aware UI viewport: 1 logical point = the DPI scale in device pixels, reflows instead of magnifying; `Frame.DpiScale`/`LogicalWidth`/`LogicalHeight` expose the scale). Wires a GLFW text clipboard into `Platform.Clipboard`. `AppWindow.SetIcon(WindowIcon...)` sets the runtime window/taskbar icon from decoded RGBA8 (Windows/Linux; no-op on macOS); `AppWindow.SetMacDockIcon(pngBytes)` sets the macOS Dock icon (via `Platform.ApplicationIcon`). Action maps (`KhaozEngine.Windowing.Actions`: `ActionMap`/`InputAction`/`RebindOperation`/`ActionMapController`) declare named, rebindable actions evaluated purely off the `InputState` snapshot, with versioned JSON persistence for bindings. Gamepad rumble via `IRumble`/`AppWindow.Rumble` (sustained levels + decaying pulses), a graceful no-op on the current GLFW backend. | Gpu, Platform, Primitives |
| **KhaozEngine.Render2D** | `SpriteBatch` (textured quads + `DrawString` over stb_truetype `SpriteFont`, optional model transform + sampler mode; `DeviceScale`/`SnapRect`/`SnapLength` device-pixel snapping in a point-space pass), `DpiFont` (HiDPI-crisp: bakes the atlas at the live DPI scale, re-bakes only on a DPI change; `SpriteFont` also bakes at a fractional density), `Camera2D`, `Texture2D`, `ImageRgba` (CPU pixel/opaque-mask decode), `Render2DSurface`, scissor/point-sampling, offscreen capture. | Windowing, Gpu, Primitives, Imaging |
| **KhaozEngine.Imaging** | Dependency-free image IO: a minimal built-in PNG encoder (`PngWriter`) for 8-bit RGBA buffers (BCL `ZLibStream` + a CRC-32 table only), so no third-party image library enters the graph, plus `GoldenGrid` - the tolerance-based golden-grid regression core (downsample, per-channel `Compare`, byte-identical `Serialize`/`Deserialize`, `DiffHeatMap`) that the golden-image tests and the snapshot-diff tool share so games can golden-test their own scenes. The canonical home of the encoder the snapshot harness and Render2D's clipboard-copy path use. Encode-only (no decode/palette/interlace). | Pure .NET |
| **KhaozEngine.Render3D** | Stylized 3D: `Scene3D` (multi-instance mesh draw, glTF + procedural meshes (`GltfLoader` reads per-vertex `COLOR_0` and multiplies it into the material base colour, rigid and skinned alike, so a palette-painted kit needs no texture), per-mesh albedo textures, materials/lighting, billboards, runtime GPU bone-palette skinning for code-driven deformation, a splat-material pipeline for PBR terrain (`LoadSplatMaterial`/`SplatMaterialHandle`), a tile-ground pipeline for an OSRS-style tile world (`LoadTileGroundMaterial`/`TileGroundMaterialHandle`/`LoadMesh(mesh, material)`: up to `TileGroundMaterialConfig.MaxMaterials` (64) tileable albedo layers in ONE texture array, four material slots per triangle blended by the vertex weights, with a per-vertex brightness jitter, and it casts shadows like a model mesh), debug draw), `IsoCamera3D` (ortho iso + screen-to-ground/ray picking), `FlyCamera3D`/`FlyCameraController` (free-fly editor camera: WASD flight + right-drag mouselook + sprint + wheel speed, drops into `Scene3D.CameraOverride` like `FollowCamera3D`), world-space overlays (`WorldLabel` text + the `NameplateRenderer` name/bars plate), `PixelPostProcess`, `Render3DSurface`. Shadows (`ShadowMode` Off/Blob/ShadowMap via `RenderQuality.Shadows`, `Scene3D.AddShadowBlob`. The ShadowMap tier is cascaded with an outer coverage fade), always-on frustum culling (`DrawnInstances`/`CulledInstances` counters), always-on per-frame draw counters (`Scene3D.LastFrameStats`, a `RenderFrameStats`), per-pass CPU timing (`Scene3D.EnableTiming`/`PassTimingsMs`, off by default), a gradient sky (`PixelPostProcessSettings.Sky`, key-light-aligned sun) with an elevation-keyed day/night mapping (`SunCycle.Evaluate`/`Apply`, `SunCycleSettings`/`SunCyclePalette`, engine owns no clock), an HDR float16 pipeline with an ACES filmic tonemap (`PixelPostProcessSettings.Hdr`, on by default), an opt-in threshold + separable-blur bloom (`PixelPostProcessSettings.Bloom`, default off), an opt-in stylized ocean surface (`Scene3D.DrawWater`/`PixelPostProcessSettings.Water`: a wind-driven Gerstner swell displacing the surface grid, a domain-warped multi-octave ripple slope spectrum with footprint band-limiting and Toksvig variance transfer, per-channel Beer-Lambert depth grading, a fresnel blend toward the analytically reflected sky, a GGX sun glint with footprint-aware roughness widening, procedural whitecap + shoreline foam, depth-sampled shore fade, default off/no-op), and a screen-space distortion pass (`Scene3D.DrawDistortion`/`DistortionSprite`: heat-haze/shockwave/lens sprites warp the scene as the first post pass, `DistortionQuality` tier knob, zero-cost when unused). Immediate-mode glowing beams (`Scene3D.DrawBeam`/`BeamStyle`) and motion-trail ribbons (`Scene3D.DrawTrail`/`TrailSample`/`TrailStyle`, built by the pure `TrailGeometry`: camera-facing or twist-following tapered strips with a fading tail, additive or alpha, for weapon swings/thrusters/tracers). Layered animation (`LayeredAnimator`/`BoneMask`: bone-masked override and additive animation layers, one-shot action layers over a base). | Ecs, Windowing, Gpu, Render2D, Collision, Physics, Primitives |
| **KhaozEngine.Gui** | `GuiSurface` (immediate-mode UI: Panel/Label/Button/Slider/Toggle, hover, click-through gate), retained widgets (`Button`/`Slider`/`Toggle`/`Dropdown`/`TextInput`/`TabBar` segmented control, `ContextMenu` right-click option menu (title band over selectable rows with right-aligned details, entries resolved from `LocalizedText` at open, pure `ComputeBounds` layout whose viewport clamp wins over the flip, click-through reservation, and an opening-gesture latch so the press that opened it cannot act on it), `SlotGrid` hotbar/inventory grid (with opt-in drag-and-drop seams), `ProgressBar` thin fill bar with `FillDirection` orientation + segmented pips), editor-inspector widgets (`NumberField` scrub-drag/tap-to-type numeric field, `TreeView` hierarchy outline, `PropertyGrid` label/editor rows over get/set delegates), `ScreenStack` (top-to-bottom routed screen stack + widgets, exposing a shared `InputManager` so screens drive keyboard/gamepad nav), `IScreenComponent` + `ScreenComponentList` (the composition unit BELOW `Screen`, mirroring `Ecs.ISystem`'s role below `World`: one HUD element / overlay / presenter with `Update(dt, receivesInput, bounds, input)` returning whether it CONSUMED input and `Draw(batch, bounds)`, optional `LoadContent`/`UnloadContent` as default interface members, and a list a host fans out to once per lifecycle moment instead of hand-wiring N collaborators into M moments. Registration order is z order, input routes top-down and drawing bottom-up exactly as `ScreenStack` does between screens. `bounds` is per-call so a resize needs no hook. An interface, so a consumer keeps its own base class), `GuiDragContext` (drag-and-drop that SPANS widgets: a `DragPayload` carrying an OPAQUE game token the Gui layer never inspects, a source seam a widget opts into, a drop target that accepts or REFUSES before the release so nothing is accepted-then-undone, first-offer-wins z ordering, a ghost drawn at the pointer from a painter the source supplies, and a fly-home return animation on a cancelled drop. `SlotGrid` is the built-in consumer, and `TreeView`'s same-widget row reorder is a different gesture and stays as it is), `FocusNavigator` (menu navigation. `Toggle`/`Slider`/`Dropdown` also take an opt-in `Update(InputManager, focused)` for keyboard/gamepad control), `DiagnosticsOverlay` (F1-toggled corner telemetry HUD: game-built `OverlaySection`/`OverlayRow`s + `PerformanceSection`/`DrawStatsSection`/`PassTimingsSection`/`NetworkSection` populators, headless-testable `Update`) + `DiagnosticsHud` (turn-key bundle of the meters + overlay + throttled provider, wired automatically by `GameApp`), `GuiTheme` (the crisp neutral-dark default palette all widgets read, settable via `GuiTheme.Default` for a one-line global reskin, `GuiTheme.Legacy` to keep the pre-10.11.0 look) + `GuiStyle` semantic presets (`Primary`/`Secondary`/`Danger`/`Active`, `Modern`, `Legacy`) with an optional `GuiSkin` nine-slice texture skin applied family-wide, `PatchNotes.*` (in-game player-facing changelog renderer: `PatchNotesParser`/`PatchNotesDocument` parse a `PLAY_CHANGELOG.md`, `PatchNotesLoader` reads it disk-first then embedded, `PatchNotesView`/`PatchNotesScreen` render it collapsible + scrollable), `ToastStack`/`ToastView`/`ToastTheme` (retained toast-notification stack: transient + sticky corner-anchored popups, tap-dismiss, keyed in-place replacement so a repeated status line stays pinned, per-`ToastKind` themed palette), `Connection.*` (reusable connection-outage primitive: `ConnectionStatusController` is a netcode-free headless policy brain turning per-frame `ConnectionStatusSignals` into a `ConnectionStatusView` of `None`/`Banner`/`Screen`, escalating a persistent unplanned drop to a takeover and a planned update immediately, with an anti-flicker floor. `ReconnectScreen`/`ReconnectScreenTheme`/`ReconnectAction` are the matching asset-free modal screen: scrim over the frozen world, m:ss countdown from an absolute ETA, dot-ring spinner, optional action buttons, fully themeable including an opaque maintenance-page look. No banner ships, `Banner` mode means the consumer draws its own). Player-facing text sinks (Label/Button widgets, `GuiSurface.Label`/`Button`/`StatChip`, `Tooltip.Show`, `ContextMenu.Open` and `ContextMenuEntry.Of`) take a `LocalizedText` (from `KhaozEngine.App`) instead of a raw `string`. The old `string` overloads remain `[Obsolete]`. `IconButton`'s string is an icon-atlas key, not localized. Retained-widget text scale: `Button.LabelScale`, `TabBar.TextScale`, per-line `TooltipLine.Scale`, and `Tooltip.TitleScale` (each defaulting to `1f`, label-only, rect and hit-test unchanged). | App, Windowing, Render2D, Updates, Diagnostics |
| **KhaozEngine.Audio** | `AudioSystem`: an OpenAL (Silk.NET.OpenAL) streaming music backend + SFX one-shots and 3D positional audio over a voice pool. WAV/OGG/MP3. Random track rotation is scopeable via `SetRotationPool` (e.g. menu-only music while every track stays playable on demand); track changes can crossfade (`MusicCrossfadeDuration` / `CrossfadeTo`, default hard cut). SFX group into named buses (`DefineBus` / `SetBusVolume`) for per-group volume. | Diagnostics, Primitives |
| **KhaozEngine.Particles** | Pure, deterministic game-feel simulation (xorshift, `System.Numerics` + BCL only): `ParticleSystem` pool, `EmitterConfig` presets, `RateAccumulator`, and `ScreenShake` (trauma-based deterministic shake; absorbed from the retired Effects package in 9.0.0). | Primitives |
| **KhaozEngine.Particles.Render3D** | The 3D render arm of particles: turn-key glue mapping a `ParticleSystem` or `ParticleEffectPlayer` onto Render3D's modern particle pass. `ParticleLook` (shape/blend/stretch/orientation/soft-fade scale per emitter, plus trail forwarding to `DrawTrail` and budgeted light links via `AddLight`, optional authored-atlas flipbook playback with motion-vector frame blending, life-swept one-shot or fps-driven looping, additive over the procedural shapes, and an optional `DistortionLook` that warps the scene per particle instead of drawing sprites), the `Scene3D.DrawParticles`/`DrawEffect` extensions, and `VfxPresets` (nine authored effects: `FireBurst`, `FrostShatter`, `HealMotes`, `EmberDrift`, `SparkShower`, `Shockwave`, `SmokePlume`, `ArcaneSparkle`, `HeatHaze`). In the `Game3D` umbrella. | Particles, Render3D |
| **KhaozEngine.Telegraphs** | Attack-telegraph / danger-zone indicators (presentation-only): `TelegraphStyle` (seven presets: `Generic`/`Fire`/`Poison`/`Steel`/`Frost`/`Nature`/`Arcane`, with feathered edges, animated noise fill patterns, and edge-glow/sparkle knobs), the pure `TelegraphResolve` progress->visual mapping, and the immediate-mode `TelegraphRenderer2D` (circle/ring/beam/cone/arc, rendering the flat legacy look only since the modern knobs are a 3D decal feature). In the `Game2D` umbrella. | Render2D, Primitives |
| **KhaozEngine.Telegraphs.Render3D** | The ground-plane arm of telegraphs: `Scene3D.GroundCircle/Ring/Beam/Cone/Arc` extensions that paint danger zones on the ground/terrain via the engine's depth-sampling `DrawGroundDecal` primitive, rendering the full modern style set (feather, warped/vortex noise fill, hollow-rim interior dim, base-fill extent tint for borderless zones, rim/sweep glow, edge sparkle, outline dash runner) plus one-shot fading impact residue marks (`BuildResidueCircle`). Honors `FillMode` (fill-only or outline-only) same as the 2D path. In the `Game3D` umbrella. | Telegraphs, Render3D |
| **KhaozEngine.Terrain.Render3D** | The render arm of terrain: `TerrainChunkBuilder` meshes finite chunks off a `TerrainField` into Render3D `GltfMesh`es with distance LOD, tier-scaled crack-hiding skirts, per-vertex splat weights, and a chunk AABB, plus `Scene3D.LoadTerrainChunk`/`DrawTerrainChunk` and `Scene3DChunkSink` (the production `IAsyncChunkSink`: an endless ring of chunks + props with a hysteresis band, distance-LOD re-meshing, and background-thread mesh build with the per-frame budget capping GPU applies not builds, disposable so a streaming rebuild against a surviving `Scene3D` frees the loaded ring instead of leaking it) plugged into `KhaozEngine.Terrain`'s headless `TerrainStreamer`/`IChunkSink`/`ChunkBuildScheduler` streaming core, and the PBR splat-material layer (`TerrainMaterialLayer`/`TerrainLayeredMaterial`, `TerrainMaterialPresets`, `TerrainScene3D.LoadTerrainMaterial` - supply a `TerrainLayeredMaterial` to render five tileable PBR layers blended by the baked splat weights, omit it for the height/slope vertex-colour ramp fallback). An optional consumer `splatRule` (`TerrainSplatContext` -> `TerrainSplatWeights`, on both the builder and the sink, null = byte-identical to the engine's own mix) lets a game influence the per-vertex material mix, which is what gives a world with a second body of water a shoreline at all. Far chunk clusters collapse to a merged coarse mesh via `PropHlod` (merge + vertex-cluster weld a cluster's props into one instance) and `PropLayer.WithHlod` (distance swap, crossfaded against the props by the 14.5.0 dissolve, flat vertex-colour so no atlas), so a continent-scale forest horizon renders at one draw per cluster. In the `Game3D` umbrella. | Terrain, Render3D |
| **KhaozEngine.TileWorld.Render3D** | The render arm of the tile world: `TileGroundMesher.Build` meshes one region-plane into a REGION-LOCAL `GltfMesh` for the `TileGround` pipeline, drawn at `TileGroundMesher.WorldMatrix`, carrying per tile the four CORNER materials as slots with per-vertex weights over them and a per-vertex brightness jitter (`TileColors`), overlay shapes cut by the shared `TileTriangulation.Triangulate` so a click lands on the triangle that was drawn, smooth normals by central differences over the GLOBAL height lattice so region borders have neither a crack nor a lighting step, and the reserved `MissingSlot` (magenta, `MissingMaterialColor`) for a material id the catalogs do not define. `TileGroundMaterials.Build` turns the catalog into the `TileGroundMaterialSet` those slots index: one albedo layer per material in id order plus the reserved one last, textures decoded through `ImageRgba` relative to the catalog FILE that declared them and a flat colour layer for every material that carries none, so a colour-only world renders through the same textured pipeline. `TileObjectProps.Build` turns a region-plane's objects into `TileRegionProps(Ground, Roofs)` `PropPlacement` lists for the `Terrain.Render3D` prop path (rotated-footprint anchor, NEGATIVE yaw per quarter turn so a rotation turns clockwise from above with north up), resolved through `ITileMeshResolver` with the procedural `GreyboxMeshResolver` as the shipped stand-in and a placeholder box plus one log line for an archetype with no mesh. `GltfMeshResolver` is the content-backed one: it maps an archetype's `MeshRef` (relative, forward slashes) to a glb under a kit root, loads it once through `GltfLoader.LoadPartsWithMaterials`, caches it per archetype id, and on a missing file or a loader throw logs ONE line naming the archetype, path and reason before answering with its fallback resolver, so a half-authored kit renders as greybox boxes where the glb is missing and as the real mesh everywhere else. `TileWorldView` owns the ground material set, the per-region meshes, props and prop mesh sets in a scene behind the `ITileWorldScene` seam (`Scene3DTileWorldScene` ships, tests drive a fake, so all view logic is headless): `LoadRegion`/`UnloadRegion`/`MarkDirty` (a tile rect grows by a 2-tile margin, because the mesher reads across region borders), a coalescing `Flush` bounded by `MaxRebuildsPerFlush` with `PendingRebuilds` and a `Flush(int)` settle-now override, one ground material upload per view (`TileWorldViewOptions.GroundMaterials`, null builds the flat one from the catalogs) that every region-plane mesh is uploaded with, and OSRS's roof-hide rule off `Observer`/`ObserverIndoors`. Water is the engine's own pass over ground the author carved: `TileWaterPlanes.Collect` groups a region-plane's `Kind = Water` underlay tiles into 4-connected bodies, puts each at the MAXIMUM corner height over its tiles (the rim it shares with its bank) minus 2 cm, and cuts the body into DISJOINT maximal rectangles rather than one bounding box, because the pass discards only where the ground is at or above the surface and a box would flood the ditch beside a bend. `TileWaterLooks.River` is the shipped per-plane `WaterLook` (procedural, flat, silty, a short depth blend so a 60 to 80 cm bed still darkens), `TileWorldViewOptions.WaterLook` overrides it, and `TileWorldView.Draw` queues every loaded region-plane's surfaces through the `ITileWorldScene.DrawWater` seam member unless `TileWorldViewOptions.DrawWater` is false. `TileRegionResidency` + `TileResidencyConfig` stream a Chebyshev ring of regions over a `TileWorldSource` with a hysteresis band and a per-update load cap, marking the eight neighbours dirty on every stream in and out, never dropping a dirty region, and `PrimeAround` fills the ring uncapped and settles for a teleport. `TileWorldSnapshot.CaptureTopDown`/`CapturePerspective` are the headless RGBA8 captures over `Render3DSnapshot` (top-down is exactly `pxPerTile` pixels a tile with north up and east right), shared by the four `[GpuFact]` goldens (`tileworld_greybox`, `tileworld_topdown`, `tileworld_textured`, `tileworld_river`) and the round-3 editor tooling. In the `Game3D` umbrella. | TileWorld, Render3D, Render2D, Terrain.Render3D |
| **KhaozEngine.Game** | The 2D game-loop facade: `GameApp` (abstract base owning the per-frame compose: clock/viewport/input/draw, plus a point-space `Ui`/`UiPointer` and an `OnDrawUi` pass for DPI-aware crisp UI, a turn-key F1 diagnostics HUD wired by default, and a lazily-built turn-key `JobScheduler` for multi-core ECS scaling - wire it into a world with `world.DefaultScheduler = App.JobScheduler`) + `GameAppOptions` (`DisableDiagnosticsOverlay`/`DiagnosticsToggleKey` govern the HUD, `DisableJobScheduler`/`JobSchedulerDegreeOfParallelism` govern the scheduler), and a `SceneManager`/`GameScene` state stack (Push/Pop/Replace/SwitchTo, `DrawUi`/`OnDrawUi`, overlay DrawBelow/UpdateBelow). Also a turn-key boot/startup screen (`Boot/`): `BootScreen` (the instant-on first scene), `BootPipeline` + `IBootStep` (weighted staged steps on a main-thread pump), the built-in `UpdateBootStep`/`ServerStatusBootStep` wrappers, `BootScreenTheme`, and `BootStrings`. | Windowing, Render2D, Gui, Updates, ServerStatus, Simulation |
| **KhaozEngine.Game.Render3D** | The 3D bridge for the Game framework: `GameApp3D` (a `GameApp` that stands up a `Render3DSurface` + drives the 3D pass), `IGameScene3D`, and a `SceneManager.Draw3D` extension, plus the animated-character layer (`AnimatedCharacter` + `ReplicatedCharacterAnimators`, the position-driven per-player animator bridge, and `AnimatedCharacter.PlayAction`/`CancelAction` for a one-shot or held masked action (an attack, a cast, or a held pose like a drawn-weapon arm idle), layered over the locomotion base). Also the canonical `CharacterFacing` helper (intended-move-direction facing, so a scraped wall never spins the model), and the Obsolete `CharacterAvatar` (composed `CharacterController3D` + `AnimatedCharacter` + facing + draw into one `Update`/`Draw` object with a `TryLoadGltf` rig loader, kept for existing pins - superseded by `ReplicatedCharacterAnimators` fed the controller's `ClimbRate`/`StepDeltaY`). Kept separate so a 2D game pulls no 3D renderer. | Game, Render3D, Locomotion, Physics |
| **KhaozEngine.Snapshot** | Headless art/UI screenshot harness (2D): `SnapshotRunner` wraps capture -> PNG encode (Imaging) -> write -> log, and `SnapshotHost` gives a one-call CLI top-level, so a game's screenshot tool is just its scenes. Deterministic, no window, 2D-only (no Render3D dependency). | Render2D, Imaging |
| **KhaozEngine.Snapshot.Render3D** | The 3D arm of the snapshot harness: a `Shot3D` extension on `SnapshotRunner` wrapping `Render3DSnapshot.Capture` with the same encode -> write -> log path. Separate so a 2D-only game never drags in Render3D. | Snapshot, Render3D |
| **KhaozEngine.Ecs** | A struct-based archetype `World`/`Entity`/`ISystem` ECS: by-ref component access, `ForEach` (a structural change from inside the loop is refused rather than silently corrupting the pass), opt-in data-parallel `ParallelForEach` (fans archetype rows across the `IJobScheduler` worker pool, with an `AccessSet` read/write-declaration model + a debug hazard guard + per-worker command buffers). `World.DefaultScheduler` is the per-world fallback a no-scheduler `ParallelForEach` call routes through (defaults to the deterministic single-threaded scheduler, and an explicit per-call scheduler always wins). Also command buffer, system groups, `CachedQuery`, `WorldSerializer`. | Serialization, Simulation, Primitives |
| **KhaozEngine.Content** | Config/content loading (embedded or disk JSON) with JSON-schema validation + build-time schema enforcement (via the bundled non-packable `Content.Validator` tool that ships inside this package). | Diagnostics, Serialization, Primitives (+ JsonSchema.Net) |
| **KhaozEngine.Diagnostics** | Logging service: levels, pluggable sinks (rotating file / console / debug / in-memory), category loggers, a static `Log` facade over an injectable `LogManager`, crash hooks. Plus the telemetry set: `FrameStats` (rolling fps / frame-ms / managed-bytes meter), `PassTimings` (rolling per-pass-name avg/min/max CPU-encode-ms meter, the same shape keyed by pass), `TelemetryRecorder` (+`TelemetryChannel`, crash-safe JSON-Lines session recorder), and `ClientNetStats` (the connection-health snapshot `WorldClient.NetStats` fills and the overlay renders). | Pure .NET |
| **KhaozEngine.App** | App/runtime helpers: `BuildMetadata` (read `AssemblyMetadata` at runtime), `AppDataPaths` (publisher-rooted OS-correct per-app data dir), `ServiceLocator`, `AppInstallStamp`, `LocalizationManager` (discover satellite-resource cultures + set the thread culture; absorbed from the retired Localization package in 9.0.0), and `IStringCatalog`/`ResourceStringCatalog` (resolve UI strings by key against the current UI culture over a `ResourceManager`; `LocalizationManager.Catalog` hands one out). Compile-time localization enforcement: `StringId` (a typed localization key with no implicit `string` conversion), `LocalizedText` (what Gui sinks accept - lazily resolves a `StringId` (+ optional format args) against the ambient `LocalizationContext.Catalog`, implicit from `StringId`, explicit `LocalizedText.Raw(...)` for non-localizable text), and the `[LocalizationExempt]` / `[LocalizationStringSink]` marker attributes the analyzer reads. `AppRelaunch` forces a clean self-restart (spawn a fresh instance carrying a predecessor-pid handshake, then cooperative shutdown, never `Environment.Exit`) for sign-out / cloud-restore / restart-to-apply flows (including applying a graphics-backend change), over `Platform.IProcessControl`, resolving both the self-contained apphost and the `dotnet <app>.dll` shapes. | Diagnostics, Platform |
| **KhaozEngine.Persistence** | `SaveEncoder` (Base64 + HMAC-SHA256 tamper-deterrent, a versioned envelope carrying tamper-protected `SaveMetadata`), `AtomicJsonWriter` + `PersistenceQueue` (crash-safe atomic writes with numbered backup-generation rotation), `SettingsManager<T>`, `MigrationChain<T>` (versioned schema migration), the `GameStorage` facade (encode-by-default saves, an outcome-reporting recovery ladder, generation restore), and `BatchedWriter<T>` (bounded async batch-write queue for a server-side append-only log: caller-driven flush cadence, drop-oldest overflow, and per-record salvage retry after a whole-batch failure). | Diagnostics, App, Serialization |
| **KhaozEngine.Serialization** | Shared `System.Text.Json` defaults (`JsonDefaults`: tolerant-read / indented-write / include-fields) so Content, Persistence, and Ecs serialize consistently. | Pure .NET |
| **KhaozEngine.Platform** | `Clipboard`: cross-platform system-clipboard facade. Text via a registered window/GLFW provider (wired by Windowing's `AppWindow`) with a macOS `NSPasteboard` fallback; images via Windows CF_DIB / macOS / optional mobile bridge. `ApplicationIcon.TrySetMacDockIcon(pngBytes)`: runtime macOS Dock icon via `NSApplication`. `WindowsWindowIcon.TrySyncTaskbarIconFromWindow(hwnd)`: point the window class icon at the live window icon so the Windows taskbar button shows it. `WindowsAppId.TrySetProcessAppUserModelId(id)`: explicit Windows taskbar identity (grouping/pinning). `WindowsConsole.EnsureParentConsoleAttached()`: attach a `WinExe` (Windows-subsystem) game head to the launching terminal's console so `Console.Write*` still reaches `dotnet run` / cmd / PowerShell, making `OutputType=WinExe` viable without losing developer console output. `IProcessControl`/`ProcessControl`: the fakeable process seam (resolve the running exe/pid/args/managed entry dll, spawn a detached instance, wait for a pid to exit) behind `KhaozEngine.App.AppRelaunch`'s self-restart. Best-effort and never-throwing. | Pure .NET |
| **KhaozEngine.Identity** | Pluggable player-identity seam (in `Foundation`): `IIdentityProvider` (client sign-in), `IIdentityValidator` (server-side verified-subject validation), `ITokenCache`/`FileTokenCache` (persisted sign-in state), `IBrowserLauncher`/`ILoopbackListener` (the OS/network seams behind an interactive sign-in), `ProviderCredential`/`VerifiedIdentity`/`CachedSession`/`IdentityState`, `SessionToken` (a stateless HMAC-SHA256 session token), `IdentitySession` (the client-side orchestrator: launch-state restore, sign-in, offline-grace, session-token attach, silent credential refresh with rotation-safe persistence), and `SignInException` (the shared base every provider backend's own sign-in failure derives from, so cross-provider code catches one type). Transport-agnostic (no HTTP). The exchange with a server, and the OIDC/Discord providers, are opt-in sibling packages. | Diagnostics, Serialization |
| **KhaozEngine.Identity.Oidc** | Generic OIDC provider + JWKS JWT validator backend for `KhaozEngine.Identity` (opt-in, NOT in any umbrella; add explicitly like `Physics.Bepu`): `OidcClientProvider` (`IIdentityProvider`, authorization-code + PKCE via the system browser + loopback redirect), `OidcTokenValidator` (`IIdentityValidator`, validates an id_token against the issuer's discovery document + JWKS), `SystemBrowserLauncher`/`HttpLoopbackListener` (the `IBrowserLauncher`/`ILoopbackListener` implementations), `OidcProviderOptions`. Works with any standards-compliant OIDC provider (Auth0, Okta, Azure AD, ...). | KhaozEngine.Identity, KhaozEngine.Platform, Microsoft.IdentityModel.Protocols.OpenIdConnect, Microsoft.IdentityModel.JsonWebTokens |
| **KhaozEngine.Identity.Discord** | Discord provider + userinfo validator backend for `KhaozEngine.Identity` (opt-in, NOT in any umbrella; add explicitly like `Physics.Bepu`): `DiscordClientProvider` (`IIdentityProvider`, Discord's OAuth2 authorization-code + PKCE flow against its fixed authorize/token endpoints, no discovery document) and `DiscordTokenValidator` (`IIdentityValidator`, verifies the access token via `GET /oauth2/@me` and rejects any token not minted for the consumer's own client id; takes a required `expectedClientId`), `DiscordProviderOptions`. Opaque-token OAuth2, not OIDC, so no `Microsoft.IdentityModel` dependency. | KhaozEngine.Identity, KhaozEngine.Platform |
| **KhaozEngine.Collision** | Deterministic 2D collision + broadphase: `CircleCollision`, `SpatialHashGrid`, static-world collision (`BoxCollision` circle-vs-AABB/oriented-box push-out, `WorldCollider`/`WorldColliders` with height-aware blocking), and walkable prop surfaces (`PropSurface`/`WorldSurface`/`WorldSurfaces` top-surface height grids you stand on / jump onto). Bit-identical math for lockstep sims (`System.Numerics`). | Pure .NET |
| **KhaozEngine.Physics** | Dependency-free 3D physics seam (in `Foundation`): `IPhysicsWorld` (static bodies, dynamic rigid bodies `AddDynamic`/`RemoveDynamic`/`GetDynamicPose`/velocity get+set/`IsAwake`, joint constraints `AddConstraint`/`RemoveConstraint`/`SetConstraintTarget`, `Step(dt)`, raycast/sweep-capsule/penetration queries), value-type shapes (`Sphere`/`Capsule`/`Box`/`Cylinder`/`ConvexHull`/`TriangleMesh`/`Compound`), `DynamicBodyDescription` (mass + initial velocity + sleep knobs), `ConstraintDescription` (discriminated `BallSocket`/`Hinge`/`Slider`/`Distance`/`Weld` joints, body-local anchors, dynamic-to-dynamic or dynamic-to-world-anchor via `ConstraintAttachment`, plus powered drives `WithHingeMotor`/`WithHingeServo`/`WithSliderMotor`/`WithSliderServo`/`WithWinch` retargeted allocation-free via `SetConstraintTarget`), `Pose`, `PhysicsMaterial`, `QueryFilter`, `StaticHandle`, `DynamicBodyHandle`, `ConstraintHandle`, `RayHit`/`SweepHit`. Also the render-free `PropCollisionFormat` KECL `.coll` reader/writer + headless loaders (`LoadDirectory`/`Load`), so an authoritative server with no GPU/windowing loads the same baked shapes a client predicts against. The backend is opt-in (see `Physics.Bepu`). | Pure .NET |
| **KhaozEngine.Physics.Bepu** | BepuPhysics v2 backend (opt-in, NOT in any umbrella; add explicitly like `WorldStore.Sqlite`): `BepuPhysicsWorld : IPhysicsWorld` over BepuPhysics 2.4.0 (pure-managed, Apache-2.0). Single-threaded deterministic `Simulation` (default 4 substeps, uniform gravity; default Earth gravity, `new BepuPhysicsWorld(Vector3.Zero)` for static-only; optional `substepCount`/`velocityIterationCount` ctor params pin the determinism fingerprint). `AddStatic`/`RemoveStatic`, `AddDynamic`/`RemoveDynamic` (dynamic rigid bodies with shape-derived inertia and an approximate deterministic game-feel restitution bounce), `AddConstraint`/`RemoveConstraint`/`SetConstraintTarget` (joints mapped to verified Bepu constraint types: `BallSocket`, `Hinge`+`TwistLimit`, `PointOnLineServo`+`AngularServo`+`LinearAxisLimit` slider, `DistanceLimit`, `Weld`; powered drives map to `AngularAxisMotor`/`TwistServo` (hinge), `LinearAxisMotor`/`LinearAxisServo` (slider), `DistanceServo` (winch), retargeted allocation-free via `Solver.ApplyDescription`; a world anchor is an infinite-mass shapeless kinematic body, not a collidable so queries pass through it; removing a body cleans its constraints), `Step(dt)`, raycast, sweep-capsule, penetration. Wire into `CharacterController3D` / `WorldServer` / `WorldClient` via the `IPhysicsWorld?` ctor param. | KhaozEngine.Physics, BepuPhysics |
| **KhaozEngine.Social** | Game-agnostic social/presence seam (in `Foundation`): `ISocialProvider` (rich presence, local identity, join/invite) with a `NullSocialProvider` no-op default and a `SocialPresenceController` that dedupes/throttles presence, retries a failed connect on a bounded backoff (so a game launched before Discord still gets presence), reconnects on that same backoff when a live connection drops (the player quit Discord mid-session) and republishes what was live, and self-disables only when a provider call throws. Provider-neutral (Discord today, Steam/other later). The backend is opt-in (see `Social.Discord`). | Diagnostics |
| **KhaozEngine.Social.Discord** | Discord Rich Presence backend (opt-in, NOT in any umbrella; add explicitly like `Physics.Bepu`): `DiscordSocialProvider : ISocialProvider` over a pure-managed Discord IPC client (Windows named pipe / unix domain socket, JSON framing) - no native libraries, no third-party NuGet. Rich presence, local Discord identity, `ACTIVITY_JOIN`/join-request. `DiscordSocialOptions` carries the game's Discord Application id. | KhaozEngine.Social |
| **KhaozEngine.Terrain** | Render-free analytic terrain: `TerrainField` (`SampleHeight`/`SampleNormal`/`SampleBiome`/`WaterLevel`) folds biome-band shaping, stateless coordinate-hash fractal noise (`TerrainNoise`), and ordered features (`LakeFeature`/`RidgeFeature`/`FlattenFeature`), height at a point depends only on `(x, z, seed)`. Plus `TerrainCollision` (ground height + slope walkability), `TerrainPresets.Clearing()`, `TerrainRaycast` (GPU-free march-plus-bisect ray/terrain intersection for editor and gameplay picking), `PropScatter`'s `IArea2D` region shapes (disc/box/polygon) feeding `ScatterConfig.Exclusions` (skip any hit) and `.Overrides` (first-match density/kind tweak) alongside the legacy single-disc `ClearingRadius`, and the headless world-streaming core (`TerrainStreamer`/`StreamerConfig` over an injected `IChunkSink`/`IAsyncChunkSink`, `ChunkCoord`/`ChunkGrid`/`ChunkRing`, `ChunkBuildScheduler`/`ChunkBuild`/`IChunkBuildDispatcher`, `TerrainLod`/`TerrainLodConfig`/`TerrainChunkRegion`, and `RenderDistanceProfile`/`RenderDistanceTier` (`Near`/`Medium`/`Far`/`Ultra`, exposing `ToStreamerConfig()`/`Validate()`/`Scaled()`) tying the decor far field, prop cull radius, camera far clip and camera-following ocean half-extent to one coherent tier, scalable as a set for a Base/2x/4x view setting) that a dedicated server runs exactly as a client does, no GPU or Render3D dependency required (`KhaozEngine.Terrain.Render3D` adds only the GPU sink and mesh builder). Plain `float`, server and client sample the same field. In the `Foundation` umbrella. | Primitives, Collision |
| **KhaozEngine.MapDoc** | The zone/map document format: a versioned, schema-validated JSON model (terrain config with parametric features, named scatter and companion layers, `MapExclusion`/`MapScatterOverrideDoc` region shapes, authored placements, NPC spawns, tagged regions, a reserved terrain-overrides slot) with `MapDocumentFile.Load`/`Save` (JSONC-tolerant, loud `MapDocumentException` failures, format-version migrations) and `MapRuntime` builders that produce the exact runtime objects games consume (`TerrainField`, `ScatterConfig`, `CompanionConfig`, `PropPlacement` lists, deterministic ground-snap, and a `MapRegionSet` point-to-region resolver the map editor's own overlay picking runs on), so client and server load one document through one path and agree by construction. An extensible `MapDocRegistry` lets a game register custom terrain feature types. GPU-free. In the `Foundation` umbrella. | Primitives, Serialization, Content, Terrain |
| **KhaozEngine.TileWorld** | The OSRS-style tile world (in `Foundation`): a discrete tile grid in 64x64 regions with N stacked planes, a global tile-corner height lattice (centimetre shorts, neighbour-read across regions, higher planes derived from plane 0 unless authored), per-tile ground `Underlay`, shaped `Overlay` and `TileSettings` flags, tile-anchored `TileObject`s and named `TileMarker`s, catalogs (`GroundMaterial`, `TileObjectArchetype`) referenced by id and never stored, with a material's optional `tilesPerMetre` (the texture repeats per metre the textured ground path gives it) and `TileWorldCatalogs.MaterialSource(id)` (the catalog FILE it was loaded from, so a relative `Texture` resolves against the file that declared it). Directory file form (`world.json` + `regions/r_x_z.json`, base64 dense layers, tmp+rename, per-region SHA-256 checked on load, `TileWorldSource` for on-demand regions), `TileWorldHash` world identity, `TileWorldValidator`, the DERIVED per-edge `TileCollisionMap` (walls mirrored onto the neighbour tile) with `TileCollision.CanStep`, `TilePathfinder` (deterministic BFS, nearest-reachable, NxN agents), `TileRaycast`, `TilePrefabs` and `TileTriangulation` (`SplitSwNe` picks the diagonal, `Triangulate` cuts the tile into `TileLatticeTriangle`s over the eight `TileLatticePoint` lattice points, both shared with the ground mesher so a click lands on the triangle that was drawn). Object and marker coordinates are WORLD tile coordinates, and rotation is quarter turns clockwise (0 west, 1 north, 2 east, 3 south). `TileWorldSpace` is the one seam between tile space and the engine's render space, where world z is MINUS tile z so north is -z and a top-down view has north up and east right at once. GPU-free, a sibling of `MapDoc`, not an extension of it. Design: `docs/design/TILE-WORLD-DESIGN-2026-08-15.md`. | Primitives, Serialization, Content |
| **KhaozEngine.TileWorld.Editing** | The editing kernel for a tile world (in `Foundation`): every mutation of a `TileWorldDocument` is a reversible command, so undo and redo behave identically whichever frontend issued the edit. `ITileCommand` (`Apply`/`Revert`/`TryMerge`/`DirtyRects`) over `TileCommandBase`, with the pre-image captured on the FIRST apply only (so a redo replays the edit rather than capturing what its own apply left) and `TileDirtyRect` as the unit both collision rebaking and renderer invalidation work in, a merge being obliged to union the absorbed command's rects. `TileEditHistory` is the undo/redo stack with gesture coalescing and a merge barrier after any undo, redo or `SealGesture`. `TileEditingDocument` owns the document, the history, the saved marker and the DERIVED `TileCollisionMap`, rebakes collision over each command's dirty rects on execute, undo and redo, rejects a rect on a plane the map does not have before anything applies, accumulates `PendingRebuilds`/`AcknowledgeRebuilds` for a renderer, tracks `IsDirty` by history position against `MarkSaved`, and raises `CommandApplied`/`CommandUndone`/`CommandRedone`. The commands are `SetTilesCommand`, `SetCornerHeightsCommand`, `PlaceObjectCommand`, `MoveObjectCommand`, `RotateObjectCommand`, `RemoveObjectCommand`, `SetObjectTagsCommand`, `SetMarkerCommand`, `RemoveMarkerCommand`, `CreateRegionCommand`, `DeleteRegionCommand`, `CompositeCommand` (many commands, one undo step, rolled back whole if a child throws) and `SnapshotRectCommand` (an arbitrary mutation made undoable by a pre-image of a rect). `TileEditOps` is the factory set that READS the document and returns the command: `Raise`/`Flatten`/`Smooth`/`SetHeights` brushes, `Line`, a hash-deterministic `Scatter`, `PlacePrefab` and `ImportHeights`, with `PgmReader`/`PgmImage` decoding binary PGM (P5, 8/16 bit) heightmaps. The single mutation path shared by the `ke-tileedit` MCP tool and, in a later round, the GUI tile editor. GPU-free and render-free, unlike `MapEditor`, whose command stack sits inside a package that carries Gui and Render3D. Design: `docs/design/TILE-WORLD-DESIGN-2026-08-15.md`. | TileWorld |
| **KhaozEngine.TileWorld.Netcode** | Server-authoritative, client-predicted movement over a `KhaozEngine.TileWorld` world (in `Server`): a player IS a `TileCoord`, a `TileDirection` facing and a `TileMoveMode`, and a step COMMITS its tile when it STARTS while the drawn body GLIDES into that tile over the step, so the simulation leads the picture and a click is answered against the tile the player is committed to. `TileMoveState` is both an `IPredictedState` and an ECS `IComponent`, and `TileMoveSimulator` is the one STEPPER the server and the client run over the same baked `TileCollisionMap` (the client's prediction reaches it through a thin `ITickSimulator` that binds the local net id, so both heads step one set of rules), so `ClientPrediction` reconciles the local player and a mismatch snaps exactly once. `TileWorldServer` sits on a `Sharding` `ShardHost` whose cell grid IS the tile region grid (`cellSize = TileRegion.Size`, so a cell is exactly a `RegionCoord` and there is no float boundary rounding), whose per-cell tick runs in a fixed eight-step order: drain one command per player, tick every spawner and write every live actor's command and tag (1b), step every cell, exactly-once region handoff with the border ghosting that follows it, resolve the action queue, roll and apply combat (4b), serve each client its plane-filtered area of interest, and last despawn the actors killed this tick (5b, behind the serve so the killing blow still reaches everyone watching the fight). Around that sit per-connection rate limiting, a ban predicate, drain-on-shutdown, and the `OnBeforeTick` / `OnGameMessage` / `OnInteract` game seams. `TileReach` is the OSRS reach rule (a tile cardinally adjacent to a footprint tile that `CanStep` can step OUT onto from that footprint tile, so the rule still holds when the target itself is solid), `TileActionQueue` holds one pending action per player and a second click replaces it. Server-owned ACTORS are a player minus a connection: `TileActorHost` carries the spawners built from a `TileActorDefinition`, the respawn timer and the per-tick command and tag write a region handoff would otherwise drop, and every live actor steps through the SAME `TileMoveSimulator` on its own `TileWorldServerConfig.ActorMove` knobs, so an actor can never move in a way a player could not. `ITileActorBehaviour` is the one decision seam (an intent names a tile or a target and never a route), `TileWanderBehaviour` is the shipped default (leash, chase, retaliate, stand-your-ground, wander) and `TileActorRandom` is the per-actor splitmix64 stream that lets it stay stateless. Combat is a FOLLOWED interaction rather than a second movement authority: `TileCommandKind.Attack` names a NET ID, `TileMoveState.CombatTarget` carries the lock so the chase runs inside the one stepper both heads predict, and melee range is literally `TileReach.Contains` against a 1x1 rect. `ITileCombatRules` is the line between what the engine owns (whether a swing is DUE and whether it is LEGAL) and what the game owns (what it DOES), `TileHealth` and `TileCombatState` are the two new replicated components, and swings ride their OWN frame (`TileProtocol.ServerFrameCombat`, raised on the client as `CombatEvent`) because a miss moves health by zero and cannot be diffed out of a snapshot. A spawned PLAYER carries no `TileHealth`, deliberately: the game writes one through `TileWorldServer.SetHealth` or that player can neither swing nor be hit, and `SkippedHealthlessCombatantCount` is the reading that says so. `TileWorldClient` owns the prediction, the remote `ClientReplicationView` and its own command tick phase-offset from the server's, and raises the door refusals. `TilePresenter` is the pure bridge to a view, and the only place `TileWorldSpace` is consulted, which no server code ever touches. What a body actually DRAWS at is the full-step LINEAR GLIDE from `StepFrom` into `Tile` by the step's own tick count, which is the OSRS model and a ruled behaviour with no knob (a glide window and a damped chase were both built and both rejected inside the unreleased 18.1.0). The bound a design reads committed tiles against is therefore one STEP, half a tile on average, plus `InterpolationDelayTicks` for a remote, and the mitigation is game-side VISIBILITY rather than a tighter curve: `TilePresenter.PoseAt(tile)`, `TileWorldClient.TryGetRemoteTile` (a remote on the delayed timeline, agreeing with its drawn body), `TileWorldClient.TryGetLatestRemoteTile` (the same remote off the freshest snapshot, which is what a RULE reads, reporting the answer's age in ticks) and the committed `Tile` plus remaining `Route` on `ClientPrediction.PredictedState` are the reads a true-tile marker and a route highlight are built on. The glide needs no route, so a remote draws exactly where its owner draws it. `TileWorldPersistence` binds the generic `WorldStore` persistence core (`StatePersistence<TState>`) to a tile head, and `TilePlayerRecord` is the tile, plane and facing it stores. GPU-free and headless: the whole suite runs both heads in one process over an in-memory transport. A SIBLING of `KhaozEngine.NetWorld`, never a dependent of it. Design: `docs/design/TILE-WORLD-NETCODE-DESIGN-2026-08-22.md` and `docs/design/TILE-COMBAT-ACTORS-DESIGN-2026-08-27.md`. | TileWorld, Netcode, Replication, Sharding, Simulation, WorldStore, Diagnostics |
| **KhaozEngine.Navigation** | Engine-owned NPC navigation: `NavGrid` clearance-grid walkability (a per-cell byte clearance from a two-pass chamfer distance transform, one bake serves every agent radius), `NavSpace`/`NavLink` (stacked layers joined by directed stair links), `NavGridBaker.BakeOverworld` (bakes terrain slope plus `WorldColliders` footprints into a grid, with an optional `extraBlocked` gameplay exclusion), `NavGridBaker.BakeOverworldSteps` (a step-aware bake over a per-cell walkable surface height from an `INavSurfaceProvider` such as `TerrainSurfaceProvider`, so ramps, staircases, and low standable props become walkable, with per-cell surface heights on the resulting `NavGrid`), `IPathPlanner`/`GridPathPlanner` (grid A* behind a swappable seam: same-layer line-of-sight fast path, 8-connected search with corner-cut prevention, string-pulled waypoints, cross-layer routing over `NavLink`s, a `PathQueryBudget`-capped `Partial` fallback), and `PathFollower` (turns a moving goal into a per-tick `CharacterMovement.StepTowards` steering direction, replanning on goal drift, corridor breach, or a cooldown-gated retry after `Unreachable`). Render-free, deterministic. In the `Foundation` umbrella. | Primitives, Collision, Terrain |
| **KhaozEngine.Dungeon** | Deterministic procedural dungeon generator: `DungeonGenerator.Generate(config, seed)` grows a multi-level room graph (rooms, corridors, and stairs) on a 3D tile grid, committing each connection together with its edge so a layout is completable by construction, then re-proves it via the always-on `DungeonSolver.Verify` (a cell-level flood fill plus four structural checks). `DungeonConfig`/`DungeonLayout`/`LayoutStats` are the tunables and result, and `DungeonJson` plus the embedded `DungeonSchema` round-trip both to JSON. `DungeonKitMap` (+ `Greybox()`) maps the abstract `DungeonPiece` vocabulary to concrete kit ids for two sinks: `DungeonMapDocEmitter.Emit` (bakes a layout into a `MapDoc` zone document) and `DungeonStamp.Build` (runtime kit-prop instances plus merged `PhysicsShape` statics), both placed in world space via `DungeonPlotTransform`. Corridors can be widened into grand multi-tile halls (`CorridorMinWidth`/`CorridorMaxWidth`) and a share of rooms grown as elongated `Hall` connectors (`HallChancePercent`), both off by default and byte-compatible when off. `CeilingMode = Roofed` turns the open-top default into an enclosed cave (turnkey ceilings over every walkable cell bar open verticals, in both sinks). `DungeonNav.Bake` (`KhaozEngine.Dungeon`) turns a layout into a headroom-aware `NavSpace` (a `Roofed` layout blocks a cell whose ceiling clearance is below the baked agent height), one layer per floor joined by stair `NavLink`s, for NPC pathfinding. Render-free and GPU-free. In the `Foundation` umbrella. | Primitives, MapDoc, Physics, Navigation |
| **KhaozEngine.MapEditor** | Opt-in in-engine map editor runtime over a `MapDoc` document: `EditorDocument`/`EditorHistory` (the engine's first undo/redo command stack, with drag/scrub gesture coalescing), `EditorCommands` (reversible placement/spawn/exclusion/scatter-override/region/terrain-feature edits, the only mutation path), `BakeRegionCommand` (freezes a scatter layer's procedural output over a rect into authored placements plus a covering exclusion), `EditorToolController` (the GPU-free per-frame tool policy: select + transform gizmo, place, draw exclusion/scatter-override/region, bake, Ctrl+D duplicate), and the turn-key `MapEditorScene` (a streamed `ViewportWorld` whose terrain far field, prop cull, camera far clip and ocean extent come from `MapEditorOptions.RenderDistance` (a `Terrain.RenderDistanceProfile`, default `Far`) as one coherent tier, a fly camera, and Gui chrome (toolbar with a Save button, tree outline, property-grid inspector, kit palette, status strip) with undo/redo/save hotkeys, a save-aware Shift+Escape exit dialog, an Escape settings menu (render distance Base/2x/4x live, sky/lighting/ocean presets and sliders, persisted through `EditorSettingsStore` to its own `editor-settings.json`), and session camera bookmarks) a per-game head pushes onto its `SceneManager`. `MapEditorLandingScene` is a turn-key entry menu (New Map, Open Recent via `EditorRecentFiles`, Open Map for maps a head-supplied `DiscoverMaps` hook finds outside the recents store, Quit) a head pushes beneath it. The headless core (document, history, commands, tool controller) is GPU-free and fully unit-tested. Only the scene's build/rebuild/teardown seams touch a device. Not bundled in any umbrella (the `Server.Admin` precedent), so a shipping game never pulls the editor. | MapDoc, Gui, Persistence, Terrain.Render3D, Render3D, Game, Game.Render3D |
| **KhaozEngine.Locomotion** | Render-free character locomotion core: `CharacterMovement.Step` from a `MoveCommand` (camera-relative WASD axis + run + camera yaw + jump) over a timestep, normalized diagonals, ground-clamped via a height delegate with an optional ground-normal delegate for steep-terrain contact. Two overloads share one horizontal core: `Step(Vector3,...)` (horizontal-only, Y instant-clamped) and the vertical-physics `Step(in MoveState,...)` (gravity + jump with coyote-time/jump-buffer + air control over a carried `MoveState`, plus optional 3D prop collision via `IPhysicsWorld?` (swept collide-and-slide: trap-proof against one-sided building meshes, walkable-surface following, and a `StepHeight` step-up so low stairs/curbs are walkable)). `StepTowards(in MoveState, Vector2 worldDir, ...)` is the world-space kinematic step for server-authoritative NPCs (enemy AI): the identical terrain follow + swept collide-and-slide + step-up + wall slide + bounds clamp, driven by a world heading instead of a camera yaw (parity by construction - player and AI share one collision core), with per-agent size/speed from `MoveTuning`. An optional fluid-medium provider `(x, z, feetY) -> MovementMedium` (a pure world read the game supplies on BOTH heads, like the ground delegate) scales horizontal speed by submersion depth for wading (a `MoveTuning` wade ramp: full speed at ankle depth to a floor at chest depth, plus the medium's own `WadeSpeedScale` zone dial). Null = dry land everywhere = bit-identical. Past chest depth the same seam drives **surface swim** (`MoveState.Swimming`, enter/exit hysteresis): gravity/ground-snap suspended, a critically-damped buoyancy settle to a waterline, `MoveTuning.SwimSpeed` travel, jump as a near-shore hop-out. Opt-in **airborne horizontal momentum** (`MoveTuning.AirMomentum`, off by default, so an unopted game is bit-identical) flies a carried `MoveState.HorizontalVelocity` while airborne, so a jump travels its whole arc at the speed it launched at, `AirControl` steers that arc instead of scaling it, and `MoveTuning.AirBrakeAccel` optionally bleeds it toward a slower command. Grounded motion is untouched. **Steep terrain is a surface you slide on, not a wall you are denied at** (17.28.0, replacing the 17.26.x ascent gate): a horizontal move whose destination is both past `MaxSlopeRadians` and above what that tick can REACH is a **wall contact** that keeps only its along-face component (so strafing along a cliff mid-jump keeps its lateral travel), and ground past `MaxSlopeRadians` grants **no traction** - `Grounded` stays false, so no jump and no landing latch, and gravity decomposed against the surface slides the character down the fall line onto walkable ground, into water, or off into open air. A contact deletes the INTO-SURFACE component and nothing else (17.28.0), so a fast run across a face keeps its contour momentum and a jump grazing one keeps its signed up-slope motion for gravity to take back, and a tick whose descent the world SWALLOWS is supported (the concave crease that motivates it is somewhere to jump out of rather than a soft-lock). Held input adds nothing to the slide carry and takes nothing from it, so only geometry sheds it. The reach is a `StepHeight` while GROUNDED and the tick's own resolved upward motion when not (17.29.0), so **altitude on steep ground comes only from real velocity, never from the ground clamp**, and wedge support additionally requires the footprint ring's fall lines to oppose by more than 120 degrees, so a gully still escapes while an open face grants nothing. A no-footing tick reads its GEOMETRY (fall line, contour, wall face, reach) from the ground-HEIGHT field by a central difference at capsule scale, leaving the ground-normal delegate to CLASSIFY only (17.29.0), because **the plane the slide resolves against must be the surface the ground clamp seats to** - a smoothed normal over a heightmap describes a different plane, and the two disagreeing pumped a measured 44 of 360 headings up a 74 degree face at 120 Hz. Climbing self-defeats for want of footing, descent and walk-offs are free, and it rides the already-replicated carried velocities, so there is no wire change and no new knob. **`MoveState.SupportGranted`** (17.29.0) exports the footing grant a jump consumes, which `Grounded` cannot show. **Authoritative facing** (17.26.0): `MoveCommand.FaceCamera` turns the body to the camera rather than to where it is walking (so a stationary character can turn on the spot), the carried result is `MoveState.FacingYaw` (0 faces -Z, canonical `[-pi, pi)`, `CharacterMovement.WrapYaw` / `FacingYawOf` convert), rate-limited by `MoveTuning.FacingTurnSpeed` (default infinite, which snaps), and `StepTowards` faces its steering direction so NPCs inherit it. **`MoveState.LandingImpactSpeed`** (17.26.0) exports the downward speed a landing erased, on exactly the landing tick, as the authoritative fall-damage input. One `MoveTuning` source of truth (speeds + gravity/jump/fall/feel + wade ramp + swim thresholds/buoyancy + airborne momentum + facing turn rate) shared by the local `CharacterController3D`, the authoritative server sim, and client prediction. No input/render/netcode. In the `Foundation` umbrella. | Primitives, Physics |
| **KhaozEngine.Determinism** | `DeterministicFpScope` / `DeterministicFp`: pins the CPU floating-point environment to a canonical IEEE state (round-to-nearest-even, FTZ/DAZ off, traps masked) for a fixed-tick / lockstep sim, then restores it, so a fixed-seed host sim doesn't drift across threads/machines. Pure-managed P/Invoke over `<fenv.h>`; `IsSupported` no-ops safely where unwired, logging a one-time Warn when it does. | Diagnostics |
| **KhaozEngine.Objectives** | Game-agnostic objective / goal tracking (in `Foundation`) for achievements / challenges / quests / dailies: `ObjectiveTracker` runs the pipeline signals -> counters -> declarative conditions -> completion event. A game `Report`s accumulators and `Observe`s peaks for opaque metric keys it chose; the tracker holds a `Persistent` (never resets) and a `Session` (`ResetScope` at the game's own run boundary) `MetricScope` counter per key, evaluates declarative `ObjectiveCondition`s (`AtLeast`/`Reached`/`AtMost`, AND-composed) that reference those keys, and fires an idempotent `ObjectiveCompleted` once per objective. Objectives are re-evaluated indexed by the changed key (no full scan), so thousands of reports/sec against hundreds of objectives stay cheap. Deterministic (pure counters + predicates, no RNG/wall-clock), presentation-free (optional localized `Name`/`Description` by `StringId`), and persistence-agnostic (`Capture`/`Restore` a plain `ObjectivesSnapshot` the game folds into its own save; `GetProgress` exposes per-condition current/target for a log). Rewards/points/trees stay game-side via an opaque `Metadata` payload echoed back on completion. No JSON in core. | App |
| **KhaozEngine.Progression** | Wall-clock progression primitives (in `Foundation`): `WallClockRewardSchedule`, a pure `readonly struct` over `DateTimeOffset` answering "is a periodic reward available now?" against real wall-clock time - immune to game `TimeScale` and to offline / time-skip catch-up caps, non-stacking (one availability at a time), and clock-step safe (a backward step or far-future timestamp can't brick or spam; arithmetic saturates instead of overflowing). Persistence-agnostic: the consumer stores the plain `NextAvailableUtc` timestamp in its own save. `Claim(nowUtc)` schedules the next one interval out; `Start(...)` exposes the first-run knob (immediate / full-interval / random 0..interval offset). | Pure .NET |
| **KhaozEngine.Items** | Game-agnostic item container kernel (in `Foundation`), the `Stats` split applied to slots: `ItemContainer` holds a fixed number of opaque `ItemStack` `(int ItemId, int Count)` slots, with stack-first adds for ids the game's own `Func<int, bool>` predicate calls stackable (one stack per container, saturating at `int.MaxValue`, never spilling a shadow stack), honest overflow (`Add` returns how many units entered, and the remainder is the game's to drop, refuse or spill), removes walking slots first to last, `Swap` for click-to-move, and `SetAt` as the sanitising codec door. `ItemContainerCodec` is the durable and wire form: version byte, `ushort` slot count, then one 10-byte entry per OCCUPIED slot ascending by slot index, little-endian everywhere, with `Validate` naming the quarantine reason (version, geometry mismatch, entry disorder, out-of-range slot, non-positive count) for a persistence layer. The engine never learns what an item IS: no catalog, no icon, no equip slot, no use effect, no economy rule. | Pure .NET |
| **KhaozEngine.Stats** | Game-agnostic layered stat computation (in `Foundation`): `StatSet` folds a per-channel `Base` value with any number of named `StatSourceId` contributions (`AddSource`/`RemoveSource`/`ClearSources`, over a `ReadOnlySpan<StatModifier>`) into `Value(channel) = (Base + sum(Flat)) * max(1 + sum(Percent), MinimumScale)`. Channels are dense int indices into a `float[]` (`ChannelCount`, `CopyValuesTo`), so a per-frame read of many channels is allocation-free, and the engine owns the channels and the fold only, never their meaning: no enum, no item, no equipment slot, no channel-to-channel derivation. `MinimumScale` (default `0`, `float.NegativeInfinity` to disable) floors the percent multiplier so stacked negative percents cannot invert a channel's sign (a `0.1f` value gives a "mitigation caps at 90%" shape). Recompute is lazy and per-channel (dirty-tracked, never accumulates a running total, since a remove is not the exact float inverse of its add), and the fold is source-insertion order, preserved across removals and stable when a source is replaced under its own id, so add-then-remove nets back to the base value bit for bit. No stacking rules, durations, expiry, or diminishing returns: that half of the boundary is the game's, the same split `KhaozEngine.Locomotion` already draws for per-entity speed scale. | Pure .NET |
| **KhaozEngine.Commerce** | Server-authoritative currency wallet: `IWalletStore` seam (atomic idempotent credit/debit, materialized balance, immutable ledger) + an `InMemoryWalletStore` reference/test backend, `Wallet` (grant/spend/redeem over the store), a source-agnostic entitlement pipeline (`VerifiedEntitlement`, `EntitlementProof`, `IEntitlementValidator`), `IProductCatalog`/`InMemoryProductCatalog`, and `PeriodicGrant` (a server-clock daily/periodic reward routed through the wallet, built on `Progression`'s `WallClockRewardSchedule`). Identity-agnostic (keyed by a verified `AccountId` the consumer supplies). Durable backends are the two opt-in packages below. | Progression |
| **KhaozEngine.Commerce.Sqlite** | SQLite `IWalletStore`/`IGrantScheduleStore` backend over `Microsoft.Data.Sqlite` (`SqliteWalletStore`): single held connection, semaphore-serialized transactions, composite unique index for idempotency. The zero-infra dev/test and single-node durable wallet store. Opt-in (NOT in the `Server` umbrella; add explicitly alongside it). | Commerce, Microsoft.Data.Sqlite |
| **KhaozEngine.Commerce.SqlServer** | SQL Server / Azure SQL `IWalletStore`/`IGrantScheduleStore` backend over `Microsoft.Data.SqlClient` (`SqlServerWalletStore`): fresh pooled connection per call, `Serializable` transaction, `MERGE WITH (HOLDLOCK)` upsert, raw parameterized ADO.NET, no EF/ORM. The production durable wallet store; same contract as the SQLite backend. Opt-in (NOT in the `Server` umbrella; add explicitly alongside it). | Commerce, Microsoft.Data.SqlClient |
| **KhaozEngine.Updates** | Delta auto-update pipeline: SHA256 manifests + diffing, a host-agnostic update source, an `UpdateService` state machine with resumable staged downloads, a cross-platform staged-apply core (`UpdateApplier`) with an atomic file swap and a resilient post-apply relaunch (settle wait + antivirus/image-race retry), and an optional native Win32 progress window (`IUpdaterUi`) for the shim. Self-update hardening: an exit barrier (the install stays untouched if the game is still running when it expires), elevate-once for protected roots (`IUpdaterEnvironment.CanWriteToDirectory`/`TryElevate`), and a commit-aware rollback backstop that restores backups on a failed apply. The shim (`UpdaterShim.Main`) ships as a `WinExe` (no console flash over the game) and attaches the parent console on launch so its diagnostics still reach a terminal; the `updater.log` file is written regardless. | Diagnostics, Platform, Primitives |
| **KhaozEngine.ServerStatus** | Out-of-band server-status for a live game (an MMO first): the versioned, tolerant-read `ServerStatusReport` wire contract a small public status endpoint serves (`schemaVersion`/`health`/`serverVersion`/`minClientVersion`/`latestClientVersion`/`lastHeartbeatUtc`/`lastDeployUtc`/`expectedBackUtc`/`motd`, `TryParse`/`ToJson`, unknown fields ignored, missing optionals default, an unknown `health` token degrades to `Unknown`, so the endpoint can evolve ahead of shipped clients), the `IServerStatusSource` fetch seam + default `HttpServerStatusSource` (HTTPS-enforced, response size-capped, never throws), `ServerStatusClient` (async never-throwing poller, default 30 s, retains the last-known report as a `ServerStatusSnapshot` with a staleness/failure clock), `ServerStatusEvaluator` (pure report + local client version + staleness -> an actionable `ServerStatusState`: `ServerOk`/`ServerRestarting`/`ServerDown`/`UpdateRequired`/`UpdateAvailable`/`StatusUnknown`, driving reconnect + forced-update + "back soon" screens, numeric `x.y.z` gates via `VersionOrder`), the `IServerHeartbeatSink` liveness-write seam + `ServerHeartbeat` row + `ServerHeartbeatService` cadence driver a game server calls on a timer (`Null`/`InMemory` reference sinks, the SQL upsert is the game's, not the engine's), and `ServerStatusReadout.Build` (pure, deterministic, GPU-free and Gui-free row-list structure for an in-game "server status" page: an ordered `IReadOnlyList<ServerStatusReadoutRow>` with a stable machine `Key` via `ServerStatusReadoutKeys`, a preformatted invariant-culture `Value`, and the raw typed value for a game that wants to format it itself, e.g. a fully localized duration). GPU-free, no cloud-specific code (the endpoint and its status DB are per-game infra). Multi-targets `net8.0` alongside `net10.0` (with its `Diagnostics` + `Primitives` chain) so the status endpoint can run as an Azure Functions isolated-worker app on the Linux Consumption plan, which has no .NET 10. | Diagnostics, Primitives |
| **KhaozEngine.Http** | A reusable bounded send-with-retry HTTP helper over a caller-supplied `HttpClient`: `HttpRetry.SendAsync` / the `client.SendWithRetryAsync(...)` extension retry a retryable status (408/500/502/503/504 by default) or a transport fault (`HttpRequestException`, a non-caller cancellation) up to `HttpRetryPolicy.MaxAttempts` (default 3), each attempt bounded by its own `PerAttemptTimeout` (default 12 s, deliberately shorter than a typical HttpClient-wide timeout so one stalled attempt is abandoned instead of eating the whole budget) via a linked `CancellationTokenSource`, with an injectable `Backoff` (default 700 ms / 1600 ms / 3000 ms, the no-sleep test seam is `_ => TimeSpan.Zero`) and `IsRetryableStatus` predicate. The caller's own cancellation is never retried. `HttpRetryPolicy` is an immutable record that validates on every assignment, including through a `with` expression. Field-proven as Nullwake's sign-in retry policy (absorbing a free-tier App Service cold start that surfaced client timeouts on roughly half of first-attempt sign-ins) before being promoted here. Multi-targets `net8.0` alongside `net10.0` for the same Azure Functions Linux Consumption reason as `ServerStatus`. | Pure .NET |
| **KhaozEngine.Netcode.Abstractions** | The zero-dependency channel-split contract: `IChannelSplittable<TSelf>` + `NetChannelReliability`. Reference this alone from a transport-agnostic DTO project (e.g. one shared with a web server). | Pure .NET |
| **KhaozEngine.Netcode** | Transport-free netcode primitives (`System.Numerics`): `UnitAxisQuantizer` (deterministic 8-bit axis codec), `ClientPrediction<TState,TCommand>`, `RemoteCommandQueue<TCommand>`, and the `INetTransport` byte-transport seam (`NetConnectionId`/`NetEvent`) with a deterministic in-memory `LoopbackTransport` for headless tests + local play; the `NetServer`/`NetClient` session layer (Hello/Welcome handshake, slot assignment, `IConnectionAuthenticator` gate that returns the verified subject) plus `SignedToken` + `HmacTokenAuthenticator`, a zero-dependency HMAC-SHA256 signed connect-token; `RateLimiter` (a deterministic per-connection token bucket for message-flood protection) and `NetServer.Disconnect(slot)` (a kick seam); `BoundedEventQueue<T>` (a drop-oldest hard cap the `NetServer` session inbox uses so a stalled or flooded host can't grow undrained events without bound, drops counted in `DroppedEventCount`); `NetTransportStats` + the default-interface `INetTransport.Stats` (RTT / loss / byte counters, `Unavailable` by default, filled by the LiteNetLib UDP binding) forwarded via `NetClient.TransportStats`. Type-forwards the channel-split contract from Abstractions. | Netcode.Abstractions |
| **KhaozEngine.Netcode.LiteNetLib** | LiteNetLib transport binding: `ChannelSplitter` maps `NetChannelReliability` to LiteNetLib's `DeliveryMethod`, plus `LiteNetLibServerTransport`/`LiteNetLibClientTransport` (`INetTransport` over reliable-UDP; their raw event inboxes are bounded the same way via `BoundedEventQueue<T>` - optional `maxQueuedEvents` + `DroppedEventCount`). | LiteNetLib, Netcode |
| **KhaozEngine.Simulation** | Headless simulation-host primitives: `FixedTickHost`, a deterministic fixed-timestep accumulator (turns variable elapsed time into whole fixed-dt ticks, with a spiral-of-death backlog guard) that decouples sim rate from render rate; and `IJobScheduler`, the engine's worker-pool seam (`SingleThreadedJobScheduler` inline default + `ThreadPoolJobScheduler` over `Parallel.For`) for fanning independent jobs across cores, each worker body pinned to the canonical FP environment so a sim fanned across the thread pool cannot diverge in the low bits. The base of the authoritative server loop. | Determinism |
| **KhaozEngine.Replication** | Authoritative ECS replication: `NetId` (64-bit since 10.0.0, allocated by `NetIdAllocator` under a node-prefix scheme) + a closure-based `ReplicationRegistry` (consumer extension component ids >= `FirstExtensionTypeId` are length-prefixed on the wire, so an older client skips an id it never registered instead of disconnecting; optional per-registration `ReplicationChannels` - `Replicate`/`Persist`/`Migrate`/`OwnerOnly` - so a component can be server-only or owner-only rather than always persisted==replicated==migrated), `SnapshotWriter` (full-state + `WriteFiltered` per-client interest, with an overload that re-emits opaque `RetainedComponent` extension frames), `SnapshotBlobReader`/`SnapshotBlobWriter` (walk / rebuild the snapshot wire format for cell-blob migrations), `ClientReplicationView` (`Apply`/`ApplyDelta`: spawn/despawn/update + interpolation, skip-unknown-extension, self-healing acked baseline; `TryApplyRetainingUnknown` retains an unknown extension frame instead of dropping it), `ServerReplicator` (per-slot acked whole-world baseline/delta), `AoiDeltaReplicator` (per-client, `NetId`-keyed, area-of-interest-scoped baseline/delta - entered->full, changed->component delta, left->despawn; what the servers serve on the live path), and `InterestGrid` (area-of-interest spatial query). Transport-free (snapshots/deltas are `byte[]`). | Ecs |
| **KhaozEngine.WorldStore** | Server-side durable-state seam: `IWorldStore` (async keyed `byte[]`, DB-shaped) + a thread-safe `InMemoryWorldStore` reference impl. Also the fleet's player-persistence core: `StatePersistence<TState>` runs load-on-join, save-on-leave, the periodic dirty snapshot, the guest policy and quarantine over the `IPersistenceHost<TState>` host seam, taking a state's encode / decode / position / validation from a `PersistenceBinding<TState>`, its knobs from `PersistenceCoreConfig` and its rejoin seed from `PositionHintCache`, so one wiring serves every head in the fleet. `NetWorld.WorldPersistence` (continuous world) and `TileWorld.Netcode.TileWorldPersistence` (tile lattice) are its two bindings. Durable backends are the two opt-in packages below. | Pure .NET |
| **KhaozEngine.WorldStore.Sqlite** | SQLite `IWorldStore` backend over `Microsoft.Data.Sqlite` (`SqliteWorldStore`): one `world_store` table, `INSERT ... ON CONFLICT` upsert, raw parameterized async ADO.NET, no EF/ORM. The zero-infra dev/test + single-node backend (keeps persistence headless-testable). Opt-in (NOT in the `Server` umbrella; add explicitly alongside it). | WorldStore, Microsoft.Data.Sqlite |
| **KhaozEngine.WorldStore.SqlServer** | SQL Server / Azure SQL `IWorldStore` backend over `Microsoft.Data.SqlClient` (`SqlServerWorldStore`): `MERGE WITH (HOLDLOCK)` upsert, raw parameterized async ADO.NET, no EF/ORM. The production backend (Azure SQL); same contract as the SQLite one. Opt-in (NOT in the `Server` umbrella; add explicitly alongside it). | WorldStore, Microsoft.Data.SqlClient |
| **KhaozEngine.Sharding** | World topology for an authoritative server: a uniform grid of authoritative cells. `CellCoord` (world position -> integer cell coord), `CellSim` (one cell = one SIMULATION ISLAND: an ECS `World` + its own optional `IPhysicsWorld` + its own island `WorldFrame` + `FixedTickHost` + `ServerReplicator` + `InterestGrid` + read-only border ghosts), `ShardHost` (owns the cell map, creates cells on demand, routes entities to the cell containing their position, ticks every cell at one fixed rate, `SyncGhosts` mirrors border-overlap entities into neighbor cells as `Ghost`s over the `ICellLink` seam, `ProcessHandoffs` transfers authority on a boundary crossing with exactly-once semantics, and `SnapshotForClient` serves a bound client its whole area-of-interest from its single home cell with seamless re-bind on crossing). `ShardHost.TryGetOwner` / `CellSim.TryGetOwned` resolve NetId ownership (cell + owned entity) in O(1) off a maintained netId -> (cell, entity) index instead of a per-cell scan, so per-player / per-NPC-per-tick lookups stay flat as the world grows. Spawn via `ShardHost.SpawnOwned` (or `CellSim.RegisterOwned`) to index eagerly. `Tick` fans the independent cells across an opt-in `IJobScheduler` (default inline) for near-linear-in-cores throughput. The in-process container the seamless-shard topology builds on. Also carries the per-cell persistence primitives: `CellSim.SnapshotOwned`/`RestoreOwned` snapshot and restore a cell's owned (non-ghost, non-migrating) entities through the Replication codec (`TryRestoreOwned` is the non-throwing form: it rolls a corrupt blob back and retains unknown extension frames for verbatim re-persist), `CellSim.MaxOwnedNetId` reads the highest owned NetId, and `ShardHost.CellCreated` (+ `EnsureCell`) is the load hook a persistence layer subscribes to on first cell instantiation. | Ecs, Simulation, Physics, Replication |
| **KhaozEngine.NetWorld** | Render-free networked-world layer wiring movement to the authoritative netcode stack: `PlayerMoveState` (wraps a `Locomotion.MoveState`: position + vertical velocity + grounded) + the replicated `MovementState` component carrying the vertical axis plus the carried airborne horizontal velocity on the wire (survives a sharded handoff, forms the client's exact reconcile basis); `PlayerMoveSimulator`/`PlayerMovementSystem` (run `CharacterMovement.Step` server-authoritatively and inside client prediction), `WorldServer`/`ShardedWorldServer` (authoritative sim + per-client AoI snapshots over `SnapshotWriter`+`InterestGrid`, headered with the receiver's net id + last-acked seq, with an opt-in server-side anti-cheat layer - `AntiCheatConfig`: per-connection message rate limiting + an `OnSuspiciousActivity` signal hook for malformed/NaN, flood, and movement-correction anomalies - plus a `Disconnect(slot)` kick seam), and `WorldClient` (wraps `NetClient`+`ClientReplicationView`+`ClientPrediction`, reconciles position + the vertical axis, exposes `EntityRenderState[]`: local predicted, remotes replicated and smoothly interpolated between snapshots by default (a remote glides instead of teleporting one ~tick-rate snapshot-step per ingest, driven by `AdvancePresentation`; opt out with `WorldClientConfig.InterpolateRemotes = false`), each carrying the exact grounded flag + vertical velocity + swimming flag + the authoritative `FacingYaw` (local predicted, remote from the replicated `MovementState`) so an animator bridge reads jump/fall/swim/heading for remotes too, plus the local player's predicted `LandingImpactSpeed`; optional `WorldBounds`/`IPhysicsWorld?` ctor params mirror `WorldServer` so prediction runs against the same bound + solid props, no rubber-banding). Also `WorldPersistence` (+ `PlayerRecord`): wires an `IWorldStore` into the server lifecycle (load-on-join / save-on-leave / periodic dirty snapshot) so the world survives a restart, backend-agnostic; a game rides its own durable per-player blob (XP / inventory / quests) on the same record + machinery via `WorldPersistenceConfig.CaptureGameState`/`ApplyGameState` (opaque bytes, account-keyed so unaffected by handoff, game-side migration). A loaded record is validated before it is applied: optional `WorldPersistenceConfig.Bounds`/`ValidateGameState` hooks reject an out-of-bounds position or an implausible blob (as does an undecodable record), quarantining the whole record to `quarantine:player:{accountId}` and firing `OnRecordQuarantined` rather than applying it. Also `CellPersistence` (+ `CellPersistenceConfig`, `ICellPersistenceHost`, `WorldMetaRecord`): per-cell world-state persistence to an `IWorldStore` for a `ShardHost`-based server, load-on-cell-create plus a periodic dirty save of changed cells plus a NetId high-water record, so a sharded cell's non-player entities survive a restart alongside the existing player-keyed `WorldPersistence`. Cell blobs evolve safely: `CellPersistenceConfig.RegisterMigration` brings an old blob forward (contiguity-validated chain), an undecodable blob is quarantined under `quarantine:cell:{x}:{y}` rather than crash-looping the server, an unknown extension frame is retained and re-persisted verbatim (a registry regression no longer strips data at rest), and `CellPersistence.Issue` surfaces migrated / skipped / quarantined / retained events for ops. `WorldClient.NetStats` (`ClientNetStats`) surfaces connection health for a telemetry overlay: RTT / loss / byte rates from the transport, the AoI snapshot ingest rate, and the prediction-correction magnitude (last + rolling avg). `WorldClient.LocalHorizontalSpeed` gives the local player's predicted planar speed (off `ClientPrediction.PredictedHorizontalSpeed`, computed per prediction tick, immune to reconciliation snaps) for a HUD / audio / locomotion blend, alongside `LocalGrounded` / `LocalVerticalVelocity`. Client self-rescue: `WorldClient.RequestSelfRescue()` asks the server to teleport the local player to a server-decided safe spot (an "unstuck"), gated by `WorldServerConfig.SelfRescueDestination` (null = off) + `SelfRescueCooldownSeconds` on both servers; reuses the admin Teleport apply path, rides a length-distinct control frame (`MoveProtocol.ClientControlKind`) so older servers ignore it. Reconnect input backlog: `WorldClient.SendInput` no-ops (returns `-1`) unless connected, so a per-frame send loop builds no stale-input backlog across a long reconnect outage, and `WorldServerConfig.MaxInputBacklog` / `ShardedWorldServerConfig.MaxInputBacklog` (default 8 ticks) caps how far behind live the server falls under a deep backlog (skip-to-newest, latest-wins), so a player is never frozen under minutes of old input on rejoin. Server-owned non-player entity seam (NPCs / enemies): a consumer-injectable `ReplicationRegistry` on `WorldServer`/`ShardedWorldServer`/`WorldClient` (build it with `MoveProtocol.CreateRegistry(configure)`, register game components at ids >= `MoveProtocol.FirstConsumerTypeId`), `SpawnEntity(x, z, configure)` (non-colliding NetId from the authoritative allocator, cell-placed, persisted), an `OnBeforeTick` server-brain hook (with its 17.26.0 mirror `OnAfterTick`, which fires after movement + serving on both heads and is where a game reads `MoveState.LandingImpactSpeed` per slot to apply fall damage, sharded-gated to frames that actually stepped so a short frame cannot re-deliver a landing), and `WorldClient.TryGetComponent<T>(netId)` to read a server-assigned discriminator per entity (NPC kind / HP / faction). An older client skips a component it never registered instead of disconnecting. Generic game-message seam: `WorldClient.SendGameMessage(kind, payload, reliability)` + `OnGameMessage(slot, kind, payload)` on both servers (client->server), `SendGameMessageTo`/`BroadcastGameMessage` + `WorldClient.GameMessageReceived` (server->client), for game-defined messages (attack / interact / chat / inventory) beside the movement protocol - opaque bytes the engine never deserializes, demuxed ahead of the move so they never alias it, rate-limited + size-capped (`MaxGameMessageBytes`, oversize flagged `SuspiciousReason.OversizedMessage`), the server frame version-skew-safe for older clients. Replicated dynamic physics props: a `DynamicBodyState` built-in component (interpolated orientation quaternion + linear/angular velocity, riding alongside `ReplicatedPosition`) plus `DynamicBodyReplication` (a server-side sampler that writes each awake body's `IPhysicsWorld.GetDynamicPose`/`GetDynamicVelocity` into those components each tick, sleep-gated), so a server-authoritative dynamic rigid body (a crate / barrel) replicates to clients that interpolate it on the same fixed-delay buffer as a remote player and never simulate it (no client-side prediction of bodies). In the `Server` umbrella. | Locomotion, Physics, Diagnostics, Netcode, Replication, Ecs, Sharding, WorldStore, Serialization |
| **KhaozEngine.Server.Admin** | Opt-in HTTPS admin endpoint (Kestrel + bearer token over TLS) over the `ServerAdmin` surface (list/teleport/kick/broadcast, account enumeration, ban/unban, and game-registered admin actions via `GET /actions`, `GET`/`POST /actions/{name}`). The only package that references ASP.NET Core; not in the `Server` umbrella. | NetWorld, WorldStore, Microsoft.AspNetCore.App |
| **KhaozEngine.Updates.Tool** | The `ke-updater` dotnet tool (`PackAsTool`): generate, sign, and verify delta-update manifests (RSA-2048) for the `Updates` pipeline. Author-time tool, not a runtime package. | Updates |
| **KhaozEngine.Localization.Analyzers** | Roslyn analyzer enforcing the `LocalizedText` localization contract: `KELOC001` flags player-facing text passed as a raw `string` to a `[LocalizationStringSink]` member, `KELOC002` flags `LocalizedText.Raw(...)` outside `[LocalizationExempt]` or DEBUG code, `KELOC003` flags a bare string literal drawn straight to `Render2D.SpriteBatch.DrawString` (literals only). Ships as warnings; a repo raises any to error in `.editorconfig`. Flows to consumers via the `Game2D`/`Game3D` umbrellas. | (analyzer; no runtime deps) |
| **KhaozEngine.CodeHealth.Analyzers** | Roslyn analyzer enforcing the fleet's file-size ratchet: `KESIZE001` flags a file listed in `.filesize-baseline` that grew past its recorded line count (it may shrink freely, never grow), `KESIZE002` flags a file not in the baseline that exceeds the 800-line cap (`KhaozFileSizeCap` overrides). Ships buildTransitive props that auto-discover the repo's `.filesize-baseline` and hand it to the analyzer as an `AdditionalFile`, so adoption is zero-touch beyond running `scripts/check-file-size.sh --init` once. The semantic authority is `scripts/check-file-size.sh` in game-template. Flows to consumers via all four umbrellas. | (analyzer, no runtime deps) |
| **KhaozEngine.Localization.TestKit** | Test-only localization coverage guard for a game's **test project**: `LocalizationCoverage.AssertComplete` takes a key universe (a keys class of `public const string` or `StringId` fields, the neutral resx's own entries via just a `ResourceManager`, or an explicit key sequence), then asserts every key resolves in the neutral resx AND in each shipped satellite with parent fallback disabled (a missing translation fails instead of silently reading the neutral language), plus placeholder-index integrity per culture. Framework-agnostic (throws `LocalizationCoverageException`, which fails any xUnit/NUnit/MSTest test). In NO umbrella - reference it explicitly from the test project. | App |
| **KhaozEngine.Sfx.Tool** | The `ke-sfxbake` dotnet tool (`PackAsTool`): manifest-driven bulk SFX generation + bake. Reads a per-game `sfx.manifest.jsonc`, generates each effect via the ElevenLabs sound-effects REST API, encodes with ffmpeg/oggenc, idempotent via `.sfxmeta` sidecars. Author-time tool, not a runtime package. | Serialization |
| **KhaozEngine.PropSurface.Tool** | The `ke-propbake` dotnet tool (`PackAsTool`): bakes a 3D collision `.coll` shape for every prop (trees get a leaning trunk-hull collider) and a walkable-surface `.surf` heightmap for walkable-solid props (rocks/logs/buildings) in a kit manifest, stamping the `surface`/`heightmap` fields and feeding `PropCollisionLoader` for physics wiring. Run as the last kit-ingest step (re-ingest = re-bake). Author-time tool, not a runtime package. | Render3D |
| **KhaozEngine.MapEdit.Tool** | The `ke-mapedit` dotnet tool (`PackAsTool`): an MCP (Model Context Protocol) server over stdio exposing 78 document, query, mutation, and headless-render verbs over a `MapDoc` document, so an AI client edits a zone the same way the `MapEditor` GUI does. GPU is needed only for the two render verbs (`render_topdown`, `render_view`), everything else runs headless. Author-time tool, not a runtime package. | MapDoc, MapEditor, Imaging, ModelContextProtocol |
| **KhaozEngine.TileEdit.Tool** | The `ke-tileedit` dotnet tool (`PackAsTool`): an MCP (Model Context Protocol) server over stdio exposing 43 world, tile, height, object, marker, prefab, collision, and headless-render verbs over a tile world, so an AI client can author a world before any GUI editor exists. One world open at a time in a lock-internal `TileEditSession` (`world.json` is self-describing, so `world_open` is one argument, and catalog paths plus every other path argument resolve against the WORLD directory rather than the client's working directory), with `QueryService`, `MutationService`, `RenderService` and `TopDownOverlayPainter` behind it. Every mutation runs through the `TileWorld.Editing` command layer and seals its gesture, so ONE VERB IS ONE UNDO STEP (`undo`/`redo` take a step count) and an MCP edit is undoable exactly like a GUI one, with the derived collision map rebaked over the edit's dirty rects before the next query reads it. Tile space throughout (x east, z north, rects `x, z, width, height` with exclusive far edges), every ASCII map and height row set NORTH FIRST, `render_view` alone in world metres where world z is minus tile z. `height_import` reads binary PGM (P5), not PNG, since the engine ships no PNG decoder. GPU is needed only for the two render verbs, which return the PNG inline as an image content block after a framing text block, plus an optional `savePath`. Author-time tool, not a runtime package. | TileWorld.Editing, TileWorld.Render3D, Imaging, ModelContextProtocol |

**Umbrella metapackages** (code-free curated dependency groups - one `<PackageReference>` instead of a dozen):

| Metapackage | Pulls in | For |
|---|---|---|
| **KhaozEngine.Game2D** | 2D runtime (Windowing/Render2D/Gui/Audio/Particles/Telegraphs) + the three engine-owned GPU backends (`Gpu.Metal` / `Gpu.D3D11` / `Gpu.Vulkan`, carried by the umbrella since 18.0.0 so a repinned game gets a working device on every desktop platform without naming a backend package by hand, and the registration itself stays automatic through `AppWindow`) + `Game` + `Foundation` + the `Localization.Analyzers` analyzer + the `CodeHealth.Analyzers` file-size ratchet (both applied in the consumer's build) | a desktop 2D game |
| **KhaozEngine.Game3D** | `Game2D` (so it also carries the `Localization.Analyzers` and `CodeHealth.Analyzers` analyzers) + `Render3D` + `Game.Render3D` (the 3D scene bridge) + `Particles.Render3D` (modern particle VFX glue + presets) + `Telegraphs.Render3D` + `Terrain.Render3D` (chunked-LOD terrain mesh + world streaming) + `TileWorld.Render3D` (tile-world ground mesher and textured ground materials, prop path, water planes, region streaming and snapshot capture) | a desktop 3D game |
| **KhaozEngine.Server** | `Foundation` + netcode (`Netcode`/`.Abstractions`/`.LiteNetLib`) + `Simulation` (fixed-tick host) + `Replication` + `WorldStore` (the `IWorldStore` seam + `InMemoryWorldStore` only, the `.Sqlite` / `.SqlServer` durable backends being opt-in siblings, added explicitly, not bundled) + `Sharding` (cell grid) + `NetWorld` (authoritative movement server + client glue + `WorldPersistence`) + `TileWorld.Netcode` (the SIBLING tile movement stack: discrete click-to-walk, server-owned actors, melee combat, its own server, client, presenter and persistence binding, and no path to `NetWorld`) + the `CodeHealth.Analyzers` file-size ratchet | a headless sim server (no GPU) |
| **KhaozEngine.Foundation** | the GPU-free foundation (Primitives/App/Content/Diagnostics/Ecs/Http/Identity/Locomotion/MapDoc/TileWorld/TileWorld.Editing/Dungeon/Navigation/Objectives/Persistence/Progression/Stats/Items/Serialization/ServerStatus/Social/Collision/Physics/Terrain/Determinism/Platform/Updates) + the `CodeHealth.Analyzers` file-size ratchet | a gameplay-logic library (no renderer) |

`KhaozEngine.Commerce` is not in any umbrella; a game or server that wants the wallet adds it explicitly. Same as `WorldStore`, its `.Sqlite` / `.SqlServer` durable backends are opt-in siblings, not bundled anywhere (including `Server`).

Target framework `net10.0`, with two exceptions: `KhaozEngine.ServerStatus` and its dependency chain
(`KhaozEngine.Diagnostics`, `KhaozEngine.Primitives`), plus the dependency-free `KhaozEngine.Http`, also
multi-target `net8.0`, so a small status endpoint (or any other Azure Functions consumer that wants the
bounded HTTP retry) can run as an isolated-worker app on the Linux Consumption (Y1) plan, which supports .NET
8 but not .NET 10. Every other package stays `net10.0`-only. MonoGame-free: Silk.NET windowing/input (GLFW
natives bundled per-RID), the engine's own Metal / Direct3D 11 / Vulkan backends behind `KhaozEngine.Gpu`
for the GPU (with `Silk.NET.Shaderc` and `Silk.NET.SPIRV.Cross` as the shader toolchain, which replaced
`Veldrid.SPIRV` in 18.0.0 and left no Veldrid package in the graph), Silk.NET.OpenAL for audio.
`System.Numerics` math throughout.

## Why it exists

KhaozEngine began as a shared input + screen-stack foundation extracted from three MonoGame games (a
Rule-of-Three extraction), with one improvement: the raw hardware read sits behind a seam so the whole
input + routing surface is **unit-testable without a device**. It then grew into a full custom stack and the
games migrated **off MonoGame entirely** - MonoGame's GLSL-1.20-on-Apple dead end forced a custom Veldrid/Metal
renderer, and once 2D, 3D, text, audio, and input were all proven on the custom stack the legacy MonoGame
packages were deleted. The headline payoff is unchanged: the **click-through fix** (a tap only registers when
press-origin and release land in the same target, and overlays reserve their footprint so clicks never leak to
the layer beneath) lives in one place. See [`docs/CROSS-PLATFORM.md`](docs/CROSS-PLATFORM.md) for the per-OS GPU story.

## The one rule that matters most

> **`AppWindow` is the only code in the entire stack that touches the Silk.NET/GLFW input.** Everything above it
> reads an immutable `InputState` snapshot (handed in each frame via `Frame.Input`) through `InputManager` /
> `Pointer`. Games must not reach around the seam - doing so re-introduces the untestable, click-through-leaking
> pattern this library exists to kill. And hit-test with the bounds helpers (`IsTapIn`, …), never raw
> position + button.

Full consumer contract: [`docs/USING-KHAOZENGINE.md`](docs/USING-KHAOZENGINE.md). Read it before wiring a game in.
All docs are indexed in [`docs/INDEX.md`](docs/INDEX.md) (living docs vs the dated design archive).

## Quickstart (the canonical game-loop wiring)

A game subclasses `GameApp` (2D) or `GameApp3D` (3D) and overrides the per-frame seams; the base owns the
`AppWindow.Run` loop, clock, viewport, input, and the 2D batch. The smallest thing that runs:

```csharp
using KhaozEngine.Game;
using KhaozEngine.Render2D;
using KhaozEngine.Windowing;

public sealed class MyGame : GameApp
{
    public MyGame() : base(GameAppOptions.For("My Game", 1280, 720)) { }

    protected override void OnUpdate(float dt) { if (Input.WasPressed(Key.Escape)) Quit(); }
    protected override void OnDraw2D(SpriteBatch batch) { /* batch.Draw(...) / DrawString(...) */ }
}

// Program.cs
using var game = new MyGame();
game.Run();
```

Full wiring reference (the 3D `OnDraw3D`/`GameApp3D` pass, scenes, input, fonts, the click-through-safe
`Pointer.IsTapIn` pattern): [`docs/USING-KHAOZENGINE.md`](docs/USING-KHAOZENGINE.md), "Wiring a game".

## Consuming the packages

Published to GitHub Packages on tagged releases, and packed to a local file-feed for day-to-day development.
The repo is public, so the Packages feed needs no authentication to restore from.

```xml
<!-- nuget.config (additive) -->
<add key="khaozengine-local" value="~/KhaozEngine/local-feed" />
<!-- or the GitHub Packages feed (public, no PAT needed):
     https://nuget.pkg.github.com/APKiwiOrg/index.json -->
```
```xml
<!-- One reference per project via an umbrella metapackage. Pick the bundle that fits: -->
<PackageReference Include="KhaozEngine.Game2D"     Version="18.9.0" />  <!-- desktop 2D: 2D runtime + GameApp/SceneManager + foundation -->
<PackageReference Include="KhaozEngine.Game3D"     Version="18.9.0" />  <!-- desktop 3D: Game2D + Render3D + the 3D scene bridge -->
<PackageReference Include="KhaozEngine.Server"     Version="18.9.0" />  <!-- headless: foundation + netcode, no graphics -->
<PackageReference Include="KhaozEngine.Foundation" Version="18.9.0" />  <!-- gameplay-logic lib: foundation only, no renderer/netcode -->
```

The metapackages have no code; they just pull in the granular packages. You can still reference those
directly (e.g. just `KhaozEngine.Netcode.Abstractions` for a wire-contract project) and mix a bundle with extra
packages (e.g. `KhaozEngine.Game2D` + `KhaozEngine.Netcode.LiteNetLib` for a 2D multiplayer game).

**Versioning is SemVer.** Each game pins a version and adopts fixes by bumping it - so you can keep one game on an old version while you migrate another. Don't fork the packages; if a game needs an API that isn't there, add it here and bump the version.

## Testability standard

Every input and routing path is covered by headless xUnit tests (split per area, e.g. `KhaozEngine.Render.Tests`
for `Windowing`/`InputManager`), by constructing `InputState` snapshots frame-by-frame and feeding them to
`InputManager.Update` (`dt` is a plain `float` in seconds). New behaviour ships with a headless test in its
matching per-area project, or the rump `KhaozEngine.Tests` when genuinely cross-cutting. This is the standard,
not a nicety - it's the reason the raw read sits behind the `AppWindow`/`InputState` seam.

```bash
dotnet test
```

## Repo layout

```
# Custom render/runtime stack
KhaozEngine.Gpu/   KhaozEngine.Windowing/   KhaozEngine.Render2D/   KhaozEngine.Render3D/   KhaozEngine.Gui/
KhaozEngine.Imaging/   KhaozEngine.Snapshot/   KhaozEngine.Snapshot.Render3D/
KhaozEngine.Audio/   KhaozEngine.Particles/   KhaozEngine.Game/   KhaozEngine.Game.Render3D/
KhaozEngine.Particles.Render3D/   KhaozEngine.Telegraphs/   KhaozEngine.Telegraphs.Render3D/   KhaozEngine.Terrain.Render3D/
KhaozEngine.TileWorld.Render3D/
KhaozEngine.Gpu.D3D11/         Engine-owned native Direct3D 11 backend, the Windows default since 17.40.0 and the only Windows backend since 18.0.0 (carried by Game2D/Game3D)
KhaozEngine.Gpu.Vulkan/        Engine-owned native Vulkan backend, the Linux default since 17.40.0 and the only Linux backend since 18.0.0 (carried by Game2D/Game3D)
KhaozEngine.Gpu.Metal/         Engine-owned native Metal backend, the macOS default since 17.40.0 and the only macOS backend since 18.0.0, headless or windowed device that records, draws and presents (carried by Game2D/Game3D)
KhaozEngine.MapEditor/         Opt-in in-engine map editor runtime over MapDoc (in no umbrella)
# Foundation (GPU-free, pure .NET)
KhaozEngine.Primitives/
KhaozEngine.Ecs/   KhaozEngine.Serialization/   KhaozEngine.Content/   KhaozEngine.Content.Validator/
KhaozEngine.Diagnostics/   KhaozEngine.App/   KhaozEngine.Persistence/
KhaozEngine.Platform/   KhaozEngine.Collision/   KhaozEngine.Physics/   KhaozEngine.Physics.Bepu/
KhaozEngine.Terrain/   KhaozEngine.Navigation/   KhaozEngine.MapDoc/   KhaozEngine.TileWorld/   KhaozEngine.TileWorld.Editing/   KhaozEngine.Dungeon/   KhaozEngine.Updates/   KhaozEngine.Progression/   KhaozEngine.Items/
KhaozEngine.ServerStatus/   KhaozEngine.Http/
KhaozEngine.Identity/   KhaozEngine.Identity.Oidc/   KhaozEngine.Identity.Discord/   Opt-in OIDC/Discord backends (in no umbrella)
KhaozEngine.Commerce/   KhaozEngine.Commerce.Sqlite/   KhaozEngine.Commerce.SqlServer/
KhaozEngine.Social/   KhaozEngine.Social.Discord/   Opt-in Discord backend (in no umbrella)
KhaozEngine.Locomotion/   KhaozEngine.Objectives/   KhaozEngine.Stats/
KhaozEngine.Netcode/   KhaozEngine.Netcode.Abstractions/   KhaozEngine.Netcode.LiteNetLib/   KhaozEngine.Simulation/
KhaozEngine.Replication/   KhaozEngine.WorldStore/   KhaozEngine.WorldStore.Sqlite/   KhaozEngine.WorldStore.SqlServer/
KhaozEngine.Sharding/   KhaozEngine.NetWorld/   KhaozEngine.TileWorld.Netcode/
KhaozEngine.Server.Admin/      Opt-in HTTPS admin endpoint (Kestrel) over ServerAdmin
# Roslyn analyzers (netstandard2.0, flow to consumers through the umbrellas)
KhaozEngine.Localization.Analyzers/   KhaozEngine.CodeHealth.Analyzers/
# Umbrella metapackages
KhaozEngine.Foundation/   KhaozEngine.Game2D/   KhaozEngine.Game3D/   KhaozEngine.Server/
# Tests, samples, tools
KhaozEngine.<Area>.Tests/ (Foundation, Simulation, Server, Render, Gui, MapEditor, Game, Audio, Particles, Showcase)   KhaozEngine.Tests/ (rump, cross-cutting only)
KhaozEngine.TestSupport/   KhaozEngine.TestSupport.Services/   KhaozEngine.TestSupport.Gpu/   KhaozEngine.TestSupport.Windowing/   KhaozEngine.Showcase/ (menu hub: 2D, GUI, input, mini-game, 3D-world, networked-walk, map-editor rooms)
SnapshotTool/ (also GPU-free `diff`/`score` subcommands)   MmoServerSample/ (reference dedicated MMO server)
KhaozEngine.Updates.Tool/ (ke-updater)   KhaozEngine.Sfx.Tool/ (ke-sfxbake)   KhaozEngine.PropSurface.Tool/ (ke-propbake)
KhaozEngine.MapEdit.Tool/ (ke-mapedit)   KhaozEngine.TileEdit.Tool/ (ke-tileedit)
tools/   docs/USING-KHAOZENGINE.md
Directory.Build.props (shared version)   nuget.config   .github/workflows/ci.yml
```

CI builds, tests, packs, and on a `v*` tag publishes to GitHub Packages.

## Running the samples

Each sample is a runnable head (`dotnet run --project <name>`) proving one slice of the engine, no MonoGame.
The windowed ones open a GPU window (need a display); the server / snapshot heads are headless.

**Interactive (windowed)** - open a window, drive with keyboard + mouse:

| Sample | Demonstrates | Run | Controls |
|---|---|---|---|
| `KhaozEngine.Showcase` | **Tile-menu hub** of feature rooms, each wearing shared chrome (title band, controls-hint bar, live status line, toggle toasts): a tabbed **2D & GUI toolkit tour** (widgets, sprites + text, input gestures / clock / clipboard / audio, immediate-mode GuiSurface, and a screens-and-dialogs page launching the modal Settings dialog, pause overlay, patch notes, and toast stack), the "Catcher" mini-game, the turn-key **boot screen**, a **walkable streamed 3D overworld** (terrain streaming + follow camera + character controller + physics + a CC0-house village) with retro post-fx (cel / outline / palette), a **particles & VFX room** (the nine authored `VfxPresets` cycled live, with bloom / HDR toggles), an **in-app networked walk** (an authoritative server + a local client + scripted bots, all in-process over loopback UDP), a **generated dungeon walk**, and the **in-engine map editor** (`KhaozEngine.MapEditor` over a committed demo document) | `dotnet run --project KhaozEngine.Showcase` | Menu: arrows + Enter or click a tile, Esc leaves a room. 2D & GUI room: Tab/Shift+Tab switch tabs. 3D room: WASD move, right-drag orbit, scroll zoom, Shift run, F2 collision overlay, O/N/C/R/P post-fx. VFX room: Left/Right switch effect, Space replay, B/H bloom/HDR. Networked room: WASD + mouse to walk among the bots. Map editor room: WASD + right-drag fly, toolbar tools, Ctrl+Z/Y undo/redo, Ctrl+S save, Shift+Esc leaves it (Esc cancels a gesture, or opens the settings menu when there is none) |

**Headless (no window)**:

| Sample | What it does | Run |
|---|---|---|
| `SnapshotTool` | Writes a 2D + a 3D PNG via the snapshot harness; the canonical shape each game's `tools/SnapshotTool` mirrors (needs a GPU device, not a display). Also has GPU-free `diff`/`score` subcommands: `diff` compares two rendered PNGs, `score` compares a PNG against a committed golden grid, exit 0 pass / 1 fail / 2 usage or IO error | `dotnet run --project SnapshotTool -- /tmp/ke-snapshot-demo` |
| `MmoServerSample` | Reference dedicated MMO server (cell grid + seamless handoff) on a UDP socket | `dotnet run --project MmoServerSample` |

**Windowed smoke (CI / quick check).** Every windowed sample honors `KE_MAX_FRAMES=N`: render N frames, then
exit 0. So any of them doubles as a smoke test on a GPU box without needing someone to close the window:

```bash
KE_MAX_FRAMES=5 dotnet run --project KhaozEngine.Showcase
# to smoke a specific room's world build, auto-enter it (case-insensitive prefix of the room title):
KE_SHOWCASE_ROOM="3D" KE_MAX_FRAMES=5 dotnet run --project KhaozEngine.Showcase
```

**Field capture (measuring a backend).** The Showcase also carries the levers a windowed performance capture
needs. They are the testbed's own, not engine API, and each is a no-op unless set:

| Variable | What it does |
|---|---|
| `KE_TELEMETRY_PATH=<file>` | Arms the engine's `TelemetryRecorder` for the whole run: a JSON Lines session with the engine's header (build, backend, adapter, every `KE_` lever) and a raw sample row every 100 ms carrying frame rate, frame times, a loop frame count and the `GpuDeviceCounters` channels |
| `KE_SHOWCASE_FRAME_CAP=auto\|uncapped\|<hz>` | Pins the frame-cap intent. The default `auto` resolves to a SOFTWARE cap on Metal plus vsync, which sleeps the loop before the present can block, so an acquire-wait or present-pacing reading has to be taken `uncapped` |
| `KE_SHOWCASE_BACKGROUND_THROTTLE=off` | Disables the unfocused/minimized pacing. The engine default caps an unfocused window at 15 Hz, which is right for a game and ruinous for an unattended capture that never takes focus |

```bash
# a bounded capture of the 3D room on a named backend, paced by nothing but the present
KE_TELEMETRY_PATH=/tmp/ke-capture.jsonl KE_GRAPHICS_BACKEND=metal-native \
KE_SHOWCASE_ROOM=3D KE_SHOWCASE_FRAME_CAP=uncapped KE_SHOWCASE_BACKGROUND_THROTTLE=off \
KE_MAX_FRAMES=8000 dotnet run --project KhaozEngine.Showcase -c Release
```

## Dev tools

Author-time dotnet tools that ship as packages on the shared version line (not runtime dependencies):

- **`ke-sfxbake`** (`KhaozEngine.Sfx.Tool`) - manifest-driven bulk SFX generation + bake. Each game owns a
  `sfx.manifest.jsonc` describing its effects; the tool generates them via the ElevenLabs sound-effects API and
  encodes into the asset tree, skipping anything already up to date (`.sfxmeta` hash sidecars). Needs
  `ELEVENLABS_API_KEY`, plus an ffmpeg with libvorbis or `oggenc` (vorbis-tools) for OGG output.

  ```bash
  export ELEVENLABS_API_KEY=...           # already set on dev machines
  ke-sfxbake bake path/to/sfx.manifest.jsonc --dry-run   # plan + estimated credits, spends nothing
  ke-sfxbake bake path/to/sfx.manifest.jsonc             # generate new/changed, skip unchanged
  ke-sfxbake bake path/to/sfx.manifest.jsonc --force     # regenerate everything
  ```

  ```jsonc
  // sfx.manifest.jsonc - paths resolve relative to this file
  {
    "sounds": [
      { "key": "ui/confirm", "prompt": "crisp sci-fi UI confirm blip, short synth tail",
        "durationSeconds": 1.2, "out": "Assets/Sfx/ui/confirm.ogg" },          // mono OGG (default)
      { "key": "ui/click", "prompt": "soft latency click", "format": "wav",
        "out": "Assets/Sfx/ui/click.wav" },                                     // 16-bit PCM WAV for one-shots
    ],
  }
  ```

- **`ke-updater`** (`KhaozEngine.Updates.Tool`) - generate, sign, and verify update manifests (RSA-2048).

## Consumers

| Game | References | Status |
|---|---|---|
| **Hardpoint** (3D) | `KhaozEngine.Game3D` (head) + `KhaozEngine.Foundation` (logic) | Fully off MonoGame. |
| **Nullwake** (2D) | `KhaozEngine.Game2D` | Fully off MonoGame. Source of the widgets, transitions, and the click-through fix. |
| **SpaceGame** (2D) | `KhaozEngine.Game2D` (head) + foundation pins on `SpaceGame.Sim` | Fully off MonoGame. Deterministic lockstep sim split into `SpaceGame.Sim`. |
| **Ruinborne** (3D MMO) | `KhaozEngine.Game3D` + `NetWorld` (client) + `KhaozEngine.Server` + `WorldStore.SqlServer` (server) | Authoritative networked overworld, Azure SQL persistence. |

Each consumer pins its own engine version (its `Directory.Build.props` `<KhaozEngineVersion>`) and adopts new releases on its own schedule.
