# KhaozEngine docs index

Start here. Two kinds of doc live here and they have different lifecycles:

- **Living docs** (this directory's root) are kept current and edited in place. When two of them state the
  same fact, the source-of-truth column says which one wins.
- **Design docs** ([`design/`](design/)) are the rationale behind a program: the why, the alternatives
  weighed, the decisions taken. They are NOT a reference surface. Shipped API and usage move out to
  CHANGELOG / USING / the package READMEs as they land, and what stays behind is the reasoning. A design doc
  is written once, corrected in place while the program is in flight, and then kept as history.

## Living docs

| Doc | What it's for | Source of truth for |
|---|---|---|
| [README.md](../README.md) | Top-level overview: the package table (granular packages + umbrella metapackages), the one hard rule, quickstart wiring, repo layout. | The package list + what each package owns. Guard-enforced complete by `check-doc-versions.sh`, so never trust a prose package list anywhere else. |
| [USING-KHAOZENGINE.md](USING-KHAOZENGINE.md) | The consumer contract: hard rules, the data-flow model, per-layer API reference, headless-test patterns. Read before wiring a game in. | How a game must use the engine. |
| [CHANGELOG.md](../CHANGELOG.md) | Newest-first, every version; each entry leads with a one-line summary, so the file doubles as the scannable "story over time". | Release history (full detail + the high-level digest). The single source for the per-version story; nothing else restates it. |
| [CROSS-PLATFORM.md](CROSS-PLATFORM.md) | Desktop GPU story: platform → Veldrid backend mapping (Metal/D3D11/Vulkan), the golden-snapshot net, the CI matrix. | How rendering is verified per OS. |
| [SECURITY-BASELINE.md](SECURITY-BASELINE.md) | The engine-wide security posture: threat model (where untrusted bytes enter), layered defenses (managed memory-safety, input validation, patched deps, signed updates, the CETCompat tradeoff), what's out of scope, and per-game vs engine responsibilities. | The security posture every game inherits. |
| [UPDATER.md](UPDATER.md) | How a consuming game wires up the signed auto-updater (`KhaozEngine.Updates`): why Azure Blob over GitHub Releases, key generation + the embedded public key, the signed manifest + feed layout, the OIDC/Key-Vault publish CI flow, the apply/rollback shim, and the macOS `.app` re-sign caveat. | How a game consumes the updater. Per-game specifics live in each game's own `docs/UPDATER.md`. |
| [RENDER-PIPELINE.md](RENDER-PIPELINE.md) | High-level (container-level) Mermaid map of how a C# draw call becomes rendered triangles: Render2D/Render3D -> the Gpu seam -> Veldrid -> backend -> GPU, plus the geometry-upload and GLSL->SPIR-V side-flows. | Understanding the render path. |
| [PHYSICS-PIPELINE.md](PHYSICS-PIPELINE.md) | High-level (container-level) Mermaid map of how a character move becomes a collision-resolved position: Locomotion -> the `IPhysicsWorld` seam -> Physics.Bepu -> BepuPhysics, plus the shape-bake and authoritative-vs-predicted side-flows. | Understanding the physics path. |
| [DEPENDENCY-SEAMS.md](DEPENDENCY-SEAMS.md) | The convention RENDER-PIPELINE and PHYSICS-PIPELINE are instances of: how every third-party library (GPU, physics, netcode, persistence, audio, windowing) is wrapped behind a dependency-free seam with an opt-in backend, and how to add a new backend. | The seam + swappable-backend pattern. |
| `../<Package>/README.md` | One-paragraph purpose + a snippet per package. Ships inside the nupkg via `PackageReadmeFile`, so it is what a NuGet consumer reads. | Per-package quick reference. |

The **engine current version** lives in `../Directory.Build.props` (`<KhaozEngineVersion>`). Docs that restate it
(the README and USING-KHAOZENGINE PackageReference examples) are guarded by
[`../scripts/check-doc-versions.sh`](../scripts/check-doc-versions.sh), which CI runs on every push. That script
also checks the newest `CHANGELOG.md` heading is for the current version, and enforces the package inventory:
every packable package must have a README catalog row and ship its own `<Package>/README.md`. Consumer *pins*
are allowed to lag and are not checked.

The **backlog and roadmap** are GitHub Issues now (`kind/backlog` / `kind/roadmap`), searchable via
[`../scripts/ledger.sh`](../scripts/ledger.sh). The old `docs/ROADMAP.md` / `docs/TODO.md` files were migrated
there on 2026-07-17 and deleted.

## Design docs ([`design/`](design/))

Rationale, not reference. Status is against the engine version line, and a "complete" doc is kept as history,
not deleted: the reasoning behind a shipped decision is the thing that is expensive to reconstruct later.

| Doc | The program | Status |
|---|---|---|
| [MAP-EDITOR-DESIGN.md](design/MAP-EDITOR-DESIGN.md) | The MapDoc zone-document format, the MapEditor GUI runtime + per-game editor heads, the `ke-mapedit` MCP tool, phasing, and the Ruinborne adoption path. | **In flight.** Phases A-D shipped 10.44.0-10.67.0 and twelve further rounds through 14.15.0. Open work is tracked in GitHub Issues (`kind/backlog` / `kind/roadmap`), not here. |
| [DUNGEON-GENERATOR-DESIGN.md](design/DUNGEON-GENERATOR-DESIGN.md) | `KhaozEngine.Dungeon`: deterministic grow-and-embed layout, completability by construction + an always-on solver, MapDoc bake + runtime stamp sinks, the greybox kit contract, the `ke-dungeon` CLI / MCP verbs. | **Complete.** Shipped 10.56.0-10.58.0, polish to ~10.109.0, `DungeonNav.Bake` at 10.123.0. Deferred follow-ups are tracked in GitHub Issues (`kind/backlog`). |
| [NPC-NAVIGATION-DESIGN.md](design/NPC-NAVIGATION-DESIGN.md) | `KhaozEngine.Navigation`: the founding grid-vs-navmesh trade-off, the clearance grid, and the statics-only call. | **Complete.** Shipped 10.123.0. |
| [NAV-STEP-SURFACES-DESIGN.md](design/NAV-STEP-SURFACES-DESIGN.md) | The follow-on to 10.123.0: walkable step surfaces, and the `INavSurfaceProvider` dependency-inversion decision. | **Complete.** Shipped 11.2.0. |
| [NAV-HOP-LINKS-DESIGN.md](design/NAV-HOP-LINKS-DESIGN.md) | The follow-on to 11.2.0: vertical hop links, the generation rule, and the admissibility derivation. | **Complete.** Shipped 11.8.0. |
| [NAV-LAYERED-SURFACES-DESIGN.md](design/NAV-LAYERED-SURFACES-DESIGN.md) | Phase 2 of vertical worlds: the layered many-surfaces-per-column extraction bake, cross-layer link generation, and the no-adjacency layer assignment that deletes the phase-1 rim erosion. | **Complete.** Shipped 14.11.0. |
| [TERRAIN-SCULPT-LAYER-DESIGN.md](design/TERRAIN-SCULPT-LAYER-DESIGN.md) | The terrainOverrides sculpt-delta layer (map format v2): authored height deltas composited inside TerrainField, editor brushes, sculpt MCP verbs. | **Complete.** Approved 2026-07-23. T1 (field/format) shipped 14.13.0, T2 (editor brushes) 14.14.0, T3 (MCP verbs) 14.15.0, closing #271. Ruinborne adoption (repin + first valley sculpt pass) is a separate game-side follow-up (https://github.com/APKiwiOrg/Ruinborne/issues/169). |
| [LOD-HLOD-DESIGN.md](design/LOD-HLOD-DESIGN.md) | Far-field terrain LOD tiers and decor ring, prop dissolve fades and LOD meshes, merged-coarse-mesh HLOD clusters: the visible-far-world arc. | **Complete.** L1 (14.16.0), L2 (14.17.0), L3 (14.18.0) all shipped (#276). |
| [PARTICLES-VFX-DESIGN-2026-07-16.md](design/PARTICLES-VFX-DESIGN-2026-07-16.md) | The particles/VFX modernization: the three-package split (`KhaozEngine.Particles` sim, Render3D's modern particle pass, the `Particles.Render3D` adapter + presets), the additive `EmitterConfig` evolution, procedural SDF shapes, the soft-depth instanced pass. | **Complete.** Shipped 10.126.0. |
| [AAA-VFX-TIER1-DESIGN-2026-07-16.md](design/AAA-VFX-TIER1-DESIGN-2026-07-16.md) | AAA VFX Tier 1: the HDR float16 chain + ACES filmic tonemap + pre-tonemap bloom, flipbook particles with motion-vector blending, and the screen-space distortion pass. | **Complete.** Shipped 10.128.0 / 10.129.0 / 10.130.0, plus ChromaPreservation at 11.7.0. Tiers 2/3 are tracked in GitHub Issues (`kind/roadmap`) with no design doc yet. |
| [BACKGROUND-PASS-VOID-DECALS-DESIGN-2026-07-17.md](design/BACKGROUND-PASS-VOID-DECALS-DESIGN-2026-07-17.md) | Moving the procedural starfield out of the final blit into a real background pass (plus the `BackgroundMode` enum folding `Post.Starfield` / `Sky.Enabled`), then the opt-in `GroundDecal.VoidFallback` virtual-plane projection it unblocks. | **Complete.** Shipped 11.9.0 (Release 1) and 12.1.0 (Release 2). Both halves of the original rule were corrected in place during implementation; the doc records the corrected reasoning. |
| [CI-SELECTIVE-TESTS-DESIGN-2026-07-18.md](design/CI-SELECTIVE-TESTS-DESIGN-2026-07-18.md) | Splitting the `KhaozEngine.Tests` monolith into per-area test projects aligned to the package layering, and the `dotnet-affected` selective path in ci.yml (pushes/PRs build+test only affected projects, tags and the weekly sweep stay full). | **Complete.** Shipped 13.0.3. Selection-precision follow-ups resolved: [#211](https://github.com/APKiwiOrg/KhaozEngine/issues/211) (over-selection, Showcase tests split to their own project) and [#212](https://github.com/APKiwiOrg/KhaozEngine/issues/212) (under-selection, csproj change adds the architecture-test rump). |
| [RECONNECT-SCREEN-DESIGN-2026-07-18.md](design/RECONNECT-SCREEN-DESIGN-2026-07-18.md) | `KhaozEngine.Gui`'s connection-outage primitive: `ConnectionStatusController`'s banner-then-screen escalation policy (with the anti-flicker floor), why both halves live in `Gui` rather than `Game`, and the `ReconnectScreenTheme`/`LocalizedText` decisions. | **Complete.** Shipped 13.2.0. |
| [SAVE-VALIDATION-DESIGN-2026-07-18.md](design/SAVE-VALIDATION-DESIGN-2026-07-18.md) | The save validation/management pass: the versioned v2 save envelope (tamper-protected metadata), the strict-with-recovery tamper posture + backup-generation ladder, default-on encoding, fsync + rotation in the write path, and `WorldPersistence`'s validate-and-quarantine hooks. | **In flight.** Program issue [#224](https://github.com/APKiwiOrg/KhaozEngine/issues/224). Design approved, implementation pending. |
| [SCREEN-COMPONENT-DESIGN-2026-07-18.md](design/SCREEN-COMPONENT-DESIGN-2026-07-18.md) | `IScreenComponent` + `ScreenComponentList`, the composition unit below `Screen`: why an interface rather than a base class, why `bounds` is per-call, why `Screen` was not modified, why font/white are not on `Draw`, and why the three Screen+View pairs [#226](https://github.com/APKiwiOrg/KhaozEngine/issues/226) names were NOT migrated (one collaborator each). | **Complete.** Shipped 13.7.0. Consumer adoption is [SpaceGame#69](https://github.com/APKiwiOrg/SpaceGame/issues/69). |
| [GUI-TEXT-SCALE-DESIGN-2026-07-19.md](design/GUI-TEXT-SCALE-DESIGN-2026-07-19.md) | The text-scale family for the three retained widgets that draw text: `Button.LabelScale`, `TabBar.TextScale`, per-line `TooltipLine.Scale` plus `Tooltip.TitleScale`. Corrects two sub-claims in [#232](https://github.com/APKiwiOrg/KhaozEngine/issues/232) (`Toggle`/`Slider` render no text at all, and `TabBar` did not already scale its text), and records the deferred `Dropdown`/`TextInput`/`TreeView`/`ProgressBar` gap plus the `Tooltip` parity audit against SpaceGame's `TooltipRenderer`. | **Complete.** Shipped 14.1.0, closing [#232](https://github.com/APKiwiOrg/KhaozEngine/issues/232) (Button/TabBar half, the Toggle/Slider half closed as not-planned) and [#237](https://github.com/APKiwiOrg/KhaozEngine/issues/237). The doc's own section 7 called this a coordination artifact needing no INDEX row by default, listed here since the file already landed in `docs/design/` on this branch, per the maintainer-disagrees path the doc itself names. |
| [INSTANCED-DISSOLVE-DESIGN-2026-07-20.md](design/INSTANCED-DISSOLVE-DESIGN-2026-07-20.md) | Per-instance dissolve on the rigid/instanced path: dissolve (not opacity) semantics with a zero default, folding the noise discard + emissive edge into `ModelFrag` gated at 0 rather than reusing `ModelDissolveFrag`, a trailing `InstanceData.Dissolve` field (not `SpecParams.z/w`, which the MASK cutout owns), and the `Scene3D.Draw` overload mirroring the skinned one. | **Complete.** Shipped 14.5.0, closing [#253](https://github.com/APKiwiOrg/KhaozEngine/issues/253). Consumers [#44](https://github.com/APKiwiOrg/KhaozEngine/issues/44) (prop fade band) and [#37](https://github.com/APKiwiOrg/KhaozEngine/issues/37) (AoI presentation fade) build their policy on this primitive. |
| [FILESIZE-ANALYZER-DESIGN-2026-07-20.md](design/FILESIZE-ANALYZER-DESIGN-2026-07-20.md) | File-size ratchet as a compile-time Roslyn analyzer (KESIZE001/002), fleet rollout. The "14.8.0 revision" section covers the three things the original design got wrong: remediation written into the descriptor `description` that MSBuild never prints (so the analyzer pushed agents toward the arbitrary split it warned against), a "visible in review" blessing premise that does not hold when an agent authors and merges its own diff, and the ratchet being a category error for files whose size is content rather than structure (hence the `exempt` line form). | **Shipped (14.6.0), revised (14.8.0, 14.8.1).** Program issue [#254](https://github.com/APKiwiOrg/KhaozEngine/issues/254). The 14.8.1 section is the one to read if you are deciding whether to exempt a file: the rule's own canonical example turned out to fail its own test, so the criterion is now growth-over-time rather than what the file looks like. |
| [PARTICLE-ATTRACTOR-DESIGN-2026-07-20.md](design/PARTICLE-ATTRACTOR-DESIGN-2026-07-20.md) | `ParticleAttractor`: system-level state over per-particle tagging, one player-level attractor forwarded to every phase pool, `IgnoreAttractor` as a bool opt-out rather than a graduated influence weight, why the pull appends after turbulence and absorb checks post-integration, and why emission-rate modulation is two knobs (`RateCurve` authored, `RateScale` runtime) for two different drivers. | **Complete.** Shipped 14.7.0, closing [#255](https://github.com/APKiwiOrg/KhaozEngine/issues/255). Second half of the Ruinborne death pass. Consumer is [Ruinborne#120](https://github.com/APKiwiOrg/Ruinborne/issues/120). |
| [GPU-COMPUTE-DESIGN-2026-07-26.md](design/GPU-COMPUTE-DESIGN-2026-07-26.md) | Compute shaders in the `KhaozEngine.Gpu` seam: separate compute handle types so a compute pipeline cannot be bound for a draw, why the workgroup size is parsed out of the SPIR-V module instead of restated by the caller (the description field is read by Metal only and validated by nothing, so a mismatch is invisible on two backends and silently wrong on the third), why structured buffers are always raw views on Direct3D11, and the two ordering patterns the seam can actually guarantee given the backend layer has no barrier API. | **Complete.** Shipped 15.2.0, closing [#309](https://github.com/APKiwiOrg/KhaozEngine/issues/309). Program 1 of two: the consumer is the FFT ocean, [#310](https://github.com/APKiwiOrg/KhaozEngine/issues/310), which the Stockham FFT proof test is deliberately the seed pattern for. The "Ordering" section is the one to read: a read-after-write chain of dispatches costs a submit + drain per stage, a ceiling not fixable from this layer and tracked as [#311](https://github.com/APKiwiOrg/KhaozEngine/issues/311). |
| [FFT-OCEAN-DESIGN-2026-07-26.md](design/FFT-OCEAN-DESIGN-2026-07-26.md) | The 16.3.0 Tessendorf FFT ocean: why FFT after it was declined at 14.24.0 (three de-tiling rounds each removed one coherent structure and the eye found the next, which is the signature of a finite sum rather than of a component count), why the stall budget forced one dispatch per AXIS with the spectrum evolution and the map assembly fused into them, in-place decimation-in-time over Stockham because Stockham's second buffer wants the whole shared-memory minimum at 256, four Hermitian-packed complex fields carrying eight real ones so normals and the Jacobian are analytic, a CPU spectrum bake for testability and a single copy of the maths, cascade bands that partition wave-number space, and the three Metal-only binding landmines it walked into. | **Complete.** Shipped 16.3.0, closing [#310](https://github.com/APKiwiOrg/KhaozEngine/issues/310). Program 2 of two, built on the 15.2.0 compute seam ([GPU-COMPUTE-DESIGN](design/GPU-COMPUTE-DESIGN-2026-07-26.md)). The "One map array, bound first" section is the one to read: on Metal a resource binding mistake does not fail, it reads zero, and a wave simulation that reads zero looks like a calm sea. Water height query ([#297](https://github.com/APKiwiOrg/KhaozEngine/issues/297)), per-body sea states ([#275](https://github.com/APKiwiOrg/KhaozEngine/issues/275)) and the projected grid ([#296](https://github.com/APKiwiOrg/KhaozEngine/issues/296)) are all deferred in the doc's last section. |
| [WATER-SAMPLING-FRAME-DESIGN-2026-07-26.md](design/WATER-SAMPLING-FRAME-DESIGN-2026-07-26.md) | The 16.5.0 FFT-ocean sampling frame: onshore wave focusing, per-cascade lattice decorrelation and a large-scale domain warp, none of which touch the spectrum, the kernels or the maps. Records why the obvious focus implementation (rotate the sampling coordinate per position) is degenerate rather than merely mistuned, what replaced it (a two-tap blend over a ring of fixed lattice rotations, L2-weighted because the maps are Gaussian fields), why the vertex stage owns the frame and hands it down, why a partial focus carries an unremovable seam, and why the domain warp has to be sized at 40 to 60 per cent of the cascade tile to do anything. | **Complete.** Shipped 16.5.0, closing [#328](https://github.com/APKiwiOrg/KhaozEngine/issues/328). Sits on the 16.3.0 ocean ([FFT-OCEAN-DESIGN](design/FFT-OCEAN-DESIGN-2026-07-26.md)). The "load-bearing decision" section is the one to read: a rotation field that turns to face a point winds once around it, so its angle cancels the sample's own azimuth and the whole plane collapses onto one ray of the map - the shader was short, every term in it was right, and the thing being asked for did not exist. |
| [WATER-CLIPMAP-DESIGN-2026-07-27.md](design/WATER-CLIPMAP-DESIGN-2026-07-27.md) | The 16.12.0 world-locked water grid and mipped cascade maps: why the camera-locked grid resampled the sea every frame (measured at ~85 per cent of the field's own motion for a 0.10 m camera step), why centre-snapping it cannot work under `GridFocusBias` (a power warp has no common quantum), why each ring snaps to TWICE its own cell size (which is what makes the nesting exact and every snap an even-cell shift that maps the lattice onto itself), why ring seams are closed by two-tap stitch vertices rather than skirts (the pass is alpha-blended) or collapsed quads, and why a compute-written map that needs mips has to be two textures. | **Complete.** Shipped 16.12.0, closing [#296](https://github.com/APKiwiOrg/KhaozEngine/issues/296) and [#344](https://github.com/APKiwiOrg/KhaozEngine/issues/344). Sits on the 16.3.0 ocean ([FFT-OCEAN-DESIGN](design/FFT-OCEAN-DESIGN-2026-07-26.md)) and composes with the 16.5.0 sampling frame ([WATER-SAMPLING-FRAME-DESIGN](design/WATER-SAMPLING-FRAME-DESIGN-2026-07-26.md)). The "snap is to twice the cell size" section is the one to read: it is one factor of two, and without it the finer ring's boundary lands halfway through a coarse cell and no amount of stitching closes the seam. The remaining ring-boundary LOD swap ([#348](https://github.com/APKiwiOrg/KhaozEngine/issues/348)) was closed by 17.3.0's geomorph ([WATER-SHORE-DESIGN](design/WATER-SHORE-DESIGN-2026-07-27.md)). |
| [WATER-SHORE-DESIGN-2026-07-27.md](design/WATER-SHORE-DESIGN-2026-07-27.md) | The 17.3.0 depth-aware water surface: why the bathymetry seam is a CPU depth array rather than a texture, a scene-depth read or an engine-side terrain bake; why the shoaling factor is the textbook one read BACKWARDS (`tanh(kd)`, because a game wants the surface to settle down to meet the beach rather than pile up against it); why the taper's `k` has to be the cascade's energy-weighted mean rather than its band midpoint; why the breaker criterion measures the UNSHOALED height (feeding the shoaled one back in is degenerate); why the FFT foam accumulator cannot carry a world-anchored surge; and why the clipmap geomorph's optimum band is structural rather than a taste call. | **Complete.** Shipped 17.3.0, closing [#355](https://github.com/APKiwiOrg/KhaozEngine/issues/355) and [#348](https://github.com/APKiwiOrg/KhaozEngine/issues/348); the amplitude half of [#330](https://github.com/APKiwiOrg/KhaozEngine/issues/330), which stays open for refraction. Sits on the 16.3.0 ocean ([FFT-OCEAN-DESIGN](design/FFT-OCEAN-DESIGN-2026-07-26.md)) and the 16.12.0 clipmap ([WATER-CLIPMAP-DESIGN](design/WATER-CLIPMAP-DESIGN-2026-07-27.md)). The "two things the first implementation got wrong" section is the one to read: both were found by looking at a probe render and neither would have failed a test, because a feature that draws a faint pale line where surf should be is still drawing something. |
| [WATER-STYLIZED-OCEAN-DESIGN-2026-07-25.md](design/WATER-STYLIZED-OCEAN-DESIGN-2026-07-25.md) | The 14.24.0 water rework: why Gerstner rather than a height field or FFT, why the swell is parameterized by wind instead of a wave table, the fixed-budget camera-focused surface grid and the mesh-swim it trades for, reusing the sky's own evaluation per-direction for the reflection (and why the reflected sun is scaled down against the glint lobe), peak-normalized GGX with footprint-driven roughness widening, per-channel Beer-Lambert grading over a two-stop lerp, and the Jacobian whitecap driver. | **Complete, revised.** Shipped 14.24.0, closing [#295](https://github.com/APKiwiOrg/KhaozEngine/issues/295) and [#292](https://github.com/APKiwiOrg/KhaozEngine/issues/292). Follows the 14.22.0 de-tiling work ([#278](https://github.com/APKiwiOrg/KhaozEngine/issues/278)). The "14.26.0 revision" section is the one to read on ripple shading: it records that the three-cosine field was non-tiling but still a ruled pattern, and that 14.24.0's band-limiting reached the specular lobe but never the normal field. Refraction, SSR, caustics and the submerged view are still deferred in the doc's own last section. |
| [AIRBORNE-MOMENTUM-DESIGN-2026-07-26.md](design/AIRBORNE-MOMENTUM-DESIGN-2026-07-26.md) | Opt-in airborne horizontal momentum in `CharacterMovement`: why the knob is off by default and why the carried velocity is written on every tick but read only when it is on (so "unchanged at the default" is structural rather than behavioural), the per-tick steer/conserve/brake resolve and why the brake is gated on a strictly slower command, why collision CLIPS the carried velocity and can never inject into it, why takeoff and landing need no special case, and why the anti-cheat's scalar `CommandedSpeed` export had to become a vector. | **Complete.** Shipped 16.0.0, closing [#320](https://github.com/APKiwiOrg/KhaozEngine/issues/320). Consumer origin is [Ruinborne#203](https://github.com/APKiwiOrg/Ruinborne/issues/203) and [Ruinborne#225](https://github.com/APKiwiOrg/Ruinborne/issues/225). Two sections earn the read: "The anti-cheat consequence" explains the public API break, and "Deferred" records the one case the undenied-clip tolerance does not cover (a slow arc beyond about 1.7 km from the origin still ratchets down, measured, unfixed because the real fix threads the achieved delta out of the collision resolve rather than tuning a constant). |
| [CELL-EVICTION-DESIGN-2026-07-27.md](design/CELL-EVICTION-DESIGN-2026-07-27.md) | Persist-then-evict for shard cells: why the persist gate spans frames and re-verifies before removing, why a recreated coordinate restores synchronously out of a bounded in-memory cache (the handoff destination is populated inside the create call, before it can tick or adopt), why exactly one restore path is armed per evicted coordinate, why eviction persists precisely what a restart persists, why `ICellLink.HasPending` defaults to true, and the reused-tick-buffer bug only eviction could surface. | **Complete.** Engine side of the Ruinborne 100 km world program, closing [#336](https://github.com/APKiwiOrg/KhaozEngine/issues/336). The "Recreation restores synchronously" section is the one to read: the asynchronous restore path was correct for a cold cell and wrong for a re-entered one, because `ProcessHandoffs` creates the destination and adopts into it in the same pass. |
| [TILED-MAPDOC-AND-RESIDENCY-DESIGN-2026-07-27.md](design/TILED-MAPDOC-AND-RESIDENCY-DESIGN-2026-07-27.md) | The tiled map document format and document residency, jointly because the tile granularity is shared: manifest plus one content-addressed file per 512 m document tile with point content bucketed and shapes kept global, a streamed writer that never materializes a string and commits through a single manifest rename, per-tile canonical hashing composed into a world identity without serializing anything, the Chebyshev `MapTileResidency` ring both heads drive, and the streamer-core extraction out of `Terrain.Render3D` into `Terrain`. | **Both releases shipped, program complete: release 1 (format) in 16.7.0, closing [#334](https://github.com/APKiwiOrg/KhaozEngine/issues/334); release 2 (residency) in 16.10.0, closing [#335](https://github.com/APKiwiOrg/KhaozEngine/issues/335).** Revised after an adversarial review, for [Ruinborne#242](https://github.com/APKiwiOrg/Ruinborne/issues/242). Three areas were redesigned rather than amended, and each records what was wrong: section 7's save ordering left the old manifest indexing new bytes after a crash, section 11's ring was declared Euclidean where an integer Euclidean ring of radius 1 guarantees ZERO coverage, and section 10's editor had two paths that destroyed a tiled world on a save that reported success. Section 1 is still the one to read for the why: the save ceiling is `int.MaxValue` elements in one contiguous buffer, confirmed by byte arithmetic against the real `island.map.json`, because `WriteIndented` puts each of a sculpt tile's 1,024 deltas on its own line. Server-side residency holes are deferred to [#341](https://github.com/APKiwiOrg/KhaozEngine/issues/341). |
| [FLOATING-ORIGIN-DESIGN-2026-07-27.md](design/FLOATING-ORIGIN-DESIGN-2026-07-27.md) | Keeping rendering, physics and movement precise at 100 km: why the frame anchor belongs to a SIMULATION ISLAND (one `World` plus one `IPhysicsWorld`) rather than to an entity, an observer or a `CellCoord`, with the entity carrying only a replicated stamp of it, why a single global origin shift is structurally impossible on a shard server, the 128 m grid derived from a binade divergence model, the round-to-nearest anchoring that makes a re-anchor bit-exact, the Bepu bulk pose rebase verified by probe, the render subtraction landing on the GPU-bound copy so every CPU-side spatial path stays absolute and byte-identical, and the three-release split. | **COMPLETE. All three releases shipped: 1 in 16.8.0 (camera-relative rendering), 2 in 16.11.0 (the chunk-local terrain bake, the `IPhysicsWorld` rebase API, and a flat-head island frame behind `WorldServerConfig.FrameAnchoring`), 3 in 17.0.0 (the MAJOR: the frame-relative wire, a read-only `ReplicatedPosition.Value`, per-cell islands with per-cell physics worlds on the sharded head, the client's adopted frame, and [#197](https://github.com/APKiwiOrg/KhaozEngine/issues/197)'s FP scope around the fan-out). The program is complete.** Revised once after an adversarial review, then verified line by line against the cited code and amended to close every finding. Roadmap issue [#337](https://github.com/APKiwiOrg/KhaozEngine/issues/337) (closed by 17.0.0), consumer program [Ruinborne#242](https://github.com/APKiwiOrg/Ruinborne/issues/242) sub-project 5. Read section 0 first: the first draft chose a per-ENTITY anchor, which collides with the single `IPhysicsWorld` the sharded head shares across all cells, so section 3 is re-scored against a new criterion (physics-space coherence) and the model changed, and the verification pass then moved the render subtraction point and cut release 2's server flag back to the flat head. Then section 14, which lists the premises from #337 AND from this doc's own two drafts that turned out wrong when checked, including the one the survey misses entirely (terrain vertices are baked at absolute world coordinates, so neither camera-relative rendering nor a physics rebase can fix them). Section 12 coordinates with the concurrent `TILED-MAPDOC-AND-RESIDENCY-DESIGN-2026-07-27.md` program (branch `feature/tiled-mapdoc`, not merged yet): its streamer extraction has landed there as `9b54be00` (sixteen forwarded types) and merges first, and this program's terrain work rebases onto it. |
| [EDITOR-SETTINGS-MENU-DESIGN-2026-07-27.md](design/EDITOR-SETTINGS-MENU-DESIGN-2026-07-27.md) | The map editor's Esc settings menu, default Day sky, and the horizon artifact: why the jagged black band is the far plane cutting displaced water against a background the water's own horizon blend never matches (`WaterRenderer` reads the sky palette unconditionally while the sky is never painted), why the fix is enabling the existing sky rather than new machinery, coherent `RenderDistanceProfile.Scaled` (chunk-quantized, invariant-preserving) instead of a blind multiply, the `DriveEnvironment` seam that lets the editor own `Post` with a host opt-out, and presets (sky/lighting, ocean) living in `Render3D` for reuse. | **Complete.** Shipped 17.6.0, closing [#364](https://github.com/APKiwiOrg/KhaozEngine/issues/364). Handles the [#363](https://github.com/APKiwiOrg/KhaozEngine/issues/363) window/decor coupling at the point of live rescale. The "What shipped" section is the one to read: the dialog is a `PropertyGrid` rather than the `PopupPanel` the plan assumed (the popup has no interactive row type, and this is ten editable rows), `EditorSettings` is a mutable class rather than a record because the menu and the environment apply share one instance and JSON round-trips it, and the surf toggle shipped instead of being deferred, since the depth field comes off data the viewport already streams. |
| [WATER-PER-PLANE-LOOK-DESIGN-2026-07-27.md](design/WATER-PER-PLANE-LOOK-DESIGN-2026-07-27.md) | Per-plane water look overrides: `WaterLook`, a 33-field nullable override a `WaterPlane` may carry so a calm inland lake and a rough FFT sea can share a frame, why almost none of it needs new machinery (each plane already owns its own UBO slot, the FFT branch is already a uniform gate), the cut between what is per-plane (the look) and what stays scene-wide (anything backing a once-per-frame GPU resource or the pass's geometry), and the fix this forces on `OceanFftProducer` so the shared ocean bakes on demand across the queued planes instead of off the scene's own default. | **Complete.** Shipped 17.7.0, closing [#370](https://github.com/APKiwiOrg/KhaozEngine/issues/370). Sits on the 16.3.0 FFT ocean ([FFT-OCEAN-DESIGN](design/FFT-OCEAN-DESIGN-2026-07-26.md)) and the 17.3.0 shore work ([WATER-SHORE-DESIGN](design/WATER-SHORE-DESIGN-2026-07-27.md)). Consumer origin is [Ruinborne#293](https://github.com/APKiwiOrg/Ruinborne/issues/293). The "Why a per-plane sea state is refused rather than deferred" section is the one to read: the naive per-body `SeaState` looks like it would work, and does not, because the producer keeps exactly one bake key and a persistent foam accumulator that only tolerates one invocation per texel. |
| [GUI-DRAG-AND-DROP-DESIGN-2026-07-28.md](design/GUI-DRAG-AND-DROP-DESIGN-2026-07-28.md) | The cross-widget drag-and-drop primitive: `GuiDragContext` as a standalone session object rather than widget-base-class members (a drag is the state BETWEEN two widgets, and the Gui widgets are independent sealed classes with no base to hang it off), a refusal shown before the release rather than an accept-then-undo, the fly-home return animation as a pure cosmetic tail, why `TreeView`'s same-widget reorder is deliberately NOT refactored onto it, and why the right-click bounds helper shipped in the same release. | **Complete.** Shipped 17.9.0, closing [#315](https://github.com/APKiwiOrg/KhaozEngine/issues/315). Consumers are [Ruinborne#295](https://github.com/APKiwiOrg/Ruinborne/issues/295) (drag a stack out of the bag to destroy it, the load-bearing one), [Ruinborne#262](https://github.com/APKiwiOrg/Ruinborne/issues/262) and [Ruinborne#263](https://github.com/APKiwiOrg/Ruinborne/issues/263). Decision 3 is the one to read before "unifying" the two drags: same-widget ordinal reorder is `TreeView`, cross-widget payload transfer is `GuiDragContext`, and folding either into the other loses something real. |
| [PHYSICS-LOCOMOTION-DESIGN-2026-08-02.md](design/PHYSICS-LOCOMOTION-DESIGN-2026-08-02.md) | The movement-model decision for [#371](https://github.com/APKiwiOrg/KhaozEngine/issues/371): movement is already physics-query-driven, the reconcile replay (256 pending commands against a pure-function stepper) rules out a rigid-body player outright, and the direction is a contact-classification kinematic controller reached by phases. Phase 1 specifies the direction-aware slope gate ([#369](https://github.com/APKiwiOrg/KhaozEngine/issues/369), descent falls through to gravity while ascent stays gated), the `LandingImpactSpeed` one-tick latch plus `OnAfterTick` server seam for fall damage, and authoritative `FacingYaw` (command flag, carried state, wire generation 10) so a character can face the camera on both heads. The 2026-08-02 addendum ([#442](https://github.com/APKiwiOrg/KhaozEngine/issues/442), 17.28.0) retires that gate two playtests later and replaces it with slope sliding plus a wall slide, an early slice of phase 2's contact semantics on the analytic path, and its round-three correction ([#468](https://github.com/APKiwiOrg/KhaozEngine/issues/468), 17.29.0) adds the invariant a measured climber-bot repro found missing: a tick without footing may never end higher than its own resolved vertical motion allows, so altitude on steep ground comes from velocity and never from the ground clamp. Round four (same issue and release) is the architectural one a 360-heading sweep forced: the resolve and the ground clamp must read the SAME surface, so a no-footing tick takes its geometry from the height field and the normal delegate is left to classify. Round five ([#475](https://github.com/APKiwiOrg/KhaozEngine/issues/475), 17.30.0) softens the gate boundary itself with a traction hysteresis band and a slide friction ramp, and the 2026-08-03 addendum ([#479](https://github.com/APKiwiOrg/KhaozEngine/issues/479), 17.30.0) is phase 1's own consequence: once `FaceCamera` pins the body to the camera the character has a front, so movement direction relative to it can cost speed, classified by sector off the command axis with the boundary rays deliberately owned by the closed forward and reverse wedges. | **In flight.** Phase 1 shipped, and its slope gate was superseded by the sliding addendum in 17.28.0, corrected twice in 17.29.0. Phase 2 (contact classification, opt-in with parity fixtures) and phase 3 (kinematic character mirror bodies, with [#31](https://github.com/APKiwiOrg/KhaozEngine/issues/31)) are follow-on roadmap work. Consumer origin is the 2026-08-02 physics ask plus [Ruinborne#292](https://github.com/APKiwiOrg/Ruinborne/issues/292). |
| [FLIPBOOK-ATLAS-SAMPLING-DESIGN-2026-07-28.md](design/FLIPBOOK-ATLAS-SAMPLING-DESIGN-2026-07-28.md) | Two independent defects that made a particle flipbook sample its neighbouring atlas cells on Direct3D11 and not on Metal: why a per-instance constant riding an unqualified varying is not bit-exact through a perspective-correct interpolator and why `FlipV` puts the packed grid in the binade where one ULP is a whole column, and why the mip chain over a grid of independent frames stops being usable long before it reaches 1x1. Carries the weighted scoring of the four mip fixes and why the shader deriving its own LOD cap beat the upload-side grid cap the consumer scoping preferred. | **Complete.** Shipped 17.12.0, closing [#389](https://github.com/APKiwiOrg/KhaozEngine/issues/389) and [#390](https://github.com/APKiwiOrg/KhaozEngine/issues/390), for [Ruinborne#365](https://github.com/APKiwiOrg/Ruinborne/issues/365). Decision 2 is the one to read before "simplifying" the clamp away: the shader can derive the cap from the packed grid and its own `textureSize`, which is what makes the consumer's adoption a pin bump rather than a code change. The implementation then hit the Metal binding-order trap ([#323](https://github.com/APKiwiOrg/KhaozEngine/issues/323)) from a `textureSize` rather than a sample, so each sheet caps off its own size in binding order and the LOD helper deliberately touches no texture. |
| [D3D11-NATIVE-BACKEND-DESIGN-2026-08-02.md](design/D3D11-NATIVE-BACKEND-DESIGN-2026-08-02.md) | The `KhaozEngine.Gpu.D3D11` native backend behind `IGpuDevice`: why recording goes into an engine-owned CPU command stream replayed at `Submit` rather than straight onto the immediate context (it eliminates the whole #423 and #415 hazard class structurally AND is what makes a constant-buffer ring legal to keep mapped across the record phase, which is worth roughly a thousand map and unmap calls per frame on the one lever the field measured), why the selection identity is a NEW `GpuBackendKind` member while the golden identity stays SHARED with the incumbent's 36 grids, why nested `Begin` is legal on this backend and still forbidden by the portable seam contract, and why the flip-model swapchain is deliberately NOT in v1. Carries the field numbers, the thirteen-site enum-append audit, and four measurement gates each with a kill switch and an exit criterion, plus one recorded non-measurement. | **In progress. Work-breakdown rows 1 to 16 landed, and row 17 is part-landed** (rows 1 to 5, 7 and 13 released in v17.30.0, rows 6 and 8 plus the cross-row wirings released in v17.31.0, and rows 9 to 12 and 14 to 16 staged in 17.32.0 together with the device that makes all sixteen reachable, the `direct3d11-native` CI leg, its T3 WARP call-parity `[GpuFact]` and the soak counters gate 4 reads). What remains of row 17 is the rollout itself: the five gates and the `ProbeOS` flip, with each gate's current standing in the 2026-08-05 rollout record at the end of section 14. Phase 2 of the staged native GPU backend program ([#420](https://github.com/APKiwiOrg/KhaozEngine/issues/420)), specified by [#421](https://github.com/APKiwiOrg/KhaozEngine/issues/421), against the [#410](https://github.com/APKiwiOrg/KhaozEngine/issues/410) field defect and the [#415](https://github.com/APKiwiOrg/KhaozEngine/issues/415) to [#418](https://github.com/APKiwiOrg/KhaozEngine/issues/418) audit. Adjudicated from two complete competing drafts (continuity-first and clean-sheet), so section 2 is the one to read: it decides the recording model, the backend identity and the nested-`Begin` contract with the reasoning, and section 17 records what was rejected from BOTH drafts and why, including the flip-model swapchain that both proposed and both flagged as their riskiest untestable departure. Section 13 is the honesty ledger: nothing had run on a device when this was written, and every unproven bet names its measurement, its kill switch and the criterion that retires the switch. Implementation issues come from section 15, and nothing is done until they are filed. |
| [VULKAN-NATIVE-BACKEND-DESIGN-2026-08-05.md](design/VULKAN-NATIVE-BACKEND-DESIGN-2026-08-05.md) | The `KhaozEngine.Gpu.Vulkan` native backend behind `IGpuDevice`: why the command list emits `vkCmd*` at record time with no op stream and therefore no M1-style A/B hanging over the rollout, why dynamic rendering replaces the render-pass port (the thing on offer to port is three uncached passes per framebuffer rebuilt on every resize, so "port a correct thing" was never the choice), why nothing is extracted into a shared home with `Gpu.D3D11` while the ring's SEMANTIC TESTS are shared now, why image layouts are tracked list-locally against a canonical resting layout (which makes lists composable and N-concurrent recording safe by construction), and why the spec-correct present path is FORCED by the validation gate rather than merely preferred. Carries the corrected picture of the incumbent, a 103-row decisions table with an Origin column, eight bets each with a measurement, a kill switch, an exit criterion and a decision DEADLINE plus two recorded non-measurements, and a nineteen-row work breakdown. | **Spec complete, implementation in progress.** The nineteen implementation rows are tracked under [#510](https://github.com/APKiwiOrg/KhaozEngine/issues/510), and as of `17.34.0` rows 1 to 17 have landed: the package, the provider, the functional support probe and the `GpuBackendKind.VulkanNative` identity (its tokens, its guest mapping into the committed `vulkan` golden family, and the thirteen-site append audit answered a second time), then a real headless device with its timeline, its block suballocator, its command lists, its uniform ring, its whole resource factory, its descriptors and bind flush, dynamic rendering, the shader path, both pipelines, the barrier and layout tracker, the windowed swapchain with its present boundary, and the DRAW, DISPATCH AND TRANSFER PATHS ([#525](https://github.com/APKiwiOrg/KhaozEngine/issues/525)), which close the command list's refusal list entirely and freeze MV4's per-draw budget. Two of the doc's own claims were corrected in flight by that row and are marked in place: section 13 named the wrong driver call for `MaxMsaaSampleCount`, and section 10.3's present row is the `End` row, because `PRESENT_SRC_KHR` is the swapchain image's resting layout rather than a transition the boundary records. **All nineteen implementation rows have landed as of `17.34.0`**, ending with the capability parity row ([#528](https://github.com/APKiwiOrg/KhaozEngine/issues/528)) and the CI and rollout row ([#529](https://github.com/APKiwiOrg/KhaozEngine/issues/529)), which brought up the blocking `vulkan-native` leg, installed the first Khronos validation layer this repo has ever had and wired both of its tiers, and swept the docs. **What is left is the ROLLOUT, which is a different thing from the implementation.** The five gates of section 17 are all PENDING with their standing recorded in that section's rollout record: three of them wait on a leg that has been committed and has never run, and two of them (a field session on the reporting machine, and a human windowed pass) are things no CI leg can stand in for, which is section 16's MV9 in as many words. The `ProbeOS` flip that makes this the Linux default is the last step after those gates, not a consequence of the code existing. Phase 3 of the staged native GPU backend program ([#420](https://github.com/APKiwiOrg/KhaozEngine/issues/420)), following the shipped phase 2 ([D3D11-NATIVE-BACKEND-DESIGN](design/D3D11-NATIVE-BACKEND-DESIGN-2026-08-02.md)). Written against engine `17.32.0`. Adjudicated from two complete competing drafts (reuse-first and Vulkan-idiomatic), so section 2 is the one to read: eight contested decisions with the arguments and the ruling, including three ruled AGAINST BOTH drafts and three where the reuse-first prior wins outright. Section 2.1 is the one to read FIRST, because it corrects the evidence base both drafts argued from: the incumbent is `4.9.103`, whose Vulkan tree is byte-identical to upstream `v4.9.0`, one draft cited a master-based ref instead, and the six-stage barrier that draft's strongest argument rests on is not in the shipped package. What IS there is worse, and neither draft found it: the shipped barrier's destination access mask does not cover uniform reads at all. Section 16 is the honesty ledger, and it names two things nobody can currently measure (the swapchain has zero CI coverage on any leg, and no Vulkan device limit is observable in CI). The implementation issues come from section 18 and are filed. |
| [METAL-NATIVE-BACKEND-DESIGN-2026-08-09.md](design/METAL-NATIVE-BACKEND-DESIGN-2026-08-09.md) | The `KhaozEngine.Gpu.Metal` native backend behind `IGpuDevice`, the LAST one, after which Veldrid can leave the graph: why the binding indices are READ OUT of the emitted MSL rather than pinned into it (the route to pinning them does not exist at the seat #566 banked, because `libveldrid-spirv` exports three C entry points and none carries a binding table), why the incumbent's `colorAttachments[0]` clear collapse is a defect shipped engine code REACHES and is therefore a deliberate rendering change on the fleet's reference golden family rather than an invisible correction, why automatic hazard tracking is KEPT on a condition that is the exact inverse of the Vulkan phase's VMA decline (Metal has no synchronisation validator, so untracked would be a hazard class with no detector on the one leg whose real hardware really reorders), why the uniform ring is a CORRECTNESS change here rather than only a cost change, and why the bind flush uses Metal's array setters the fork's bindings do not declare. Carries the corrected picture of the incumbent, a decisions table with an Origin column, thirteen bets and recorded non-measurements, and a nineteen-row work breakdown. | **All nineteen rows landed in `17.35.0`, so the program is BUILT and the only thing left is the ROLLOUT: five gates PENDING with their instruments built and none read.** Row 18, the #531 extraction, landed last and re-assessed every candidate against the three implementations the list was written before: TWO extractions and THREE refusals rather than five and none. The `DeviceLiveness` latch moved whole into `KhaozEngine.Gpu/Internal/` and absorbed a fourth copy already sitting in that namespace for the Veldrid wrappers, and the counters moved at the CARRIERS (`WaitTotals`, `WaitAccumulator`, `RingPatchStats`) and were refused at the accumulation SITES, where every backend's counting rule differs for a reason argued in the code that holds it and `DrainCount` is a shipped seam channel a shared rule would move in the field. The diagnostic rate limiter, the shader-cache key and the completion timeline's bookkeeping were each refused in writing, and two of those three overturn a FACT the decisions table asserted rather than a judgement it made: Metal has no rate limiter and no disk shader cache and cannot have either, which the doc's own section 12.5 already knew and section 2.8 was never reconciled with. Row 19 is the last buildable row and it built the `metal-native` CI leg: hosted `macos-26` pinned by number, the incumbent Metal leg's whole-suite-on-every-trigger tier, a GUEST in the committed `metal` golden family, `KE_METAL_REQUIRED=1` so a dormant row cannot report green on the one leg built to run it, both of Metal's validation tiers (`MTL_DEBUG_LAYER` every run, `MTL_SHADER_VALIDATION` on the deep sweep) and `MTL_CAPTURE_ENABLED` on the deep sweep alone, which is the only thing in the net that runs the frame-capture interop end to end. That leg is the strongest regression net in the program: the other two native legs run golden-only on push on software rasterizers, and this one runs everything on a real GPU every time. It also fixed what row 14 measured and handed on, and the fix is NOT the one the work breakdown predicted: the engine-wide `[GpuFact]` suite failed 284 of 6039 rows under `metal-native`, every failure the same registration refusal, because `MetalCompletionHandler`'s four-slot completion table met a suite where every device-building class builds one of these devices in parallel. Both `[GpuFact]` assemblies already carried the `NativeDeviceLifecycle` definition the design's cell asked for, so the table was raised instead, on a measurement: a fully serialised run of that assembly takes 20 minutes against 4 parallel, and the collection is backend-agnostic, so serialising would have charged the Windows and Linux legs for a limit only Metal has. The five rollout gates are all PENDING with their instruments now BUILT and none READ, and the `ProbeOS` flip deliberately does NOT land with the leg: section 17 gates it on five green gates, this row can read none of them, and all three flip sites carry the doc comment saying what they wait for. Row 17 took MM6's measurement and PASSED it with three pixel probes plus a device-free mechanism row: the one-uniform-buffer-per-pipeline rule is the INCUMBENT'S numbering rather than a property of Metal, the rule is kept because the Veldrid Metal leg still ships, and its removal is filed as [#604](https://github.com/APKiwiOrg/KhaozEngine/issues/604). The nineteen implementation rows of section 18 are filed as #567 to #585 and tracked under [#566](https://github.com/APKiwiOrg/KhaozEngine/issues/566). Row 15 made the backend WINDOWED and left `IGpuDevice` with no unbuilt member on it: the `CAMetalLayer` configured field for field from the incumbent, the drawable acquired at the present boundary for the NEXT frame and COUNTED into the acquire-wait pair the incumbent throttles on with nothing watching, `maximumDrawableCount` taking the last unclaimed consumer of `KE_METAL_FRAMES_IN_FLIGHT`, a nil drawable binding a device-owned orphan target so the frame records, submits, completes and COUNTS with only its present skipped, `displaySyncEnabled` written unconditionally where the incumbent writes it inside three values of an enum deprecated since macOS 10.15, and a resize queued and applied at the boundary after a drain where the incumbent applies it inline with none. It also corrected three of the doc's own claims in place, one of which changes what the honesty ledger says: **MM7's "zero automated coverage anywhere in the net" was over-general**, because a `CAMetalLayer` with no window, no view and no display server vends drawables, so the layer half runs in the `[GpuFact]` suite and what is genuinely uncovered is four Cocoa selectors and whether anything appears on a screen. The other two are in 7.1 and 11.3: M-W5's orphan target means the nil-drawable frame's draws LAND rather than going nowhere, and the depth-texture half of row 15's regression column has no occupant because the seam cannot ask for a swapchain depth attachment at all. Row 13 built the bind flush: one ARRAY call per (kind, stage) through the binding table, the offsets-only `setBufferOffset:` rebind per visible stage, a per-slot encoder-epoch stamp so a record-time upload's mid-pass blit re-activates the binds, the vertex-stream cache landing WITH its invalidation rather than without it, and the composed `frameBase + rangeOffset + callerDynamicOffset` against the segment the RECORDING captured. It measured two numbers 2.7 did not predict and both are recorded there: across the 34 shipped graphics programs the worst full activation is 6 array calls and NO program produces a non-contiguous index run, so the extra call a hole costs is never paid today. Row 4 built the engine-owned Objective-C interop layer (`Internal/ObjC/`, one file per class, one `objc_msgSend` prototype per signature shape), a real `MTLDevice` and one `MTLCommandQueue` behind HEADLESS creation, `KE_METAL_DEVICE` selection whose default is `MTLCreateSystemDefaultDevice()` rather than the enumeration's first element so `DeviceName` parity stays satisfiable by construction, the command-buffer error latch that closes [#427](https://github.com/APKiwiOrg/KhaozEngine/issues/427) for this leg (the incumbent reads `status` in one place and never reads `error`), a drain before teardown built from an empty command buffer because Metal has no device-level wait, and the autorelease rule as an IL walk over call sites rather than a field walk, because M-N5 requires a CALL on a path and V-D2's shape can only forbid a type. It also took the M-G3 fallback the row-1 control forced: `KE_METAL_VALIDATION` reports and warns rather than arming, since Metal reads its variables at launch, and the WARN for a variable set afterwards turned out to be detectable by comparing the managed environment against `getenv`. Row 3 appended `GpuBackendKind.MetalNative`, its tokens, its guest mapping into the committed `metal` family and `IsMetal()`, and answered the append audit a third time. It is the first append of the three whose sites were NOT correct by default: the two software frame-cap sites would have silently dropped the cap on a native Mac client, so both route through `IsMetal()` with the incumbent's arm as a conservative default that gate 5 settles by measurement (M-W3), and the third silent site, the Veldrid wrapper's frame-capture gate, answers that it must not be widened at all. Row 5 built the completion timeline ahead of the rows it looks like it should follow, because row 8's uniform ring recycles segments against a completion value: one device `MTLSharedEvent`, `IGpuFence` as a remembered value on it, a sliced counted drain, and the `[UnmanagedCallersOnly]` completion handler that only reports. It decided two things the doc left open and 10.1 now records both: the drain's timeout is a 250ms SLICE rather than a number nobody expects to reach, because a Metal command-buffer failure is asynchronous and the only notification is the liveness flip a single unbounded wait cannot observe, and the handler routes to its latch by reading the device off the command buffer. Row 6 built the resources: buffers on Shared storage with the incumbent's four-byte allocation rounding, Private textures, samplers whose two incumbent conditionals are RESOLVED rather than reproduced (the border colour's condition is always true here and the compare function's is never true), fences off the row-5 timeline, and the shared WRAP sampler pair whose engine-static namesakes CLAMP, which is the collision that cost two goldens on the Direct3D 11 leg. Its risky half is the staging subresource layout, which every golden reads back through, so it is pinned by a checked-in 232-row table generated from Veldrid's own nine functions rather than re-derived from the implementation. `Map` WAITS on the read path, closing a guarantee that rests on a caller convention in the incumbent, and a device-level `UpdateTexture` records into a device-owned setup command buffer instead of issuing a whole queue submit per call (eight uploads, one batch, measured). It also landed M-M10's eager view set as EMPTY and recorded why that is stronger than the rule asked for: nothing this seam can express NARROWS a texture, so the package declares no view factory at all. Two device-only lessons are in 9.3: a command buffer held across autorelease-pool scopes must be retained, and the blit copy is the one ABI shape row 1's spike does not cover. Row 1 built the `KhaozEngine.Gpu.Metal` skeleton, its guard rows and the three verification spikes, and the spikes RAN on real hardware: the interop layer is ABI-clean on an Apple M2 Max, the `MTLCompileOptions` defaults are measured (MSL 3.2, fast math on), and **the MSL name-join spike REFUTED M-B1**, the design's largest risk, and 2.2b re-adjudicated it on the spike's second measurement: the join is keyed on the SPIR-V ID and reaches 159 of 159, so rows 9, 10 and 13 build that and [#586](https://github.com/APKiwiOrg/KhaozEngine/issues/586)'s fallback is closed as superseded. Read 2.2, then both halves of 2.2a, then 2.2b, which is the operative ruling. Row 2 added `KhaozEngineMetal.Register()`, the provider, M-N4's four-read functional probe and the `MetalBackendRegistration` seat, keyed on a pinned ordinal until row 3 appended `MetalNative = 6` in this same version, and it measured one more of the design's premises wrong: Metal exposes NO constant-buffer offset alignment query at all, so the probe asks for it through `respondsToSelector:` and falls back to `minimumLinearTextureAlignmentForPixelFormat:`, which reads 16 and agrees with the number the incumbent hardcodes. Phase 4 of the staged native GPU backend program ([#420](https://github.com/APKiwiOrg/KhaozEngine/issues/420)), following the shipped phase 2 ([D3D11-NATIVE-BACKEND-DESIGN](design/D3D11-NATIVE-BACKEND-DESIGN-2026-08-02.md)) and phase 3 ([VULKAN-NATIVE-BACKEND-DESIGN](design/VULKAN-NATIVE-BACKEND-DESIGN-2026-08-05.md)). Written against engine `17.34.0`. Adjudicated from two complete competing drafts (reuse-first and Metal-idiomatic), so section 2 is the one to read: eleven contested decisions with the arguments and the ruling, FOUR of them ruled AGAINST BOTH drafts. Section 2.1 is the one to read FIRST, because it corrects the evidence base and three of its corrections overturn a draft's central claim: the MRT clear defect IS reached by `ModelRenderer.BeginModelPass`, which carries a shipped comment about it, so the draft that called the fix golden-invisible was wrong. The incumbent's `SetPipelineCore` has NO identity guard, so the draft that said reproducing one was the ruling was wrong. And `SlathRepro`, which BOTH drafts made a rollout gate, was deleted in `9.0.0`. Section 16 is the honesty ledger and it names six things nobody can measure, including that the swapchain has zero CI coverage on any leg, that Metal has no synchronisation validator at all, and that no Metal field baseline exists anywhere in this program's record, so gate 4 has to take its own. Section 19 resolves the contradiction nobody had written down: three separate "the incumbent stays selectable indefinitely" sentences would mean Veldrid never leaves, so the three incumbent legs retire TOGETHER in a closing act after all three native backends pass their own gate 4. |
| [SHADOW-RERECORD-STALL-DESIGN-2026-08-12.md](design/SHADOW-RERECORD-STALL-DESIGN-2026-08-12.md) | The four ways to stop the key-light depth pass re-recording the whole cascade atlas on a scene where nothing visible changed ([#410](https://github.com/APKiwiOrg/KhaozEngine/issues/410)): a rigid/skinned split, a light-movement epsilon, per-cascade dirty, and caster-count-scaled budgets, each costed against a trace-shaped bench and against the ghosting risk the issue forbids. Section 2 is the one to read: the fit already texel-snaps TRANSLATION and gives rotation no equivalent, so the epsilon is a missing half rather than a new idea, and the receiver tail is rebuilt from the current fit every frame, which is why the epsilon has to freeze the light DIRECTION rather than the fitted matrix. | **Option B shipped in 17.36.1, the rest unbuilt.** The light-movement epsilon landed as `ShadowSettings.ShadowLightHoldTexels` / `ShadowLightHoldCasterHeight` (see section 3.3's shipped note for the five things that came out differently, including the two knobs and the float resolvability floor), taking the bench's two sun-moving rows from 200 rendered frames out of 200 to 0 and Ruinborne's daylight rate to one re-fit every 28.6 frames. #410 stays open until a Windows field capture confirms it. A-lite, per-cascade dirty, the two-atlas split and the budgets are all still unbuilt, in that order of preference. The bench (`KhaozEngine.Render.Tests/Gpu/ShadowRerecordBenchGpuTests.cs`) is the before-and-after: 1,525 casters in 4,593 instances over four cascades, four dirty regimes measured in one process, dirty-reason facts asserted and milliseconds reported rather than gated. A serialized local Metal run says the re-record costs about 0.15 ms of encode and about 0.41 ms of everything else per frame, that a skipped frame still pays 0.042 ms building the signature it compares, and that a sun rotation of a thousandth of a degree per frame re-recorded all 404 spans on all 200 measured frames before option B (it now skips all 200). Recommendation was the light epsilon first (done), skinned-caster hashing second (palettes AND world matrices AND the caster set, because a palette-only compare ghosts a translating still pose), per-cascade dirty third, and the two-atlas rigid/skinned split left unbuilt. Explicitly NOT the fix for the 10 fps plateau that opened #410: that was a D3D11 per-call tax taken by the 4.9.101 fork bump, whose [#418](https://github.com/APKiwiOrg/KhaozEngine/issues/418) bind batching is the mechanism match rather than an isolated A/B. |
| [TILE-WORLD-DESIGN-2026-08-15.md](design/TILE-WORLD-DESIGN-2026-08-15.md) | An OSRS-style tile world as a SIBLING of the continuous-terrain map stack, not an extension of it ([#629](https://github.com/APKiwiOrg/KhaozEngine/issues/629)): `TileWorld` (64x64 regions, N planes, a global tile-corner height lattice in centimetre shorts, underlay/shaped-overlay/settings layers, tile-anchored objects and markers, catalogs by id, a stable-name directory form with per-region hashes and a composed `WorldHash`), a DERIVED per-edge collision map baked from settings + object archetypes with walls mirrored onto the neighbour tile, a deterministic BFS pathfinder with OSRS's nearest-reachable rule, `TileRaycast` and `TilePrefab`, a vertex-colour ground mesher over the existing lit model path (no new shader), objects through the existing `PropLayer`, an editor kernel (`KhaozEngine.Editor`) extracted from `MapEditor` behind non-obsolete forwarding aliases (warnings are errors fleet-wide, so `[Obsolete]` would break Ruinborne's head exactly like the rename), the `TileEditor` GUI and the `ke-tileedit` MCP tool executing ONE command set. Section 3 is the one to read: the fold-in and the terrain-overlay alternatives are scored and rejected, with what the sibling stack actually reuses verified against the code. | **R1 and R2 shipped (the document package, then `TileWorld.Render3D`: ground mesher, props, `ITileWorldScene` seam, view, region residency, headless snapshot, two cross-backend goldens), R3 pending (editor kernel, tile editor, MCP tool).** The kernel extraction moved from R2 to R3, because it has no consumer until the editor exists. Sub-project 1 of the Grimhollow program (a new low-poly 3D MMO on the Ruinborne shell): tools plus one authored 3x3-region starter world, fly-camera only, with tick-based click-to-walk deliberately left to the next sub-project's spec. Three release rounds (document + collision, kernel + renderer, editor + MCP tool), then the Grimhollow side (scaffold, editor head, a Blender-generated greybox kit, catalogs, the world) in that repo. |

**Adding a design doc.** Put it in `design/`, add a row above, and give it a status line. A doc for a program
that is in flight carries its open work in GitHub Issues (`kind/backlog` / `kind/roadmap`), never as a private
backlog here: a follow-up recorded only in a design doc is invisible to the ledger and gets lost.

## Process docs

- [../AGENTS.md](../AGENTS.md) - concurrent-dev rule (worktree per change), the release ritual, build/test commands.
- Release ritual, short form: bump `Directory.Build.props` `<KhaozEngineVersion>` -> add the `CHANGELOG.md` entry
  (its newest `## X.Y.Z` heading must be the new version) -> update every `<PackageReference>` example in `README.md`
  and `USING-KHAOZENGINE.md` -> `dotnet pack -c Release -o ./local-feed` -> commit -> `scripts/tag-release.sh` (annotated `vX.Y.Z`) ->
  push `main` + tag. (Don't hand-type `git tag vX.Y.Z`: a lightweight tag is rejected by the `pre-push` hook.)
